Dashboard split (2/?): extract idle-tasks + insights panels

Continues the per-feature module split. Behavior-preserving (same code,
relocated into modules; esbuild bundles back to one IIFE).

- panels/idle-tasks.js: idle-task queue CRUD + history (self-contained;
  lib + state.idleTasksCache only).
- panels/insights.js: lessons, preferences, orchestrator, manuscript-hub
  cards. Preserves the window.deletePreference / orchAction / orchRemove
  globals used by inline onclick handlers in this panel's HTML.
- main.js imports the two; 2390 -> 2021 lines.

Remaining (tracked in docs/TODO.md): settings, then the interconnected core
(projects + context-continuity, home, hq, chat, library); nav (switchPanel),
status (polling) and theme stay in main.js as the app shell.

Verified: build clean (token placeholder + the 3 window.* globals present in
the bundle, no dup symbols); npm test green (29 unit + API + smoke).
