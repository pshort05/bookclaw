Dashboard split (4/?): extract projects panel (the big one)

Continues the per-feature module split. Behavior-preserving.

- panels/projects.js: 16 functions — templates, create/pipeline modal, project
  list, detail view (steps + per-step model picker), files, compile, and the
  context & continuity tabs. Keeps defaultTemplates internal. Uses lib +
  state.*; imports renderHomeProjects from the main shell (home stays in main
  for now). main.js exports renderHomeProjects and imports projects'
  loadTemplates/loadProjects/showProjectList/openProjectDetail — esbuild
  resolves the cycle.
- main.js: 1559 -> 573 lines.

Remaining (TODO): the smaller panels — chat, library, home, hq, activity-feed;
nav (switchPanel), status (polling), theme + init stay in main.js as the shell.

Verified: build clean (token placeholder + the 3 window.* globals present,
no dup symbols); npm test green (29 unit + API + smoke).
