docs: add TODO for handling provider 429 (rate-limit) errors

- Surfaced by tests/openrouter-pipeline.sh: OpenRouter returned HTTP 429 on the
  free google/gemma-3-4b-it model during rapid sequential calls, failing steps.
- TODO: retry with backoff honoring Retry-After in the router's HTTP completion
  paths, then fall through to getFallbackProvider before throwing. Outbound
  provider throttling (distinct from the inbound per-channel limiter).
