Dashboard: AI-spend widget in the Manuscript Hub (Home)

Surfaces the (now-accurate) AI cost in the UI — previously only via /api/costs.

- panels/insights.js (loadHub): fetch /api/costs alongside /api/hub and render
  an "AI spend" row in the Manuscript Hub — daily and monthly spend vs. the
  configured caps + a daily budget bar; turns red / shows "(over budget)" when
  the daily/monthly cap is exceeded. Cost comes from CostTracker, which now
  reflects the real provider charge (OpenRouter usage.cost).
- docs/TODO.md: the cost-follow-on item's "spent so far" panel marked done;
  optional /api/v1/credits (account total) + /api/v1/models (pre-call
  estimates) follow-ons remain.

Verified: dashboard build clean (/api/costs fetch + widget bundled, token +
window globals intact); npm test green (34 unit + API + smoke).
