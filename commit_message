Remove footer author-links icons (Site/YT/Sub/Tools) from dashboard

- dashboard/dist/index.html: delete the .footer-links block, its CSS, the
  mobile display:none rule, and the applyFooterLinks() JS + boot call.
- config/default.json + settings.routes.ts: drop the now-orphaned
  branding.footerLinks config and the /api/config branding field (the
  removed JS was their only consumer).
- docs: move the item from TODO.md to COMPLETED.md.
