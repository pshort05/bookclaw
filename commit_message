Rename project: AuthorClaw -> BookClaw (code + env vars + class)

- Bulk-rename all occurrences across 147 tracked files: AuthorClaw->BookClaw,
  AUTHORCLAW->BOOKCLAW (incl. standalone all-caps banners the runbook's
  AUTHORCLAW_ rule would have missed), authorclaw->bookclaw
- Decision 2a (hard env rename): AUTHORCLAW_* env vars -> BOOKCLAW_*; no
  dual-read fallback. Local .env updated in place (var value preserved); no
  vault.enc existed, so no credential migration needed
- Decision 4a: AuthorClawGateway class -> BookClawGateway
- Verified: tsc --noEmit clean, smoke test 16/16 pass, 0 residual references
- Remote rename (1a) and local directory rename (3a) handled separately
