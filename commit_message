Prune unused imports left in index.ts after the Level 1 refactor

- Remove 4 utility imports orphaned when their logic moved out of index.ts:
  fileURLToPath (url) + dirname (path) -> moved to paths.ts; randomBytes
  (crypto) -> moved to the security init phase; createAPIRoutes -> now used
  only by init/phase-11-http.ts.
- Surgical: drop the url and createAPIRoutes lines, keep join + timingSafeEqual
  on their shared lines. The service-class imports are all still used as
  public field type annotations, so none were removable.
- docs: move the item from TODO.md to COMPLETED.md.

Verified: 0 unused imports remain (re-scan), tsc --noEmit clean, npm test green
(26 unit + 17 API + 19 smoke).
