Fix init-ordering bug: characterVoices wired with undefined styleClone

- Phase 6g9 wired characterVoices.setStyleClone(gw.styleClone) before
  styleClone was constructed in Phase 6j, passing undefined.
- Move the wiring to Phase 6j (init/phase-09-export-wave.ts), right after
  styleClone is constructed; characterVoices.initialize() only does mkdir and
  never touches styleClone, so this ordering is safe.
- Update the phase doc-comments to describe the deferred wiring.
- docs: move the item from TODO.md to COMPLETED.md.

Verified: tsc --noEmit clean; npm run test:smoke all pass.
