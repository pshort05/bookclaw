docs: reflect Phase 0 storage change (host bind-mount + schema marker)

Follow-up to 096c81d — update the prose docs to match the now-deployed storage
model.

- CLAUDE.md: list workspace/.bookclaw/workspace.json (schema marker) under
  Stateful directories; note that workspace/ is a host bind-mount
  (BOOKCLAW_WORKSPACE_PATH, default /home/paul/bookclaw-workspace) rather than a
  named volume; add a Conventions bullet on the bind-mount + the uid-999
  ownership-alignment that deploy.sh performs.
- docs/LAUNCH-GUIDE.md: add the host workspace mount path to the data-locations
  table (what to back up; owned by uid 999 so host writes need sudo).
- docs/COMPLETED.md: add the Phase 0 entry (workspace bind-mount + marker,
  deployed to Mercury 2026-06-05, commit 096c81d).
