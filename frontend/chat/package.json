{
  "name": "@bookclaw/chat",
  "version": "0.0.0",
  "type": "module",
  "scripts": { "dev": "vite", "build": "tsc -b && vite build", "preview": "vite preview" },
  "comment": "All deps are build-time only: Vite bundles them into dist/, which is what the chat server serves. Declaring them devDependencies keeps them out of the production image after `npm prune --omit=dev`.",
  "devDependencies": {
    "@bookclaw/shared": "*",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
