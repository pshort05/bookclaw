# TODO

Tracking list of items surfaced while writing CLAUDE.md. Grouped by effort.

Anything currently being worked on must appear in this list. When an item is finished, move it to [COMPLETED.md](COMPLETED.md) with a `YYYY-MM-DD` completion date — don't just check the box and leave it here.

## Targeted feature roadmap (product, consolidated 2026-06-21)

A single ranked, deduplicated list of the **differentiating product features** distilled from [STRATEGY-LEADING-AI-WRITING-ASSISTANT.md](STRATEGY-LEADING-AI-WRITING-ASSISTANT.md) + the six tool reviews ([NARRATIVE-ENGINE](NARRATIVE-ENGINE-INTEGRATION.md), [STORYTHREAD](STORYTHREAD-STUDIO-INTEGRATION.md), [MIRRORSHARD](MIRRORSHARD-INTEGRATION.md), [NOVELMINT-TOOLS](NOVELMINT-TOOLS-REVIEW.md), [CLAUDE-CODE-METHODS](CLAUDE-CODE-WRITING-METHODS-REVIEW.md), [LONEWRITER](LONEWRITER-REVIEW.md)). **Detailed scoping for each item lives in the linked doc** — this is the canonical index; the scattered strategy/review bullets that fed it were removed from "Larger items" below so each feature appears once. The **multi-author/multi-book studio** (North Star, below) is the umbrella platform these build on; it is not re-listed here.

Ranked by impact (differentiation × reach × leverage). Difficulty scale: Low / Low-Medium / Medium / Medium-High / High.

| # | Feature | Why it's impactful | Difficulty | Detail |
|---|---------|--------------------|------------|--------|
| **0** | **Import the `~/data/Writing` assets** (authors, PKstyle voice, named editor personas, curated craft prompts, series bibles) | **Enabler** — pure-data import that unblocks #1, #2, #7 and populates the multi-author studio with real identities. Do first. | Low-Medium | STRATEGY Item 4 |
| **1** | **The "Easy Button" onboarding** — 3-click novel wizard + Starter Bundles + optional "AI Muse" chat onboarding (progressive disclosure, not a separate app) | Biggest reach: turns a power engine into a beginner-usable product without forking it. Almost no tool has both. | Medium | STRATEGY Item 1 + addendum #6 |
| **2** | **Author Voice DNA + Adaptive Voice Auto-Tuning** — capture each author's style DNA (PKstyle seed) and a learning loop that updates the voice profile from human edits | "Write in MY voice" is a top market demand; BookClaw can do it *per author* across many books, and self-sharpen from edits. `style-clone.ts`+`track-changes.ts` exist. | Medium | STRATEGY #3 + addendum #1 |
| **3** | **Deep continuity engine** — Character Knowledge Matrix ("who knows what, when") + Selective Exclusion (dream/flashback exemption) + red-herring ledger | Leads the #1 market theme (continuity) deeper than rivals; the Matrix is a new dimension beyond entity tracking. Corroborated by two sources. | Medium | CLAUDE-CODE-METHODS, LONEWRITER |
| **4** | **Importance-tiered context economy** — core/present/background/contextual/hidden trait injection + section-level inclusion modes | Cuts prompt bloat/cost, adds the Hidden-subtext craft control, and compounds across a long book. Attacks the "inject whole bible every step" problem. | Medium | STORYTHREAD, NOVELMINT |
| **5** | **Narrative Physics Engine (NPE)** — author-level structural rulebook + inspect→rewrite compliance loop | A third author-identity axis (causality/pacing/resolution physics) no competitor offers; North-Star aligned (identity = config). | Medium-High | NARRATIVE-ENGINE |
| **6** | **Pro publishing last mile** — WritingUtils format finisher (KDP DOCX, drop caps, scene breaks, typeset) + print-interior PDF + unified Launch Orchestrator (one phase: PDF/EPUB/metadata/cover/ad-copy/calendar) | Idea→uploadable book including the typeset print finish most tools skip. Many services already exist; mostly wiring. | Low-Medium → Medium | STRATEGY Item 3, NOVELMINT, addendum #4 |
| **7** | **Curated craft library at scale** — Atelier craft-prompt taxonomy + named editor personas (Maeve/Neil/…) + structure frameworks (Crucible/Farland/Actantial) + deterministic anti-pattern linter | Breadth + named-persona polish no rival matches; mostly data the owner already has. | Low-Medium | NOVELMINT, CLAUDE-CODE-METHODS, MIRRORSHARD |
| **8** | **Editorial Council** — debating named-persona beta panel → Judge synthesis | High-fidelity critique that reuses the imported editors + parallel engine + the `editorial-outline-council` pattern. Near-assembly. | Medium | STRATEGY addendum #5 |
| **9** | **AI-as-reviewer inline editing (Smart Advisor)** — anchored findings + word-level diff + accept/ignore/re-cast + micro-ops | Captures the large "AI should edit, not ghostwrite" segment; makes BookClaw a co-writer, not only an autopilot. | High (frontend) | STORYTHREAD |
| **10** | **Branching Drafts / "Multiverse Mode"** + CYOA/Twine export | Draft 2-3 next-scene directions in parallel, version the rest; export branches to interactive fiction. Distinctive, demo-friendly. `file-versions.ts`+parallel exist. | Medium | STRATEGY addendum #2 |
| **11** | **Predictive Co-Writing / "Drive Mode"** — ghost-text autocomplete in the author's voice | Fills the Sudowrite autocomplete gap; covers the manual-writing segment the strategy omitted. | High | STRATEGY addendum #3 |
| **12** | **Hybrid semantic memory + bible knowledge-graph** — embedding-based recall over long books/series + visual worldbuilding graph | Closes the semantic-memory + graph themes at once. LoneWriter proves a light local model suffices; `memory-search.ts` is FTS5 keyword (real build, not free). | High (infra) | LONEWRITER, MIRRORSHARD |
| **13** | **Brainstorm primitives now, visual idea-graph canvas later** — Missing Link + Node Alchemy (text-in/out); full node/edge canvas as a future studio surface | Non-linear ideation BookClaw lacks. Primitives are cheap; the canvas is a large, autonomous-first-adjacent build (gate on real demand). | Low (primitives) / High (canvas) | MIRRORSHARD |
| **14** | **Monetization layer** — Storefront Generator (+Stripe direct sales) + Serialization Orchestrator (drip to Substack/Patreon/WordPress) | A revenue angle the product lacks; builds on the existing site rendering. | Medium-High | STRATEGY addendum |
| **15** | **Try-fail & escalation auditor (+ crucible check)** — per-protagonist detection of attempt→fail→escalate cycles; verify early attempts fail and each conflict *deepens and/or broadens*; flag conflicts resolved too easily; confirm a binding "crucible" traps characters together | Extends structure-check / plot-promises into the try-fail layer rivals model only as generic tension graphs; a recognized craft concept (Sanderson "Yes-but/No-and"; the "crucible" per Bell). | Medium | [Craft-research features](#craft-research-future-features-2026-06-22) |
| **16** | **Resonance engine** — internal motif payoff tracker (warn when a climax invokes an under-seeded motif), universal-experience beat detection, generation-time motif/archetype injection | Packages the "resonance" craft concept as tooling no rival ships; today only generic theme/motif feedback exists (ProWritingAid / Marlowe / Studio Vi). | Medium-High | [Craft-research features](#craft-research-future-features-2026-06-22) |

**Sequencing:** #0 first (unblocks the rest). Then #1–#2 for *reach* (Easy Button on top of the imported bundles + voice DNA). Then the quality differentiators #3–#5 (backend-heavy, already scoped) and the #6–#7 last-mile + craft library (mostly data/wiring). #8–#10 are mid-effort experience wins; #11–#14 are the higher-effort / later bets (autocomplete, semantic infra, idea-graph, monetization).

## Craft-research future features (2026-06-22)

Two differentiating craft features distilled from a generic review of four writing-craft books (owner ask 2026-06-22; author name and titles deliberately omitted from the research). Indexed as **#15–#16** in the [Targeted feature roadmap](#targeted-feature-roadmap-product-consolidated-2026-06-21) above. Each was checked against existing tools and craft literature **before** being added, so the scoping records what is genuinely new vs. already served elsewhere.

- [ ] **#16 — Resonance engine.** A manuscript analyzer + generation aid for "resonance" — the emotional power a story gains by echoing things the reader already loves. Three parts: (a) an **internal motif tracker** that finds recurring images/objects/beats, counts occurrences, and warns when a climax or payoff invokes a motif that was *under-seeded* earlier (the inverse of plot-promises: promises track explicit hooks, this tracks repeated motifs); (b) a **universal-experience detector** that tags scenes against a library of near-universal life milestones (leaving home, losing a mentor, first day, awe at nature) and suggests where one could be added; and (c) **generation-time injection** of an active motif list + genre archetypes into drafting prompts (via `buildSystemPrompt`) so generated prose carries resonance by construction. Relates to `context-engine.ts` (entity/motif extraction), the world-repository motif material, and the payoff model in `plot-promises.ts`. **Research validation (2026-06-22):** "resonance" is a recognized craft concept (K.M. Weiland / Helping Writers Become Authors frames "cohesion and resonance" as standard craft) and motif/recurring-imagery is standard technique. Generic theme/motif feedback already exists in tools — ProWritingAid's Manuscript Analysis claims to judge whether "themes and motifs resonate," Studio Vi uses topic modeling to surface motifs, Marlowe analyzes theme/subplots — but **none packages the motif-payoff seeding warning, universal-experience beat detection, or generation-time motif injection**, so this is the most novel of the two. **Difficulty: Medium-High.**

## Consistency engine — Worldfall-inspired additions (2026-06-23)

Two low-effort, high-leverage additions to the `consistency/` engine, distilled from the worldfall.ink "Unit tests for a novel" post — an external project with the same thesis ("a novel is a thousand small promises; stop trusting your head") but an **inverse, author-declarative** architecture. Our factual-continuity layer already leads theirs (we have the Character Knowledge Matrix, selective dream/flashback exclusion, and storyTime/transition modeling, which they explicitly lack); these two close gaps in the tooling *around* facts.

- [ ] **Reverse index / "impact list" (fact → chapters).** For every canon/bible fact, list every chapter whose extracted facts depend on it, so editing a world/bible fact yields an instant worklist of scenes to revise. We already store `LedgerFact`s keyed by chapter/scene in `fact-store.ts` (SQLite), so this is the inverse query + a report endpoint + a studio-panel column. Pairs with the World Repository (edit a world-repo doc → which chapters to revisit). **Difficulty: Low.**
- [ ] **Orphan-fact / "Chekhov's gun" report.** Flag canonical / World-Repository facts that no chapter ever asserts — unfired guns that are either a cut candidate (decoration) or a missing scene. Couples directly to the World Repository relevance-pull just shipped (Phase 3): a world doc pulled into no book's bible, or a bible/canon fact dramatized in no chapter, surfaces on the report. **Difficulty: Low-Medium.**

## Consistency engine — production issues ("Love Between Departures" run, 2026-06-23)

Issues surfaced running the audit against "Love Between Departures" on the Neptune writing instance. The three clear-cut bugs (job-state, cost, activity) are **DONE** — see [COMPLETED.md](COMPLETED.md) 2026-06-23; **#3** (emotional-state false positives across time skips) is **DONE** — see [COMPLETED.md](COMPLETED.md) 2026-06-24; **#2** (downloadable report asset type) is **DONE** — see [COMPLETED.md](COMPLETED.md) 2026-06-24; **#6** (per-run/per-book model selection) is **DONE** — see [COMPLETED.md](COMPLETED.md) 2026-06-25. All production issues from this run are now resolved.

Larger ideas from the same worldfall.ink review, parked for later scoping. The throughline is their **declarative** model — the author (LLM-proposed, human-confirmed) *declares* per-chapter which facts/motives a scene serves, and a deterministic linter gates new prose on it — vs. our **extractive** model (LLM extracts facts from finished prose; post-hoc audit). These would graft a declarative/assertion layer onto our engine.

- [ ] **Reliable physical-trait tracking (eye color, hair color, height, weight, build, marks) [owner ask 2026-06-26].** The extractor prompt already lists most physical attributes as `immutable` facts ("eye color, hair, scars, height, species, age" — `extractor.ts:166`), so they're *in scope* today — but tracking is LLM-best-effort with no guarantee, and **weight/body-build are not listed**. Scope: (a) add weight, build/body-type, and distinguishing marks to the extractor's physical-attribute list; (b) make these high-value immutable traits reliably captured + cross-checked — e.g. a dedicated per-character trait profile (seeded from the World Repository character docs / persona) that the audit always reconciles against, so an eye-colour flip can't slip through just because the LLM didn't re-extract it in a later chapter. Relates to roadmap **#3 (deep continuity engine)** and the character-knowledge matrix. **Difficulty: Medium.**
- [ ] **No-unmotivated-action check + character "maxims" (Wants / Fears / Lines).** Their standout idea and a genuinely new *kind* of check for us — a "unit test for character." Each character declares wants (W), fears (Fx), and **lines** (L = the thing they will never do); each meaningful choice in a chapter must `serve` a declared motive **on that same character**, else it is a finding (either the prose has plot-driven action → fix prose, or the canon is missing a motive → fix canon). A craft/character dimension our purely *factual* auditor misses entirely. New finding category alongside `contradiction/continuity/knowledge-violation`; maxims live in the World Repository character docs / persona; needs an LLM-proposed → human-confirmed motive projection per scene. Relates to roadmap **#3 (deep continuity engine)**. **Difficulty: Medium-High.**
- [ ] **Beliefs as a distinct fact kind + dramatic-irony ledger.** Add a `belief` `FactType` (a faction or character can hold a *wrong* belief, kept distinct from `fact`/`rule` so it is never silently promoted into truth). The gap between the belief table and the fact table is a queryable list of dramatic ironies — things the reader can know that the characters can't — to track and "spend like a budget." Complements the Character Knowledge Matrix (who-*knows* vs. who-*wrongly-believes*). **Difficulty: Medium.**
- [ ] **Consistency-as-a-gate (red run = stop sign).** Their linter gates new prose (exit 0/1, pre-commit); ours is post-hoc / on-demand. Optionally run the consistency audit as a pipeline gate so a high-severity finding blocks or flags continuation of the next generation step (reuse the `ProjectEngine` step model + the `ConfirmationGate` posture). **Difficulty: Medium.**
- [ ] **Open-questions aggregation.** Let a chapter declare deliberately-unresolved items (`open_questions`), gathered into one owner-tracked list so ambiguity stays a decision with an owner rather than a forgotten loose end. A lighter cousin of `plot-promises.ts`; could extend it. **Difficulty: Low.**
- [ ] **(Reader-facing, non-consistency) Spoiler-aware self-building wiki — "the Margin."** Their canon graph renders into a reader wiki that **unfolds with the reader's position** (each fact knows the chapter it enters in, so entries appear only once read) plus hover-cards in the prose. Overlaps the World Repository appendix render; the new twist is reader-position-gated reveal. Parked as a reader/publishing product idea, not a consistency feature. **Difficulty: Medium.**

## North Star — the ultimate goal (use this to weigh every other decision)

**This is the destination the project is steering toward. It is not a near-term task — it is the lens for evaluating other work.** When scoping any non-trivial change (refactors, the rename, new features, storage shapes, API design), prefer the option that moves toward — or at least does not move away from — this end state. Where a decision is a toss-up on its own merits, the tie-breaker is "which choice makes the North Star cheaper to reach later."

**The end state:** BookClaw runs as a **multi-author, multi-book studio**. Several books are in flight at once, each owned by a distinct **author identity**, written in its own **genre**, produced by its own **customizable pipeline** — e.g. a Romantasy from author A, a contemporary romance from author B, and a mundane sci-fi from author C, all simultaneously. Adding another author, genre, or pipeline variant should be **configuration, not code changes**.

**The four entities this implies (none exist as first-class concepts today):**

- **Book** — owns state across `planning → bible → production → revision → format → launch`, references an author profile + genre profile + pipeline, and carries its own history. Today `workspace/projects/<id>/` is a flat list with no "book" that spans phases.
- **Author profile** — a named identity bundle (its own SOUL/STYLE-GUIDE/VOICE-PROFILE/PERSONALITY), selectable per book, cloneable/editable without touching the global `workspace/soul/`. Today there is exactly one author identity, hardcoded to `workspace/soul/`.
- **Genre profile** — a named pack (tropes, beats, reader-expectation notes, comp-title pointers), selectable per book, replacing the free-text `genre` field. Today `genre` is free text on each project.
- **Customizable pipeline** — editable, data-driven, versioned step definitions: add/remove/reorder steps, edit each step's prompt, change its `taskType`/tier and `wordCountTarget`, attach skills — **all without editing code**. Selectable per book (and differing per author/genre). Today the 6 templates + `novel-pipeline` are hardcoded in `services/projects.ts`.

**How this guides decisions already on the books:**

- **God-class refactor** ([GOD-CLASS-REFACTOR.md](GOD-CLASS-REFACTOR.md)) — **Level 1 complete (2026-06-01, see [COMPLETED.md](COMPLETED.md)); this guidance now applies to Levels 2–3.** The North Star is *the* "explicit goal larger than tidiness" that the doc says Level 1 needs before starting. The `init/` split should leave a clean seam where book/author/genre/pipeline services slot in (`06e-project-engine.ts` is the one most affected); the routes split should land `pipeline.routes.ts` (already listed) as a real boundary; and the Level 3 plugin contract should treat **pipelines as plugin-shaped data**, alongside the AI-provider/channel/TTS plugin work. Don't refactor in a way that hardens the single-author / hardcoded-pipeline assumptions.
- **Rename to BookClaw** ([RENAME-PLAN.md](RENAME-PLAN.md)) — the chosen name *is* this vision (a studio of **books**, not one author's voice). The rename is a natural moment to stop hardcoding "the author" in banners/config/copy and to phrase operator-facing strings in book/author-profile terms, so the brand and the data model agree.

The full implementation breakdown lives below under **[Larger items → Multi-author, multi-book platform](#larger-items-worth-scoping-before-starting)**; this section is the framing, that item is the work. The product/market framing — who this is for, how it differs from AuthorClaw, and the five anchoring use cases — lives in **[PRODUCT-DIRECTION.md](PRODUCT-DIRECTION.md)**.

## Full security review (post-LAN-exposure)

Opening the bind from loopback to LAN turned several previously-defense-in-depth
assumptions into single points of failure. A proper security review should
cover at minimum:

- [ ] **Bearer token is readable from the public dashboard HTML.** The auth gate covers only `/api/*`; the dashboard at `/` and its static assets are public (`gateway/src/index.ts:280`), and `/` serves its HTML with the token injected (`__BOOKCLAW_AUTH_TOKEN__` placeholder). So in the **default posture** (auth token set, `BOOKCLAW_ALLOWED_IPS` unset) anyone who can reach the port can `curl http://host:3847/`, read the token out of the HTML, then authenticate — the bearer token does **not** stop a deliberate LAN actor; only the source-IP allowlist or the host firewall does.
  - **Threat model (decided):** single-user home LAN, occasional family curiosity. A casual family member won't view-source to extract the token, so token-injection is adequate *against that actor*. This is also why bearer-token was chosen over HMAC/mTLS (rejected as disproportionate). The exposure is therefore a **known, accepted limitation** for the home-LAN deployment — recorded here so it's a conscious decision, not a silent gap.
  - **What actually closes it** for a stronger threat model (e.g. VPS / untrusted LAN): set `BOOKCLAW_ALLOWED_IPS` to the trusted source IP(s)/CIDR(s) — this gates `/` too, in front of auth — and/or enforce at the host firewall / provider security group (the most robust control; mind the Docker bridge caveat that masks source IPs). A reverse proxy doing its own auth + TLS in front is the belt-and-suspenders option.
  - **If it ever needs a code fix:** gate `/` behind auth and hand the dashboard its token a different way (e.g. a one-time login form that sets an httpOnly cookie, or a short-lived session token), so the long-lived bearer token is never embedded in publicly-fetchable HTML. Out of scope for the current threat model — only revisit if the deployment posture changes.
  - **Related minor notes:** the `?token=` query fallback (native `<img>`/`<a>`/`<Audio>` GETs) puts the token in URLs, which proxies / access logs / browser history can capture — low risk on a trusted LAN but worth eliminating if `/` is ever hardened. And there is no TLS (plain HTTP on the LAN); a reverse proxy is the documented path when that matters.
- [ ] **Audit logging: source IP + native Linux log manager.** *(Deferred 2026-05-30 — design investigated, implementation parked.)* Two related pieces:
  - **(original) Audit-log the request source IP** on every gateway HTTP route and every WebSocket connection, so a compromise is forensically traceable. `workspace/.audit/` currently logs `websocket_connected` with socket id only. The IP plumbing already exists (`socketClientIp()`, `req.ip` with `trust proxy` set — from the source-IP allowlist work).
  - **(new ask) Route audit events to the native Linux log manager, with an off switch.** So events land in the OS-managed log (journald / syslog) with its rotation/retention/forwarding, not just the in-workspace file.
  - **Key finding (drives the design):** BookClaw is **Docker-first**, and the `node:22-slim` image has **no in-container journald or syslog** (`/dev/log` absent). So the container-native way to "rely on the native log manager" is to **emit structured audit events to stdout/stderr** and let the platform capture them: under a systemd unit, stdout → journald automatically; under Docker, stdout → the logging driver, which the user points at the host by setting `logging: driver: journald` (or `syslog`) in `docker/docker-compose.yml` (currently the default `json-file` driver, no custom logging). Writing directly to a `/dev/log` syslog socket would NOT work in the default Docker setup (needs `/dev/log` bind-mounted or a remote UDP syslog host), so it's the wrong default for this app — at most an advanced opt-in.
  - **Existing state to reconcile:** there is already a **hash-chained JSONL** audit log (`gateway/src/security/audit.ts` → `workspace/.audit/<date>.jsonl`, tamper-evident), the single sink behind `this.audit.log(category, action, data)` used app-wide (ip_blocked, confirmation events, etc.). The native target would be an **added sink**, not a replacement — journald/syslog don't hash-chain, so keeping the JSONL preserves tamper-evidence. (Lean: keep both, each independently toggleable.)
  - **Open decisions (all deferred):** mechanism (structured stdout *[recommended, cross-env, no dep]* vs. direct syslog socket vs. both); JSONL fate (keep both *[recommended]* vs. native-replaces vs. native-primary); off-switch scope (native sink only *[recommended, e.g. `BOOKCLAW_AUDIT_SYSLOG=0`]* vs. all audit logging vs. per-sink toggles). Implementation lands as a second sink inside `AuditLog.log()` plus the source-IP enrichment; add a smoke-test assertion (e.g. an audit line appears on stdout and carries the client IP).
- [ ] **Vault key on a multi-host Docker volume.** Verify `config/.vault/vault.enc` permissions survive volume mounts. Document the backup-and-restore path for `BOOKCLAW_VAULT_KEY` since losing it means losing every stored credential.
- [ ] **Outbound egress posture.** The AI router and `ResearchGate` make outbound calls to Anthropic / Gemini / OpenAI / DeepSeek / OpenRouter / allowlisted research domains. Confirm `helmet`-level CSP doesn't accidentally weaken egress controls. (Note: `connectSrc` was tightened to `'self'` on 2026-05-30 — but CSP governs the *browser* dashboard, not the server's own outbound AI/research calls, so this item is about the server-side egress path, not CSP.)
## Deferred from code review 2026-06-12 (see docs/CODE-REVIEW-2026-06-12.md + COMPLETED.md)

The full-codebase review's CRITICAL/HIGH/MEDIUM findings and all safe LOW items were **fixed in a 2026-06-12 batch** (recorded in COMPLETED.md). Only these genuinely-large or intentionally-deferred items remain:

- [ ] **schemaVersion gate at the data-output path.** Template writes (`writeTemplate`/`repull`) refuse on non-`ok` status, and `BookService.setPhase` now also carries the `assertWritable` gate (added with the 2026-06-14 v1→v2 bump — see COMPLETED.md). **Still deferred:** the engine's `dataDirOf` data-output path enforcement — make `dataDirOf` (or its callers) refuse on non-`ok` (see the deferral comment at `book.ts` `dataDirOf`). Now reachable since books can be `readonly` (written by a newer app) under the v2 schema.
- [ ] **god-class extraction (LOW, refactor).** `index.ts` (~2,300 lines, ~60 service fields) and the heavy inline business logic in `projects.routes.ts` `/execute`+`/auto-execute` (continuation/quality/assembly/narrate/DOCX in the handler) are the hardest-to-test code; extract into `ProjectEngine`. Not surgical — schedule deliberately.
- [ ] **library overlay drift signal (from F4).** When a workspace overlay first shadows a builtin author/voice/genre entry, record the shadowed builtin's content hash/version and surface it so the UI can flag drift against later builtin improvements. Needs a stored hash + read/list shape change + UI — beyond a small metadata add.
- [ ] **whole-workspace restore atomicity (from F6).** Make `backup.restore()` whole-workspace path atomic (restore into a sibling temp dir, then swap) rather than per-top delete+copy. Non-trivial because the loop preserves `.vault`/`.audit`/non-snapshotted tops; pre-restore snapshot is the current mitigation.
- [ ] **audit-chain verify routine (from F10).** The hash chain now seeds from the last log line on init (continues across restarts). Add a verify routine that walks a day's log confirming each entry's `previousHash` matches the prior line's `hash`.
- [ ] **TLS-aware chat CSP (LOW, only when TLS lands).** `phase-12-chat-http.ts` `connect-src` hardcodes `http://`/`ws://`; derive the scheme from `req.protocol`/`X-Forwarded-Proto` when a TLS reverse proxy is introduced. No action for the current LAN-HTTP posture.
- [ ] **`?token=` query fallback in URLs (LOW, accepted LAN limitation).** Bearer token appears in native-GET URLs (EventSource, export links). Acceptable on the single-user LAN; if ever internet-exposed, scope the query token to GET download routes or issue short-lived one-time download tokens. (Cross-referenced under the security-review section above.)

## Owner roadmap — 2026-06-12 batch

Eight items the owner queued on 2026-06-12. **#1 (versioning format), #2 (README warning), and the breaking-change gate follow-on to #1 are done** — see COMPLETED.md. The remaining work (items #3–#8):

- [ ] **Import the owner's existing genre + pipeline documents.** Bring the owner's existing genre guides and pipeline definitions into the product as library entries (built-in or workspace overlay). Pairs with Phase 12 library element import (`/api/library/import`) and the `LibraryService` overlay write path; may need a small content-conversion step to the 7-file genre-guide schema (`docs/GENRE-GUIDE-TEMPLATE.md`) and the `pipeline.json` shape. Owner to provide the source documents.
- [ ] **File explorer for documents.** A studio UI to browse the uploaded manuscripts under `workspace/documents/` (and likely a book's `data/` outputs) — list/preview/download/delete. Backend largely exists (`/api/documents`, `/api/books/:slug/files`); this is mostly a frontend route + reuse of the sanitized markdown preview. Decide scope (documents only vs a unified per-book file tree) and whether delete needs the confirmation gate.

## Bug fixes queue (2026-06-19) — address one by one


- [ ] **Consistency audit didn't show its cost; OpenRouter cost slightly over-counted (found 2026-06-26).** Running the audit on Opus-via-OpenRouter spent real money but the consistency screen never displayed the run's cost (owner had to check the OpenRouter dashboard). Also BookClaw recorded ~$5.71 vs OpenRouter's actual $5.00 because the request didn't opt into OpenRouter usage accounting, so it summed a token-cost estimate (incl. calls OpenRouter cut off at the daily cap) instead of the provider-reported `usage.cost`. Fix: (a) `AuditReport.estimatedCost` accumulated in the audit wrapper, shown in the report + studio panel; (b) send `usage: { include: true }` on OpenRouter requests so the real per-model cost is used. (The 2 skipped chapters were the owner's OpenRouter $5 daily limit being hit — now surfaced via `failureSamples`, not a BookClaw bug.) _(Owner ask 2026-06-26.)_

- [ ] **Consistency auditor silently scanned only 4 of 24 chapters (found 2026-06-25, "Love Between Departures" on Neptune).** Root cause: the extractor routed to Ollama `gemma3:4b` (Neptune's only configured provider); `completeOllama` never sets `num_ctx`, so Ollama truncated every chapter >~4k tokens to its default context → non-JSON output → `parseExtractorResponse` threw → the audit's catch did `continue`, dropping the chapter without counting it. Only the 4 tiny winter-interludes fit, so the report showed "4 chapters scanned · No findings" — a false all-clear on an 83%-unscanned book. Fix (owner-approved): **(A)** consistency requires a large-context model — exclude Ollama from the consistency task, fail loudly (422) when no capable provider (Gemini/Claude/OpenAI/DeepSeek/OpenRouter) is configured; **(B)** surface per-chapter extraction failures in the report/UI (`chaptersTotal`/`chaptersFailed`) so a mostly-failed scan can never read as clean. _(Owner ask 2026-06-25.)_

- [ ] **(Low priority) Normalize pandoc-escaped manuscripts to clean markdown (found 2026-06-26).** Every stored manuscript is heavily backslash-escaped pandoc markdown (`\"`, `\'`, `\.`, `\*`, and in `soft-edge-of-wild` even the headings: `\## Chapter 1`) — thousands per book (`hedgefund-heart` ~10.5k). On render the escapes collapse to the literal char, so readers/EPUB are unaffected, but any consumer reading the raw text (the consistency chapter-splitter — now made escape-tolerant — and the LLM extractor input) sees noise. Optional library-wide normalization pass that unescapes manuscripts to clean markdown once (deliberate, **backed-up**, not an audit-time side effect; a perfect unescape is non-trivial — prefer a real markdown round-trip over a blanket backslash strip). Benefits extraction quality + plain-text rendering; does **not** affect chapter detection (that's handled in the parser). _(Owner ask 2026-06-26; deprioritized in favor of the parser fix.)_

- [ ] **Prompt Runner needs a "book selection" item.** The Prompt Runner (`/prompts` route + `POST /api/prompts/run`, shipped 2026-06-14) runs a prompt against a chosen file but lacks an explicit book selector — the file picker is implicitly scoped. Add a book-selection control so the user picks the book (then its `data/` files), wiring through the existing `bookSlug?` param on `POST /api/prompts/run`.
- [ ] **`/version` chat + Telegram command.** Add a `/version` command (dashboard/Chat-app chat + Telegram bridge) that prints the current `DISPLAY_VERSION` + breaking version + server boot time / uptime, so the owner can confirm a fresh deploy is live before testing.
- [ ] **Add Claude/OpenAI/Gemini as authorized applications (subscription OAuth), not just API keys.** Today each provider is configured only via an API key in the Vault. Add an option to authorize via the provider's subscription/OAuth application flow (Claude Pro/Max, ChatGPT Plus, Gemini Advanced) so users on subscription plans can connect without a pay-per-token API key. Scope: investigate each provider's supported OAuth/app-auth path and what's actually permitted by ToS for programmatic use, decide storage (Vault token + refresh) and the AI-router/provider wiring, and the studio settings UI. Likely staged per provider (Claude first).
- [ ] **`track-changes.ts` rejected/pending deletion silently drops restored text (found 2026-06-23 by the coverage Batch A).** In `applyDecisions`, rejecting or leaving-pending a deletion restores the deleted run as a bare `<w:r>text</w:r>` (no `<w:t>` wrapper), but `extractRunsText` only reads text inside `<w:t>…</w:t>` — so the restored text is dropped from the rebuilt paragraph (e.g. `"The quick lazy fox"` comes back as `"The quick fox"`). Fix: re-wrap restored del text as `<w:t>`. Two tests in `tests/unit/track-changes.test.ts` currently pin the buggy behavior with `// NOTE: possible bug` — update them to the corrected expectation when fixed.
- [ ] **`external-tools.ts` — no path containment on user-supplied `manuscriptPath` (found 2026-06-23, coverage Batch D; security-relevant).** `runFormatPro`/`runManuscriptAutopsy` pass `params.manuscriptPath` straight into the spawned Python args (`--input <path>`) with no traversal/sandbox check, so `../../../../etc/passwd` reaches the subprocess. The invoked *tool name* is hardcoded (not user-controlled), but the manuscript path is — and the `SandboxGuard` isn't applied at this layer. Add a workspace-containment check (reuse `safePath`/`SandboxGuard`) before spawning. `tests/unit/external-tools.test.ts` pins the current behavior with `// NOTE: possible bug`.
- [ ] **`bookbub-submitter.ts` — omitted deal price yields `undefined` instead of the 0.99 default (found 2026-06-23, coverage Batch D; low).** In `buildDraft`, `BOOKBUB_PRICE_POINTS.includes(input.suggestedPriceUSD ?? 0.99)` is true when the price is omitted (0.99 is on the grid), so the ternary returns `input.suggestedPriceUSD!` — literally `undefined` — and the intended 0.99 default is never applied. Produces a draft with a missing price (human reviews before submitting). `tests/unit/bookbub-submitter.test.ts` pins it.
- [ ] **`preferences.ts` — plural-anchored detection misses (found 2026-06-23, coverage Batch D; low).** `inferPreferenceKey`'s adverbs rule `/\b(adverb|ly words?)\b/` has a trailing `\b`, so plural "adverbs" doesn't match and falls through to the generic `preference.adverbs` bucket instead of `writing.adverbs`; likely affects other singular-anchored rules. `tests/unit/preferences.test.ts` pins it.

## Mercury test run issues (2026-07-01)

- [ ] **"My Second Test Medical Romance" run review — B1–B6 fixes (code done + unit-tested 2026-07-02; see docs/RUN-REVIEW-2026-07-01-my-second-test.md §6).** Six bugs from the deepseek-v4-pro run, fixed in the §5 order: **B1** headless server-side phase driver (opt-in `BOOKCLAW_HEADLESS_PIPELINE=1` — the CRITICAL fix so chained runs don't orphan at a boundary with no browser open; complements the browser-driven Fix #B); **B2** `canonical` column migration for `facts`+`knowledge` (re-enables the whole consistency audit on any pre-`canonical` DB incl. Mercury); **B3** entity-extraction attribute shape (unambiguous flat-map prompt + defensive `coerceAttributes`); **B4** compile meta-strip (drops leaked `**… Word Count …:**` lines); **B5** launch pipeline's cover step repointed from the missing `book-launch` skill to `cover-designer` (+ a guard test that every pipeline skill ref resolves). **B6** now also fixed: `isLargeContextProvider`/`extractionProviderError` (`model-selection.ts`) make the context-engine extraction refuse an Ollama fallback **when a large-context provider is configured** (mirrors the consistency guard; Ollama-only deployments unaffected), so the fallback is no longer silent. `docker/docker-compose.yml` passes `BOOKCLAW_HEADLESS_PIPELINE` through for B1. All `tsc` clean, full unit suite 1296/1296. **POV pass (§7, done 2026-07-02):** manuscript is consistently third person across all 33 chapters (1st/3rd ratio 0.024) — the my-fourth flip did NOT recur. **Pending:** headless re-run on Mercury to verify B1 end-to-end + B2's audit fires.

- [ ] **B7 — leaked AI process-narration + truncated scene inside Chapter 22 (found 2026-07-02 by the POV pass; see RUN-REVIEW-2026-07-01-my-second-test.md §7).** The chapter-22 production step emitted a truncated first draft ("… before. She", cut mid-word) + the model's own first-person "let me continue" reasoning wrapped in ``` fences ("I'll revise by…", "Let's produce the full revised chapter.```markdown") + a retry, and all of it saved into the chapter file → carried verbatim into the assembled novel. `stripMetaCommentary` misses it because it is mid-document and fenced (distinct from B4's word-count meta). **Proposed fix:** a chapter-level validate/repair (the per-chapter analogue of `validateAssembly`) that detects a fenced/inline AI-reasoning block or a mid-word truncation in a chapter step's output and re-runs/repairs it — rather than broadening the safe-first leading/trailing strip to eat mid-document content. **Difficulty: Medium.**

- [ ] **Novel pipeline stalls at every phase boundary — next phase orphaned in `active` (found 2026-07-01, Mercury "My Test Medical Romance" run).** The pipeline is 6 chained projects. The studio's `PipelineRail` fires `/auto-execute` **once**, on phase 1 (autostart, `PipelineRail.tsx:199`); `/auto-execute` runs that whole project server-side. On completion the server hook `advancePipeline` (`phase-06-content.ts:72`) MARKS the next phase `active` but runs no AI, and with autonomous mode OFF nothing drives it. The F1 "follow" effect (`PipelineRail.tsx:106–127`) switched the tracked project to the next phase and polled it (GET only) but **never re-fired `/auto-execute`** — so Planning completed, Bible sat `active` with no driver (idle CPU, no outbound call, no error), and the run dead-ended. **Fix (frontend, done):** the follow effect now fires `/auto-execute` on the phase it follows to (fire-and-forget; server in-flight guard makes a dup a harmless 409). Studio typechecks clean; no frontend component test harness exists, so verification is a Mercury rebuild + observing the pipeline roll Planning→Bible→Production→… hands-off. **Pending:** rebuild Mercury to verify end-to-end.

- [ ] **Runtime config changes don't persist across rebuilds — OpenRouter model reverts to gemma (found 2026-07-01, Mercury test run).** Root cause: `setAndPersist` wrote `config/user.json`, which is baked into the image (`COPY config ./config`) and is **not** on a persistent mount (only `/app/workspace`, `/app/backups`, `/app/config/.vault`, `/app/author-os` are). So the write survived a `docker restart` but every image rebuild / container recreate reset the file to its baked copy (`ai.openrouter.model: google/gemma-3-4b-it`), reverting the value. Affected **all** `setAndPersist` settings (per-provider default model, `costs.*`, `ai.preferredProvider`, telegram/backup config). **Fix (code done + unit-tested, `tests/unit/config-persist.test.ts`):** `ConfigService` takes an `overridesPath` that lives on the workspace bind-mount (`workspace/.config/config-overrides.json`); baked `config/user.json` is now a read-only seed layer, runtime writes go to the persistent overrides file. **Still pending:** deploy to Mercury + Neptune, and a one-time migration copying each host's live container `/app/config/user.json` → `workspace/.config/config-overrides.json` **before** the rebuild so current live settings (esp. Neptune production) aren't lost.



- [ ] **Settings: change the default (global preferred) provider.** The Settings "Providers" section (`Settings.tsx`) only *displays* active providers; add a control to set `ai.preferredProvider`. Backend already supports it (`GET /api/config` returns it; `POST /api/config/update` accepts `ai.preferredProvider` and syncs the router live) — frontend-only. List active providers + an "Auto (smart routing)" option that clears the override. _(Owner ask 2026-06-29.)_
- [ ] **Easy Start: pick the LLM, persisted as the book default.** Add a provider picker to the Easy Start wizard (step 3), defaulting to the global default provider, options = active providers. The choice persists as the book's default so later phases reuse it: add `preferredProvider?` to `BookManifest` (additive-optional, no schema bump — mirrors `consistency`), accept it on `POST /api/books`, store it on the manifest, and have `POST /api/projects/create` inherit it from the active book when no explicit override is given. _(Owner ask 2026-06-29.)_
- [ ] **Model selection for OpenRouter (Easy Start + Settings).** OpenRouter is a meta-provider, so picking the provider alone runs the configured default model (often a tiny one). Easy Start now shows a model datalist for catalog providers (OpenRouter/Claude/Gemini) — required for OpenRouter — persisted per-book as `BookManifest.preferredModel` → `project.preferredModel`, applied at the model resolver (`index.ts` `stepModel = stepOverride?.model || project.preferredModel || global default`). Settings makes the OpenRouter provider chip's model field editable (writes the global `ai.openrouter.model`), reusing `ProviderModelField` + `useModelCatalog`. _(Owner ask 2026-06-29.)_ **Bug found + fixed 2026-06-29:** the per-book `preferredModel` pin was honored only on the `index.ts` bridge/Telegram path — the REST **auto-execute/execute** path (`_shared.ts` `stepRouting()`) read only `step.modelOverride.model` and dropped `project.preferredModel`, so dashboard-run projects silently used the provider default (4B `google/gemma-3-4b-it`) despite a DeepSeek/Claude per-book pin. This was the root cause of the stalled Neptune "My Medical Romance" / "My Second Medical Romance" books (Planning completed on the wrong tiny model; the large Book Bible step then orphaned in `active`). Fixed `stepRouting` to fall back to `project.preferredModel` (mirrors the bridge path) + regression tests in `tests/unit/step-routing-temperature.test.ts`; the three stuck bible projects were reset to `pending`. **Still pending:** the Easy Start datalist + Settings editable-model UI, and a Neptune redeploy to ship the resolver fix.
- [ ] **Neptune: chat/SSE disconnects during the initial pipeline.** Running the planning pipeline on Neptune drops and reconnects the chat Socket.IO (and likely the activity SSE). The Write page only polls, so this is the live streams tripping Socket.IO's default 20s ping-timeout — most likely a synchronous, CPU-bound step blocking the Node event loop. Mitigate (raise `pingTimeout`/`pingInterval`) and instrument (event-loop-lag logging) to pinpoint the blocking step on the next run. _(Owner ask 2026-06-29.)_

## Easy-book run issues (Neptune "My New Romance", 2026-06-29)

Three issues from the Easy Start run on Neptune. **Dominant cause: the book ran entirely on a 4B model** — Neptune's only providers are Ollama `gemma3:4b` and OpenRouter `google/gemma-3-4b-it` (no Claude/Gemini/OpenAI/DeepSeek keys in the vault, and the preferred provider's model is a 4B). A 4B model won't honor chapter counts or hold names across steps. Items 2 & 3 are amplified by this; item 2 is *also* a real truncation bug worth fixing regardless of model.

- [ ] **Planning context truncation drops character names (item 2 — names changed between steps).** `ProjectEngine.buildProjectContext` default branch truncates each prior step's result with `result.slice(-2000)` — it keeps the **last** 2000 chars and drops the head, where character names/identities are stated. So the chapter-outline step (step 4) never saw the nurse's name from the character-profiles step (step 3, "Olivia Hayes") and the model invented a new one ("Ava Carter"). Fix: for planning-type projects keep the head (or head+tail, or raise/remove the cap — planning outputs are small ~6KB). `projects.ts:1261-1262`. **Difficulty: Low.**
- [ ] **Chapter-by-chapter outline ignores the chapter-count target (item 3).** The outline declared "I'll aim for around 25 chapters" but produced 12 (full Romancing-the-Beat arc to HEA — not truncation; the model self-compressed). It did follow the 3-act structure + alternating POV correctly. Fix: harden the outline prompt to require *exactly N* chapters distributed across the structure, and/or add a post-generation chapter-count validation (reuse `structure-check`/`check-outline-structure`) that flags or continues when short. Primarily a weak-model symptom, so a capable model is the bigger lever. **Difficulty: Low-Medium.**
- [ ] **Easy-book pipeline advanced past planning then halted at the bible, paused (item 1 — "restarted, stuck on phase 1").** Planning (project-1) ran once and completed cleanly (no actual re-run — file mtimes are sequential). The next phase (book-bible) was advanced and its first step started, then `pauseProject` was called (it resets the active step back to `pending`, so the observed state is paused + 0 steps run). The exact pause trigger isn't in the available logs. The "restarted/stuck" *appearance* most likely ties to the chat/SSE disconnect-reconnect (the Socket.IO mitigation + event-loop-lag logging shipped 2026-06-29 but is **not yet deployed to Neptune**). Next step: deploy the socket fix to Neptune, reproduce, and use the lag logs to pin the pause cause; consider hardening the autostart/follow guards against a reconnect re-fire. **Difficulty: Medium (needs reproduction).**

## Flagship pipeline engine — Sub-project 1 complete (2026-07-03)

The genre-flagship book-generation engine (Sub-project 1, Plans 1-8) is built,
reviewed, deployed to Mercury, and pushed. Spec: `docs/superpowers/specs/2026-07-02-flagship-pipeline-engine-design.md`; plans under `docs/superpowers/plans/2026-07-0[23]-flagship-*.md`. Shipped: casting layer, content axes + heat routing, consistency spine, enhancement wrappers (grounding/rolling-summary/analyze-apply), gate cadence, scheduler + cost control, genre bases + casting sheets, opt-in ideation ensemble.

- [ ] **Wire the remaining generation seams into the bridge/headless drive path.** `resolveGroundingBlock`, `resolveIntimacyRouting`, `resolveAnalyzeApplyBlock`, and `resolveEnsemblePremise` are wired only into the Studio routes (`projects.routes.ts` `/execute` + `/auto-execute`), NOT the Telegram/Discord/heartbeat/headless runner `startAndRunProject` (`index.ts`). So a book whose premise/chapters are driven purely via a bridge or the autonomous heartbeat skips grounding research, intimacy/uncensored routing, analyze-apply polish, and the ideation ensemble. Narrow blast radius today (opt-in knobs aren't bridge-settable; premise usually Studio-kicked), but a real gap for pure-headless runs. Cross-cutting; fold into the Sub-project 2 intake work or a dedicated bridge-parity pass. **Difficulty: Medium.** _(Update 2026-07-03, combined review H2: the Plan 3 **consistency spine** — the fifth such seam — was inert on this path too, and is now FIXED: `startAndRunProject` injects the pre-draft ledger and persists post-draft facts via the shared `gateway/src/services/consistency/spine-inject.ts` helpers. `projects.routes.ts` still has an equivalent inline block that should converge on those helpers as part of this pass.)_
- [ ] **Combined-review LOW findings (2026-07-03, not fixed).** From the whole-branch review of Plans 1-8: `heatLadder.fallbackOrder` is dead config (heat.ts reads only `uncensoredByLevel`); `resolveIntimacyRouting`'s `selectProvider('general')` is unguarded (throws only on a zero-provider deployment); `/consistency/import-audit` skips the up-front capability gate, has no test, and runs the full audit synchronously in-request; redraft-of-chapter-N can raise a spurious self-contradiction flag against its own superseded draft; the `skipPersist` decision has a narrow TOCTOU window vs a concurrent audit; casting-sheet model-slug punctuation is internally inconsistent (all pass `isValidModelId`); `bibleSeedChains` and persisted `review.pendingResult` are unbounded/un-truncated (mild state bloat). Individually low-impact; batch when next in these files.
- [ ] **Sub-project 2 — Smart Intake Wizard (separate spec to write).** Expand Easy Start into an interview (genre, structure, author identity/brand, prose model, spice/gore ceiling, gate cadence, ensemble toggle) that surfaces and sets the per-book knobs Plans 1-8 added (contentCeiling, uncensoredProvider, review.cadence, ensemble, costBudget, prose model). Until then those knobs are API/MCP/hand-edit only. _(Combined-review M1 fixed the `ensemble` knob so it is now settable via `POST /api/books` like its siblings; the 2026-07-03 MCP lockstep pass added all of them — contentCeiling/uncensoredProvider/reviewCadence/costBudget/ensemble/preferredProvider/Model — to the `create_book` MCP tool, so they are genuinely MCP-settable now, not just documented as such.)_

## Quick cleanups (minutes)

- [ ] **Model catalog: Google ListModels pagination.** `parseGoogleModels`/`fetchGoogleModels` request `pageSize=1000` and ignore `nextPageToken` — fine today (all Gemini models fit one page) but would silently truncate if Google ever paginates. Follow `nextPageToken` if it appears. (Deferred low finding from the 2026-06-27 model-picker review.)

- [ ] **DRY the model picker across screens** _(from the per-step model/temp code review, 2026-06-27)._ The new shared `frontend/studio/src/components/asset/ModelPicker.tsx` (provider select + OpenRouter datalist + temperature) duplicates the inline pickers still living in `PromptRunner.tsx` and `Consistency.tsx`. Switch those two routes to the shared component so provider-list/datalist/temperature changes happen in one place. Low risk but they differ slightly (labels, no-temperature in places) — verify each screen after.
- [ ] **Per-step provider: offer only configured providers** _(from the per-step model/temp code review, 2026-06-27; residual of finding #3)._ The `ModelPicker` lists all six `AI_PROVIDERS` regardless of which have API keys; pinning a skill phase (or pipeline step) to an unconfigured provider fails gracefully at run time with `[AI provider failure] Provider X not found` (typos are already rejected at save time by the parse-trust-boundary check). Consistent with the existing Consistency/Prompt Runner pickers, which also don't filter. Consider an app-wide "configured providers" source so all pickers only offer usable providers. Low priority — failure is graceful and self-explanatory.

- [ ] **Cost warning on the Consistency Auditor screen [owner ask 2026-06-26].** Running a full-book audit with a premium model is genuinely expensive (the "Love Between Departures" Opus run cost ~$5.71; Haiku ~$0.80). Add an up-front notice on the Consistency panel so cost-conscious authors aren't surprised — e.g. a short warning that a full-book scan makes one premium AI call per chapter and cost scales with book length + model, nudging toward a cheaper large-context model (Haiku/Gemini Flash). Bonus if cheap: a rough pre-run cost estimate from chapter count × the selected model's rate. The audit already surfaces actual spend post-run (`estimatedCost`); this is the *before* nudge.
- [ ] **Choosable download location for file/report downloads.** Today every "Download" (reports `.md`/`.json`, book files, exports) goes to the browser's default Downloads folder with no choice of location/filename. Investigate offering a real "Save As" picker — the browser File System Access API (`window.showSaveFilePicker`) lets the user pick a directory + name on supported browsers; fall back to the current anchor-download behavior where unsupported. Applies across the Reports page, Files explorer, and the export buttons. _(Owner ask 2026-06-26.)_
- [ ] **OpenRouter model picker (Prompt Runner + Consistency).** Under the "Exact model" field, when the provider is `openrouter`, offer the full OpenRouter catalog as a searchable `<datalist>` (type-to-filter, arbitrary slugs still allowed) instead of a bare free-text box. New gateway endpoint `GET /api/models/openrouter` proxies `https://openrouter.ai/api/v1/models` (frontend can't call it directly — CSP `connectSrc 'self'`) and caches the list in-memory for 24h, fail-soft to free-text on any fetch error. Shared `useOpenRouterModels` hook used by both the Prompt Runner and Consistency model pickers. _(Owner ask 2026-06-25.)_

## Investigations (under an hour each)

- [ ] **Rework the "Files" surface.** The studio Files explorer (`frontend/studio/src/routes/Files.tsx`, mounted at `/files` — the document + book-output explorer) needs reworking. Scope TBD — review it in use and capture specifics as they surface. _(Owner ask 2026-06-23; details to follow with more usage.)_

## Test coverage expansion (2026-06-12)

From the 2026-06-12 test review. **First batch DONE** (see COMPLETED.md 2026-06-12 — +78 unit tests covering confirmation-gate, vault, audit, sandbox, the wordcount util, asyncHandler, the research SSRF guard, and ProjectEngine orchestration; plus 4 free smoke checks for the chat token-bridge, version/breakingVersion, and the Socket.IO handshake gate). Remaining/deferred:

- [ ] **ProjectEngine fake-AI *generation-loop* coverage.** The orchestration the engine exposes (`createProject` variants, `completeStep` sequencing, `onProjectCompleted`) is now unit-tested with supplied step results. The *generation loop itself* — multi-pass continuation, the `[AI provider failure]` guard, fallback-cost recording, per-step model retry — lives in the `index.ts`/`projects.routes.ts` handlers (not the engine), so deterministic coverage needs an injected fake AI provider at that layer. Pairs with the god-class extraction (move the loop into `ProjectEngine`, then test it with `setAI` + a stub). Until then it's only covered by the real-AI feature-smoke.
- [ ] **Chat WebSocket round-trip smoke.** The served-HTML token-bridge integrity + bad-token-handshake checks were added to `feature-smoke.sh`; a full Socket.IO message round-trip (connect with bearer → send → receive `response`) still needs `socket.io-client` reachable from the smoke runner — add it (or a tiny node harness) so the chat path is exercised end-to-end, not just its HTML.
- [ ] **Website deploy/publish gate smoke.** The deploy/publish ConfirmationGate routing (202 → finalize-on-approval) has no smoke coverage; add a free check that POST deploy returns 202 with a `pendingConfirmation` and the finalize endpoint rejects until approved (don't actually deploy).
- [ ] **Playwright end-to-end suite** — see the dedicated item below (browser-level flows; complements the unit/integration layer).

## Larger items (worth scoping before starting)

- [ ] **Pipeline-quality follow-ups from the "My Third Medical Romance" run review [2026-06-30; see docs/RUN-REVIEW-2026-06-30-my-third.md].** The first full 6-phase run shipped (planning→launch) but had catastrophic consistency drift + structural bugs. Shipped this session: deterministic manuscript assembly + shrink validator, full-novel download (endpoint + Studio Publish button), story-canon injection into every generation step, auto consistency audit on production/revision completion, and a meta-commentary stripper. **Remaining:** (1) **Per-chapter revision** — change the deep-revision "apply macro/scene/line revisions" steps from a single whole-manuscript regeneration (which truncated to ~10–25% of the book on gemini-3.5-flash) to per-chapter patch/diff passes (like production's per-chapter write/polish), gated by the new `validateAssembly` shrink check so a truncating pass fails instead of silently destroying the novel. (2) **Real export in the format pipeline** — the `format-export` "compile export DOCX/EPUB" steps are AI text reports, not files; wire them to call `generateDocxBuffer`/`generateEpubBuffer` against the assembled manuscript, and persist a real `manuscript.md`/`.docx` at production end (the production "compile" step is still an LLM completion report). (3) **Lightweight model-free consistency gate** — a registry entity-diff per chapter vs the canon (names/town/hospital), as a cheaper complement to the LLM audit (the consistency agent showed items 1–4 are pure string/registry checks that would have flagged this run on chapter 1). (4) **World-building persistence** — the bible's "World-building document" step output never landed in `data/`; investigate the save path. (5) **Manifest title typo** — "My Third Medial Romance" → "Medical"; and consider a larger-context model for the bible + revision phases (gemini-3.5-flash can't hold canon across phases or emit a whole 80K manuscript). **Difficulty: Medium-High.**

- [ ] **Extract Worlds into a first-class subsystem (out of the Library type) — LOW PRIORITY [owner ask 2026-06-27].** Today **`world` is dual-natured**: it's a Library *kind* (manages `worlds/<name>/world.json` config — documentTypes/domains/classificationScheme — edited in the Asset Studio via `WorldEditor` with built-in→workspace overlay resolution) *and* it already has a dedicated subsystem (`WorldService` in `world.ts`/`world-parse`/`world-appendix`/`world-authoring`, the `/api/worlds/*` routes for documents + per-book `/api/books/:slug/world/*` binding, and MCP `tools/world.ts`). As a stopgap, Worlds was promoted to a top-level nav item under Series that **deep-links into the Library world kind** (`/library/world`); it still lives in the Library. The deeper move — removing `world` from the Library type entirely — is a real cross-cutting refactor and should be planned (spec → plan → TDD): (1) drop `world` from `LIBRARY_KINDS`/`FILE_KINDS`/shared `LibraryKind`/`KindRail`/`EntryList`/`AssetStudio`/`book-types` snapshot-kind union **in lockstep** (partial removal re-creates the `sequence`/`toolkit` drift-bug class); (2) re-home world.json **config CRUD** — `/api/worlds` only serves documents/binding today, with no create/edit-config endpoint, so add it and re-point `WorldEditor`, and re-implement the built-in→workspace **overlay** inside `WorldService`; (3) rework the **book-container snapshot + re-pull drift** logic that treats `world` as a template kind (`book.ts`, `library-removed` classification) — this touches **live Neptune data** (books bound to worlds with snapshotted world templates), so needs a migration/back-compat check; (4) update **MCP** (`tool-groups.ts` + `tools/world.ts`) in the same commit; (5) library-transfer already hard-excludes world ("not supported yet (Phase 1)") so that special-case simply drops. Upside: `world` is already the only Library kind that's a directory-with-config-plus-externally-owned-documents, so extracting it makes the library uniform and aligns with the ★ World Repository roadmap. Only worth doing when committing to a standalone World Repository screen. **Difficulty: High (refactor + live-data migration).**

- [ ] **In-depth beta/alpha read feature [owner ask 2026-06-26].** A deeper, full-manuscript developmental read than what exists today: the current **beta-reader** (`gateway/src/services/beta-reader.ts`) is a panel of ~4 reader *archetypes* giving reaction-style feedback, and **`editorial-alpha-read`** (`library/pipelines/`) is a per-chapter analyze→apply pass that needs supplied feedback. The ask is a richer, structured read that goes well beyond those — e.g. reader personas that hold **sustained memory across the whole book** (running reactions, predictions, where they'd put it down), and a structured report across craft dimensions (pacing/tension curve, character-arc satisfaction, emotional beats, stakes escalation, dialogue, plot holes vs. the plot-promises ledger, ending payoff, comp-title positioning) with chapter-anchored, actionable revision notes. Should compose with the existing engines (consistency ledger, plot-promises, structure/length) rather than duplicate them. Scope the report schema, the persona/memory model, per-chapter vs. whole-book passes, and cost (this is many AI calls over a full book — pair with the cost-warning item). **Difficulty: High.**

- [ ] **Consistency-ledger database maintenance** _(owner ask 2026-06-22; follow-on to the consistency auditor)._ Lifecycle housekeeping for the fact-ledger DB: prune facts for **deleted** books, re-seed **stale** per-world canon when a world/bible changes (content-hash/version check), `VACUUM`/compaction after large rebuilds, orphan detection (facts whose book/world no longer exists), and size/row-count monitoring surfaced somewhere visible. Keep it deterministic + fail-soft like the rest of the SQLite usage.

- [ ] **Strategy + product features — see the [Targeted feature roadmap](#targeted-feature-roadmap-product-consolidated-2026-06-21) at the top.** The full plan ([STRATEGY-LEADING-AI-WRITING-ASSISTANT.md](STRATEGY-LEADING-AI-WRITING-ASSISTANT.md) + its Gemini-merge addendum) and the per-tool review docs feed that ranked, deduplicated roadmap; the individual feature bullets that used to live here (Easy Button, Voice DNA, continuity engine, importance-tiering, NPE, publishing last mile, craft library, editorial council, inline-review UX, branching drafts, Drive Mode, semantic memory/graph, brainstorm primitives, monetization, the `~/data/Writing` import, and the WritingUtils format wiring) were consolidated up there so each appears once. Deep scoping stays in the linked docs.

- [ ] **Convert the n8n writing workflows into BookClaw pipelines (in progress 2026-06-20).** The owner runs n8n on **two** instances — **Neptune** (59 workflows) and **Mercury** (24) = 83 total; the goal is to port every *writing* workflow into a BookClaw asset (pipeline / sequence / editor / prompt). **Step 1 (DONE this pass): audit + classify** every workflow by server / type / book-pipeline? / importable — output in `docs/N8N-WORKFLOW-IMPORT-AUDIT.md` (64 importable, 2 already ported, 19 skip; collapses to ~10 canonical assets). Key finding: **Mercury holds the newer, cleaner architecture** — a modular **StoryHackerAI** suite (Orchestrator + Stage 1-5 via `executeWorkflow`, FS-based → a BookClaw **sequence**) and a phase-numbered **MSF (Mundane SF)** suite (Phase 0-6, multi-model parallel ideation); **Neptune holds the older R&D** (the already-ported romantasy pair, a Google-Docs `0-`…`9-` numbered suite, the editor-template family, scene-brief/humanizer/summarizer tools, plus media-ops + `My workflow N` scratch to skip). Several families are duplicated across both servers (port once). Common adaptation: swap Google Docs/Drive + local-FS (`msf-novels/<slug>/`, `Dropbox/Writing/`) I/O + splitInBatches/Merge fan-out + executeWorkflow orchestration for BookClaw per-book `data/` + genre/skill assets + `parallel` blocks + pipeline sequences. **Progress:** (1) ✅ **StoryHackerAI** suite ported — 5 pipelines (`nerdynovelistai-stage1-dossier`…`stage5-chapters`) + `nerdynovelistai` sequence (2026-06-20; the BookClaw suite is branded **NerdyNovelistAI**, renamed from `storyhacker` at owner request — n8n-source provenance still cites StoryHackerAI). (2) ✅ **MSF** suite ported — 6 pipelines (`msf-phase1-ideation`…`phase6-finalize`) + `msf` sequence (2026-06-20). **Progress:** (3) ✅ **Neptune editorial suite** ported (2026-06-20) — 6 revision pipelines (`editorial-developmental-edit`, `editorial-line-edit`, `editorial-copy-edit`, `editorial-proofread`, `editorial-alpha-read`, `editorial-outline-council`) + the **`editorial-review-and-edit`** sequence (chains dev→line→copy→proof; alpha-read + outline-council imported standalone, not auto-chained). Each manuscript editor is a per-chapter `expand:chapters` analyze→apply pair; Council is a multi-model `parallel` outline-improvement pass. **Progress:** (4) ✅ **standalone prompt assets** ported (2026-06-20) — 6 `library/prompts/` assets (`scene-brief`, `chapter-summary`, `character-arc-summary`, `public-domain-modernize`, `story-hack`, `short-story-hack`); plus the **scene-drafter** pipeline (`scene-drafter`, per-chapter brief→prose) and **both humanize pipelines** (`humanize-claude` 11-pass + `humanize-gemini` 10-pass, per-chapter `expand:chapters` de-AI chains built from the `github.com/pshort05/{ClaudeHumanizer,GeminiHumanizer}` repos). **Remaining:** (5) optionally fold the Neptune romantasy + numbered-suite + Braindump→Dossier/Dossier→Outline/Outline→Chapters variants into the existing ported pipelines (most are superseded duplicates — likely just mark them and move on rather than import). This effectively completes the n8n→BookClaw conversion of the importable writing workflows; what's left is dedup/cleanup, not new imports. **Caveat for ports 1+2:** the per-step `modelOverride` model IDs were carried over verbatim from the n8n nodes and are **provisional** — the owner should confirm them against the actual OpenRouter catalog (some n8n nodes referenced preview/experimental model strings).

- [ ] **True concurrency for parallel groups on the `/auto-execute` REST path (follow-up to the 2026-06-19 parallel-step-execution feature; see [COMPLETED.md](COMPLETED.md)).** Parallel step execution shipped with correct fan-out/barrier bookkeeping everywhere and **true wall-clock `Promise.all` concurrency in the autonomous driver** (`runProjectAutonomously` in `index.ts`), but the `/auto-execute` REST route (`projects.routes.ts` `/api/projects/:id/auto-execute`) — the **studio's primary runner** — still executes a parallel group's members **one per tick (sequentially)**. The barrier is correct (the join waits for all members), but members don't run concurrently on that path, so a studio-driven romantasy-planning run won't get the wall-clock speedup. The route's step-execution logic is ~200 lines of inline code (continuation loops, quality judge, context engine, file IO); making it concurrent means extracting that into a reusable per-step executor the route can drive with `Promise.all` over `engine.activeFrontier(projectId)` (the helper already exists). This is the natural place to also land the deferred **cost-spike / partial-failure handling** for concurrent groups. Pairs with the **god-class `ProjectEngine` extraction** below — do it there rather than refactoring the 200-line inline block in isolation.
- [ ] **Phase 8 multi-book concurrency — code-review follow-ups (deferred 2026-06-10, non-blocking).** Phase 8 itself is DONE ([COMPLETED.md](COMPLETED.md) 2026-06-10); these three altitude items from its `/code-review` were deferred to keep that change surgical — do them when Phase 9/10 next touches this code. **(a)** Consolidate the `dataDirOf(project.bookSlug) ?? activeDataDir() ?? <legacy>` ladder (repeated at ~12 call sites across `index.ts`, `projects.routes.ts`, `documents.routes.ts`) into one `BookService.projectDataDir(bookSlug)` helper — Phase 10 will add a per-channel rung, and a single helper makes that a one-line change instead of a 12-site grep. **(b)** Centralize the `bookSlug` binding: `createNovelPipeline`/`createBookProduction` take no `context`, so the active-book is stamped post-hoc at ~4 call sites — a future new creation path can silently forget to stamp and bind to nothing. Give those factories an explicit `bookSlug?` param (as `createPipeline` already has) and drop the scattered stamps. **(c)** Tighten the feature-smoke Tier-D check to positively assert the *new* output file exists in Book A's `data/` (currently relies on `hasOutput`, which would also be true from pre-existing files).
- [ ] **Extend test coverage to `ProjectEngine` step orchestration + completion hooks.** The test suite now exists — `node --test` (via `tsx`) unit tests + curl API tests, see [COMPLETED.md](COMPLETED.md) 2026-06-01 — with `AIRouter` fully covered and `ProjectEngine`'s pure surface (`getTemplates`, `inferProjectType`) covered. The remaining gap is the *orchestration* path: step sequencing, multi-pass continuation, and the `onProjectCompleted` hooks. Those chain real AI calls through `aiRouter.complete`, so testing them cleanly needs an injection seam (a fake AI provider) — which is intertwined with the **god-class Level 2–3** decomposition below. Do this alongside that refactor rather than bolting test-only hooks onto the current monolith.
- [ ] **God-class refactor — Levels 2–3 (deferred).** **Level 1 is DONE** — see [COMPLETED.md](COMPLETED.md) 2026-06-01: `index.ts` 2,649 → 2,103 (init phases extracted into `gateway/src/init/` modules; `initialize()` is a thin composition root) and `routes.ts` 5,518 → 59 (234 endpoints split into 12 per-feature mounters under `gateway/src/api/routes/` + `_shared.ts`). Remaining per [GOD-CLASS-REFACTOR.md](GOD-CLASS-REFACTOR.md): **Level 2** (service registry) and **Level 3** (plugin contracts — pipelines/providers/channels as data-driven plugins, the North-Star seam). **Defer until** a feature actually needs them *and* `AIRouter`/`ProjectEngine` have unit-test coverage (the doc's stated prerequisite — see the "No test suite exists" item). Don't refactor in a way that hardens the single-author / hardcoded-pipeline assumptions.
- [ ] **Playwright end-to-end test suite.** Once the Mercury deploy automation lands, build a Playwright suite that drives the dashboard against the live container on Mercury: dashboard loads, chat flows, project creation through one production phase, file upload/export, persona CRUD, confirmation-gate approval flow. Decide test runner home (`tests/e2e/` in this repo), how Playwright authenticates to the Mercury host (URL via env var), and what counts as a passing baseline before adding more flows. Complements — does not replace — the existing "No test suite exists" item, which targets unit-level coverage of `AIRouter` and `ProjectEngine` rather than browser-level e2e.
- [ ] **Comprehensive real-call feature smoke (`tests/feature-smoke.sh`).** _(Built + validated 2026-06-06 — 24/24 passed against Mercury, ~$0 on gemma; → COMPLETED.md after the next push.)_ Expands beyond `openrouter-pipeline.sh` (which only covers pipeline task-type routing) into a broad integration smoke that exercises as many features as possible against a running gateway using **real OpenRouter calls on the cheap `gemma-3-4b-it` model** (Ollama disabled → OpenRouter-only, restored on exit). Tiers: **A** new features (library read API; books create-from-library + list + gate) — free; **B** core AI paths (chat, persona-generate, per-step model override, research); **C** a tiny 1-chapter pipeline → the craft/analysis suite on it (continuity / craft-critique / dialogue-audit / pacing / structure / beta-reader / plot-promises / style-clone) → compile/export; **D** teardown (DELETE created projects + personas + the created book via `DELETE /api/books/:slug`). Per-feature PASS/FAIL/SKIP + cost delta; **graceful feature-detection** (skips endpoints absent on the target build, so it runs against both the Phase-1 and Phase-2 deployments). Excludes TTS/image (different providers, can't use gemma). Book + per-step-model assertions only pass once Phase 2 is deployed to the target.
- [ ] **Report real AI costs — follow-ons (account total + pre-call estimates).** DONE: the per-call fix (`completeOpenAICompatible` uses OpenRouter's `usage.cost`) and a dashboard **AI-spend widget** in the Manuscript Hub on Home (daily/monthly spend vs. caps + a budget bar, from `CostTracker` via `/api/costs`) — see [COMPLETED.md](COMPLETED.md) 2026-06-01. Remaining, optional: `GET /api/v1/credits` (OpenRouter *account*-level total/remaining credits — a truer "spent" figure across all clients) to augment the local `CostTracker` number; `GET /api/v1/models` real per-token `pricing.prompt`/`pricing.completion` to replace the placeholder *pre-call* estimates and to cost providers that don't return per-call cost (DeepSeek/Gemini/Claude/OpenAI still use the Sonnet-priced placeholder). Docs: [Usage Accounting](https://openrouter.ai/docs/cookbook/administration/usage-accounting).
  - **Cost-recording coverage gap (surfaced 2026-06-14 by the per-book spend review).** `CostTracker.record` is only called at the two `index.ts` `handleMessage` sites and the executable-skill runner. Auxiliary AI calls that go **directly** through `services.aiRouter.complete` — the quality-loop writing judge + dual-judge (`projects.routes.ts:688`), direct-routed continuation passes (`:787`), and the research / image-gen / video-research / craft-critic paths (each computes an `estimatedCost` but never records it) — are **not** counted in daily/monthly/lifetime/per-book spend. Pre-existing (predates the lifetime/per-book odometers), so the displayed totals read lower than real OpenRouter/provider billing. Fix is broad (route every paid `aiRouter.complete` through a single recording wrapper, ideally as part of the god-class `ProjectEngine` extraction) — do it there rather than bolting `record()` onto each call site.

- [ ] **Multi-author, multi-book platform with customizable per-author/per-genre pipelines. _(Ultimate goal — umbrella item; framing lives in [North Star](#north-star--the-ultimate-goal-use-this-to-weigh-every-other-decision).)_** **Concrete data-model design approved 2026-06-04 — see [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md):** book = a self-contained, portable container with a `templates/` snapshot + `data/` outputs; a **library** you pull from (built-in read-only + user-editable overlay, mirroring the skills overlay); **copy-on-create with opt-in re-pull** (no auto-propagation, so a library edit never corrupts a book mid-production); whole-`workspace/` host bind-mount for backup/sharing; **`schemaVersion` on each versioned artifact with a fail-closed compatibility gate** (refuse/quarantine read-only rather than silently coerce, so version mismatches can't corrupt old books even if upgrade scripts don't run); **backup & recovery** (default-ON local mirror snapshots, keep-last-N=10 with auto-prune, per-book/whole-workspace restore; opt-in cloud zips via directory-drop → optional rclone → optional hook, confirmation-gated at setup, never auto-pruned; default content books+library+config, vault excluded). That doc carries the phased implementation plan (Phase 0 storage → 1 library → 2 book entity+snapshot → 3 per-book soul/pipeline wiring → 4 re-point the editor + re-pull → 5 share/import security → 6 front-end/UI rewrite → 7 genre wiring → 8 multi-book concurrency → 9 book-board UI → 10 per-channel active book → 11 backup & recovery → 12 library element share/import [post-release]) and absorbs the in-dashboard editor (#1 below) as the per-book editing surface. **(Re-ordered 2026-06-07: front-end rewrite inserted as Phase 6 — the current single vanilla-JS dashboard bundle is the binding constraint on the multi-book studio UX; the four North-Star gaps — genre wiring, multi-book concurrency, book-board UI, per-channel active book — are phases 7–10; backup & recovery moved to the very END as Phase 11, the release gate, since a temporary host-level workaround covers backups until then.)** The end state: BookClaw runs as a multi-author studio where several books are in flight at once, each owned by a distinct author identity writing in its own genre with its own production pipeline — e.g. a Romantasy from author A, a contemporary romance from author B, and a mundane sci-fi from author C, all simultaneously, with room to add more authors/genres/pipelines without code changes. Today the gateway assumes a single author identity (`workspace/soul/{SOUL,STYLE-GUIDE,VOICE-PROFILE,PERSONALITY}.md`), treats `genre` as a free-text field on each project, and hardcodes the production pipeline (6 templates + `novel-pipeline` in `services/projects.ts` — steps, prompts, taskTypes, and word-count targets are all in code). Build out:
  - A **book** entity (id, title, current phase, author-style ref, genre-style ref, pipeline ref, project history) so multiple books can be in flight simultaneously at different production phases, with a dashboard view that shows each book's current phase and next action.
  - **Author style profiles** — multiple named identity bundles (each its own SOUL/STYLE-GUIDE/VOICE-PROFILE set), selectable per book. Reuse `services/personas.ts` and `services/style-clone.ts` rather than inventing a parallel system; the storage shape should let an author profile be cloned, edited, and assigned without touching the global `workspace/soul/`.
  - **Genre style profiles** — named genre packs (tropes, beats, reader-expectation notes, comp-title pointers) selectable per book, replacing the current free-text `genre` field while keeping backward compatibility with existing projects.
  - **Customizable pipelines (the flexibility ask).** Lift the hardcoded `ProjectEngine` templates into editable, data-driven pipeline definitions so a user can add/remove/reorder steps, edit each step's prompt, change its `taskType`/tier and `wordCountTarget`, and attach skills — all without touching code. A pipeline becomes a named, versioned artifact selectable per book (and per author/genre — a Romantasy author's pipeline can differ from a sci-fi author's). Keep the existing 6 templates + `novel-pipeline` as built-in defaults/seeds; allow cloning one as a starting point. Decide storage shape (JSON under `workspace/.config/` vs. per-author files), how step prompts interpolate author/genre/book context, and how in-flight books pin a pipeline version so editing a pipeline doesn't corrupt a book already mid-production.
  - **New Book page (component selection).** A book-creation UI listing library components per kind (author / genre / pipeline / sections / skills) with per-component include toggles; default pulls all. Lands with the Phase 2 book entity + snapshot-on-create, and consumes the Phase 1 library read API. (Owner ask 2026-06-06.) Concept: `dashboard/concept/new-book.html` (single-choice Author/Voice/Genre/Pipeline + multi-toggle Sections/Skills, each option showing source + description + canonical kind definition; live snapshot summary with copy-on-create framing).
  - Migration path for the existing single-book / single-soul setup: treat the current `workspace/soul/` as a default author profile, existing projects as a default book, and the current hardcoded templates as the default built-in pipelines, so nothing breaks on upgrade.
  - **Phase 1 (Library read side) is DONE** (2026-06-06, on `main` / pushed). `LibraryService` + built-in `library/` + `workspace/library/` overlay + read API + skills-overlay fold + pipelines-as-data exporter. See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 1.
  - **Phase 2 (Book entity) is DONE — lean** (2026-06-06; pending Mercury deploy + acceptance, then → [COMPLETED.md](COMPLETED.md)). `BookService` (snapshot author/genre/pipeline/sections → `workspace/books/<slug>/` + `book.json`) + version gate + `GET`/`POST /api/books` + New Book dashboard panel. **Deferred:** migration runners + existing-data migration (no v2 schema / fresh workspace); books don't drive generation until Phase 3. See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 2. **Phase 3 loose ends DONE 2026-06-07** (Author/Voice split, per-book skills snapshot, book DELETE — see [COMPLETED.md](COMPLETED.md)).
  - **Phase 4 (editor re-point + re-pull) is DONE** (2026-06-07; pending Mercury deploy + acceptance, then → [COMPLETED.md](COMPLETED.md)). Two-scope editor (library overlay CRUD + active-book snapshot) + per-book 3-way-merge re-pull against a pristine `.baseline/`. See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 4.
  - **Phase 5 (share/import security) is DONE** (2026-06-07; pending Mercury deploy + safety-net run, then → [COMPLETED.md](COMPLETED.md)). `BookTransferService`: export a book to a whitelisted `.zip`; import with zip-slip/symlink guards + injection scan + version classify → clean lands, flagged routes through the ConfirmationGate. See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 5.
  - **Phase 6 (front-end / UI rewrite) is DONE** (2026-06-08 → [COMPLETED.md](COMPLETED.md)). v6 React + Vite studio replaced the vanilla-JS dashboard; standalone Chat app on a 2nd port; legacy `dashboard/` retired. See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 6.
  - **Phase 7 (genre wiring) is DONE** (2026-06-09 → [COMPLETED.md](COMPLETED.md)). `BookService.getActiveGenreGuide()` composes the active book's `templates/genre/*.md` and `buildSystemPrompt` injects it alongside Author + Voice (reaches chat + every pipeline step); 7-file genre-guide schema with `romantasy` as the worked example. See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 7.
  - **Phases 8–10 are DONE** (2026-06-10/11 → [COMPLETED.md](COMPLETED.md)): multi-book concurrency, book-board UI, per-channel active book.
  - **Phase 11 (backup & recovery — THE RELEASE GATE) is DONE** (2026-06-12 → [COMPLETED.md](COMPLETED.md)). `BackupService`: default-ON local mirror snapshots (keep-10, outside-workspace root, Docker bind-mount), per-book/whole-workspace restore with automatic pre-restore snapshot, confirmation-gated opt-in cloud zip push (directory-drop/rclone/hook), scheduled + on-completion + manual triggers, studio Settings Backups card. Live-verified on Mercury (feature-smoke 77/0/0). See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 11.
  - **Phase 12 (library element share/import — the FINAL planned phase) is DONE** (2026-06-12 → [COMPLETED.md](COMPLETED.md)). Export/import a single library entry as a `.zip` through the Phase 5 security pipeline (shared `transfer-security.ts` guards + `LibraryTransferService` + Asset Studio export/import UI); skills land via the SkillLoader overlay path. Live-verified on Mercury (feature-smoke 84/0/0). **The book-container roadmap (Phases 0–12) is now COMPLETE.** See [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) Phase 12.

- [ ] **"Canon-driven" novel pipeline set (from an r/WritingWithAI workflow). _(Owner ask 2026-06-15 — full process write-up in [CANON-DRIVEN-PIPELINE.md](CANON-DRIVEN-PIPELINE.md); source: reddit.com/r/WritingWithAI/comments/1u6g6e4.)_** A published novelist's Claude-Code workflow that drafts a full ~20-chapter / ~138K-char novel "in compliance with a Canon you build" rather than free-writing (their stats: 20 chapters, 137,806 chars, ~96 min, ~89 parallel sub-agents, 87.5→89.0 quality). Build out a new set of BookClaw pipelines implementing its three layers:
  - **Layer 1 — Foundation / Canon** (one-time per series): 7 canon files in 3 groups — **core** (`canon.md` = physical laws / core engine, `character_bible.md` = voice + ruling patterns), **reference** (`glossary.md` = locked proper nouns, `timeline.md` = relative dates, `world_bible.md` = world rules), **style engine** (`style_guide.md` = rhythm/density/format, `forbidden_patterns.md` = never-do / never-say). Maps onto the existing per-book `templates/` snapshot (author/genre/world/sections); decide whether "canon" is a new sections set, a genre-guide variant, or a new library kind.
  - **Layer 2 — Chapter pipeline** (per chapter): a 4-stage hand-off **Plot agent → Scene-card agent → Writing-brief agent → Prose agent**, each consuming structured JSON from the prior stage + all canon files (~5–8K chars/chapter). Extends the existing `expand:chapters` construct into a multi-stage sub-pipeline per chapter.
  - **Layer 3 — Parallel review**: a 4-dimension consistency check after all chapters are generated (mirrors the existing continuity / craft-critique passes).
  - **Scoping gap to resolve first:** the source runs all 20 chapters' agents *in parallel* (~80–89 sub-agents at once); BookClaw's generation runs steps **sequentially** today, so decide whether to add parallel/fan-out chapter execution or run this canon→4-stage→review flow sequentially as a new pipeline sequence. Preserve the key insight: "AI doesn't write a novel — it drafts massively in compliance with the Canon you build." Relates to the Nerdy-Novelist mode (canonical artifacts) and builds on the config-not-code pipeline/sequence engine.
- [ ] **The Nerdy Novelist workflow — human-in-the-loop novel mode. _(Design approved 2026-06-02; see [NERDY-NOVELIST-WORKFLOW.md](NERDY-NOVELIST-WORKFLOW.md).)_** A net-new mode layered on the existing system that mirrors The Nerdy Novelist's four-step process (gather ideas → bible+outline → prose → validate/edit), with the author moving one book between interactive and automated production **per phase**. **Decisions captured in the design doc:** separate command per phase (`/brainstorm`, `/characters`, `/world`, `/outline`, `/write` + `/book new|use|list`); **book = the existing project** (`workspace/projects/<slug>/`) as the shared container both modes read/write; **canonical artifacts** (`dossier.md`/`characters.md`/`world.md`/`outline.md`/`chapters/chapter-NN.md`/`previous-chapter.md`) are the interop contract — the one small touch to the existing pipeline is a `canonicalOutput` field so automated phases land the same files; **templates as skills** in a new `novel` category (editable via the Authoring panel, workspace overlay), shipping the six phase skills + one worked genre plot template (Romance, framework + example); **execution reuses** the existing single-step run + pause/resume — `/write` writes exactly one chapter (anti-bulk) and `/write next` copies the latest human-edited chapter to `previous-chapter.md` and injects it. Surfaces: dashboard chat + Telegram bridge (Discord stub skipped). **Out of scope (documented):** broad genre library, North Star multi-author/genre-profile entities, rich dashboard UI, per-channel active book. Seeds the North Star book-as-container + editable-template patterns. Next step: writing-plans → implementation.
- [ ] **MCP server follow-ups (post-vendor).** The MCP server is **built and vendored into `mcp/`** and updated for the latest gateway features (see [COMPLETED.md](COMPLETED.md) 2026-06-22). Remaining: **archive/redirect the standalone `github.com/pshort05/bookclaw-mcp` repo** — the canonical copy now lives in `mcp/`, and the old local checkout at `~/data/dev/bookclaw-mcp` is superseded (kept on disk until the GitHub repo is archived). Optional: **publish `bookclaw-mcp` to npm** from the subdirectory (the plugin manifest already points at `mcp/plugin/`; consumers install the package, never clone the monorepo). Standing rule: keep MCP tools in lockstep with `gateway/src/api/routes/*.routes.ts` (update both in the same commit).

- [ ] **API-first frontend — every UI action goes through a system API.** Establish and enforce going forward that no dashboard/front-end feature implements behavior client-side only: every action the UI takes must call a REST endpoint, so the dashboard, the Telegram/Discord bridges, and the MCP server (`mcp/`) all drive the same handlers. **Payoff:** full MCP/API parity — anything a user can do in the UI is scriptable/automatable. Work: audit the current dashboard for any client-only logic that bypasses `/api/*`, add endpoints where missing, and treat this as a standing review rule for new UI. Directly enables the MCP server item.

## User experience & product features

- [ ] **Make the studio responsive / mobile-friendly [owner ask 2026-06-28].** The v6 React studio is desktop-first and works poorly on phones. Quick audit of `frontend/studio/`: the viewport meta is present, but the shell is a fixed two-column grid `.app { grid-template-columns: var(--rail) 1fr; height: 100vh }` with a **232px fixed nav rail** (`--rail`, `frontend/shared/src/tokens.css:30`) that **never collapses** — there's no hamburger/drawer/bottom-nav, so on a narrow screen the rail permanently eats horizontal space and content is cramped. Only **2 of 20 CSS modules** (`NewBook`, `Write`) have any `@media` rules, and those break at 980px/1100px (tablet), not phone widths; the other ~13 route panels (Settings, Consistency, Reports, PromptRunner, TryFail, Board, Series, Activity, Files, AssetStudio, Publish, Confirmations, EasyStart) have **no mobile layout** at all. Concrete fixes to scope: (1) collapse the rail to an off-canvas drawer or bottom tab bar below a phone breakpoint (~≤640px) with a hamburger toggle and ≥44px tap targets (rail icons are 13–19px today); (2) add a single shared phone breakpoint and stack the multi-column content grids that use fixed side columns (`280px 1fr`, `248px 1fr 296px`, `360px 1fr`, `1fr 332px`, `repeat(6,1fr)`, etc.) into one column; (3) swap `height: 100vh` (4 files, no `dvh`) for `100dvh` so mobile browser chrome doesn't clip the shell; (4) make the wide tables (TryFail ladders, model/reports lists — 3 components) horizontally scrollable or card-stacked; (5) audit `min-width: 200–220px` content blocks for horizontal overflow. Decide a target (phone-usable vs. fully fluid), a breakpoint token, and whether the standalone Chat app (separate port) needs the same pass. The `dashboard/concept/` mockups are the design source-of-truth to update alongside.
- [ ] **More detailed logs + an in-app log viewer [owner ask 2026-06-26].** Two parts: (a) richer/more granular server logging (e.g. a debug level, per-request/per-AI-call detail) so a failing run can be diagnosed without re-instrumenting code (mirrors the `tests/*.sh -v` debug-log convention); and (b) a way to *view* logs from the UI — e.g. a "View logs" button in Settings that surfaces recent server + activity/audit log output (the activity feed + `workspace/.audit/` JSONL already exist; the gap is a readable, filterable viewer). Decide scope: tail the existing JSONL logs vs. a dedicated structured log store; respect that logs may contain sensitive content (never expose secrets/tokens).
- [ ] **Editors on the Telegram bridge (follow-up).** The `/editors` + `/editor:<name>` commands are dispatched only from the dashboard/API chat (`handleDashboardCommand`); the handlers exist in `buildTelegramCommandHandlers` but the Telegram bridge's own command router doesn't dispatch them yet. Wire `/editor`/`/editors` into the bridge so Telegram users can enter editor mode (the per-channel session machinery already keys off `telegram:<chatId>`). Deferred from the Editors feature (2026-06-14 → COMPLETED.md).

- [ ] **Prompt Runner — multi-prompt chains (follow-up).** The single-prompt-at-a-time Prompt Runner shipped (2026-06-14 → COMPLETED.md). The prototype's **multi-prompt chaining** (run a sequence of prompts over a file with per-step model overrides + restart/recovery, like the prototype's YAML chains) was deferred — add it on top of the `prompt` kind + `POST /api/prompts/run`. Also deferred: an optional "apply book voice" toggle on a run (inject the book's author/genre for rewrite prompts).
- [ ] **Built-in "start a new project" tutorial.** A guided, in-app walkthrough for creating a first project (idea → persona → pipeline → first output) so new users aren't facing a blank dashboard. Decide the form: interactive coach-marks/overlay vs. a step-by-step wizard vs. a self-seeding demo project. Pairs with the help system below.
- [ ] **Extensive in-app help system (for non-technical users).** Assume some users aren't technical: contextual help, plain-language explanations of the core concepts (personas, pipelines, providers, skills, the confirmation gate), and answers to common "how do I…" questions — surfaced inside the dashboard, not only in `docs/`. Decide scope (tooltips + a searchable help panel vs. an in-app docs viewer) and how it stays in sync with `docs/`.
- [ ] **Change the AI model from within Chat.** Let the user switch the model for a chat conversation directly in the studio Chat UI (a model picker), without editing provider/config settings. Decide scope (per-message vs. per-session override) and how it interacts with the AI router's task-tier routing and OpenRouter model pinning.
- [ ] **Help item: how to enable the editors.** Add an in-app help entry explaining how to turn on editor mode — the `/editors` / `/editor:<name>` (or `/editor <name>`) chat commands, the opt-in `/editor <name> book` context, and `/editor off` to exit. Pairs with the in-app help system item above.

- [ ] **Genre duplicate-dir cleanup.** Fold `yakuza-triad`/`yakuzatriad` (same genre, two dirs with near-identical content) into one; audit for other near-duplicate genre dirs (e.g. `satire`/`satire-fiction`, `uchronia`/`uchronia-speculative-fiction`, `spy-thriller`/`spy-thriller-fiction`, `portal-sf`/`portal-science-fiction`). Surfaced during the genre-grouping work (2026-06-20).

## Standing constraints (do not "fix" these)

- Server bind defaults to `0.0.0.0` and is overridable via `BOOKCLAW_BIND`. This is a deliberate departure from the upstream localhost-only contract so the Docker image is usable on a LAN. **Do not revert to `127.0.0.1`-only.** (CORS, Socket.IO, and Helmet `connectSrc` were since tightened from permissive `*` to deny-by-default / `'self'` — see COMPLETED.md; the bind itself stays `0.0.0.0`.)
- Fail-soft init (services log `⚠`, gateway continues) is intentional — don't make startup require optional deps (better-sqlite3, yt-dlp, Ollama).
- `.js` import extensions on `.ts` source files are required by `NodeNext` module resolution — don't strip them.
- `skills/premium/*/` is intentionally gitignored — never commit premium skill content.
- `ConfirmationGateService` must gate every new feature with external side effects (publish, send, submit, upload, bid, purchase).
