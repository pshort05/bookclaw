# TODO

Tracking list of items surfaced while writing CLAUDE.md. Grouped by effort.

Anything currently being worked on must appear in this list. When an item is finished, move it to [COMPLETED.md](COMPLETED.md) with a `YYYY-MM-DD` completion date — don't just check the box and leave it here.

## North Star — the ultimate goal (use this to weigh every other decision)

**This is the destination the project is steering toward. It is not a near-term task — it is the lens for evaluating other work.** When scoping any non-trivial change (refactors, the rename, new features, storage shapes, API design), prefer the option that moves toward — or at least does not move away from — this end state. Where a decision is a toss-up on its own merits, the tie-breaker is "which choice makes the North Star cheaper to reach later."

**The end state:** BookClaw runs as a **multi-author, multi-book studio**. Several books are in flight at once, each owned by a distinct **author identity**, written in its own **genre**, produced by its own **customizable pipeline** — e.g. a Romantasy from author A, a contemporary romance from author B, and a mundane sci-fi from author C, all simultaneously. Adding another author, genre, or pipeline variant should be **configuration, not code changes**.

**The four entities this implies (none exist as first-class concepts today):**

- **Book** — owns state across `planning → bible → production → revision → format → launch`, references an author profile + genre profile + pipeline, and carries its own history. Today `workspace/projects/<id>/` is a flat list with no "book" that spans phases.
- **Author profile** — a named identity bundle (its own SOUL/STYLE-GUIDE/VOICE-PROFILE/PERSONALITY), selectable per book, cloneable/editable without touching the global `workspace/soul/`. Today there is exactly one author identity, hardcoded to `workspace/soul/`.
- **Genre profile** — a named pack (tropes, beats, reader-expectation notes, comp-title pointers), selectable per book, replacing the free-text `genre` field. Today `genre` is free text on each project.
- **Customizable pipeline** — editable, data-driven, versioned step definitions: add/remove/reorder steps, edit each step's prompt, change its `taskType`/tier and `wordCountTarget`, attach skills — **all without editing code**. Selectable per book (and differing per author/genre). Today the 6 templates + `novel-pipeline` are hardcoded in `services/projects.ts`.

**How this guides decisions already on the books:**

- **God-class refactor** ([GOD-CLASS-REFACTOR.md](GOD-CLASS-REFACTOR.md)) — **Level 1 complete (2026-06-01, see [COMPLETED.md](COMPLETED.md)); this guidance now applies to Levels 2–3.** The North Star is *the* "explicit goal larger than tidiness" that the doc says Level 1 needs before starting. The `init/` split should leave a clean seam where book/author/genre/pipeline services slot in (`06e-project-engine.ts` is the one most affected); the routes split should land `pipeline.routes.ts` (already listed) as a real boundary; and the Level 3 plugin contract should treat **pipelines as plugin-shaped data**, alongside the AI-provider/channel/TTS plugin work. Don't refactor in a way that hardens the single-author / hardcoded-pipeline assumptions.
- **Rename to BookClaw** ([RENAME-PLAN.md](RENAME-PLAN.md)) — the chosen name *is* this vision (a studio of **books**, not one author's voice). The rename is a natural moment to stop hardcoding "the author" in banners/config/copy and to phrase operator-facing strings in book/author-profile terms, so the brand and the data model agree.

The full implementation breakdown lives below under **[Larger items → Multi-author, multi-book platform](#larger-items-worth-scoping-before-starting)**; this section is the framing, that item is the work. The product/market framing — who this is for, how it differs from AuthorClaw, and the five anchoring use cases — lives in **[PRODUCT-DIRECTION.md](PRODUCT-DIRECTION.md)**.

## Full security review (post-LAN-exposure)

Opening the bind from loopback to LAN turned several previously-defense-in-depth
assumptions into single points of failure. A proper security review should
cover at minimum:

- [ ] **Bearer token is readable from the public dashboard HTML.** The auth gate covers only `/api/*`; the dashboard at `/` and its static assets are public (`gateway/src/index.ts:280`), and `/` serves its HTML with the token injected (`__BOOKCLAW_AUTH_TOKEN__` placeholder). So in the **default posture** (auth token set, `BOOKCLAW_ALLOWED_IPS` unset) anyone who can reach the port can `curl http://host:3847/`, read the token out of the HTML, then authenticate — the bearer token does **not** stop a deliberate LAN actor; only the source-IP allowlist or the host firewall does.
  - **Threat model (decided):** single-user home LAN, occasional family curiosity. A casual family member won't view-source to extract the token, so token-injection is adequate *against that actor*. This is also why bearer-token was chosen over HMAC/mTLS (rejected as disproportionate). The exposure is therefore a **known, accepted limitation** for the home-LAN deployment — recorded here so it's a conscious decision, not a silent gap.
  - **What actually closes it** for a stronger threat model (e.g. VPS / untrusted LAN): set `BOOKCLAW_ALLOWED_IPS` to the trusted source IP(s)/CIDR(s) — this gates `/` too, in front of auth — and/or enforce at the host firewall / provider security group (the most robust control; mind the Docker bridge caveat that masks source IPs). A reverse proxy doing its own auth + TLS in front is the belt-and-suspenders option.
  - **If it ever needs a code fix:** gate `/` behind auth and hand the dashboard its token a different way (e.g. a one-time login form that sets an httpOnly cookie, or a short-lived session token), so the long-lived bearer token is never embedded in publicly-fetchable HTML. Out of scope for the current threat model — only revisit if the deployment posture changes.
  - **Related minor notes:** the `?token=` query fallback (native `<img>`/`<a>`/`<Audio>` GETs) puts the token in URLs, which proxies / access logs / browser history can capture — low risk on a trusted LAN but worth eliminating if `/` is ever hardened. And there is no TLS (plain HTTP on the LAN); a reverse proxy is the documented path when that matters.
- [ ] **Audit logging: source IP + native Linux log manager.** *(Deferred 2026-05-30 — design investigated, implementation parked.)* Two related pieces:
  - **(original) Audit-log the request source IP** on every gateway HTTP route and every WebSocket connection, so a compromise is forensically traceable. `workspace/.audit/` currently logs `websocket_connected` with socket id only. The IP plumbing already exists (`socketClientIp()`, `req.ip` with `trust proxy` set — from the source-IP allowlist work).
  - **(new ask) Route audit events to the native Linux log manager, with an off switch.** So events land in the OS-managed log (journald / syslog) with its rotation/retention/forwarding, not just the in-workspace file.
  - **Key finding (drives the design):** BookClaw is **Docker-first**, and the `node:22-slim` image has **no in-container journald or syslog** (`/dev/log` absent). So the container-native way to "rely on the native log manager" is to **emit structured audit events to stdout/stderr** and let the platform capture them: under a systemd unit, stdout → journald automatically; under Docker, stdout → the logging driver, which the user points at the host by setting `logging: driver: journald` (or `syslog`) in `docker/docker-compose.yml` (currently the default `json-file` driver, no custom logging). Writing directly to a `/dev/log` syslog socket would NOT work in the default Docker setup (needs `/dev/log` bind-mounted or a remote UDP syslog host), so it's the wrong default for this app — at most an advanced opt-in.
  - **Existing state to reconcile:** there is already a **hash-chained JSONL** audit log (`gateway/src/security/audit.ts` → `workspace/.audit/<date>.jsonl`, tamper-evident), the single sink behind `this.audit.log(category, action, data)` used app-wide (ip_blocked, confirmation events, etc.). The native target would be an **added sink**, not a replacement — journald/syslog don't hash-chain, so keeping the JSONL preserves tamper-evidence. (Lean: keep both, each independently toggleable.)
  - **Open decisions (all deferred):** mechanism (structured stdout *[recommended, cross-env, no dep]* vs. direct syslog socket vs. both); JSONL fate (keep both *[recommended]* vs. native-replaces vs. native-primary); off-switch scope (native sink only *[recommended, e.g. `BOOKCLAW_AUDIT_SYSLOG=0`]* vs. all audit logging vs. per-sink toggles). Implementation lands as a second sink inside `AuditLog.log()` plus the source-IP enrichment; add a smoke-test assertion (e.g. an audit line appears on stdout and carries the client IP).
- [ ] **Vault key on a multi-host Docker volume.** Verify `config/.vault/vault.enc` permissions survive volume mounts. Document the backup-and-restore path for `BOOKCLAW_VAULT_KEY` since losing it means losing every stored credential.
- [ ] **Outbound egress posture.** The AI router and `ResearchGate` make outbound calls to Anthropic / Gemini / OpenAI / DeepSeek / OpenRouter / allowlisted research domains. Confirm `helmet`-level CSP doesn't accidentally weaken egress controls. (Note: `connectSrc` was tightened to `'self'` on 2026-05-30 — but CSP governs the *browser* dashboard, not the server's own outbound AI/research calls, so this item is about the server-side egress path, not CSP.)
- [ ] **API-level rate limiting.** *(Deferred to lower priority 2026-05-30 — design settled, implementation parked.)*
  - **What already exists:** the chat/agent path is already throttled — `PermissionManager.checkRateLimit(channel)` (`gateway/src/security/permissions.ts:69`) enforces 30 messages/minute per channel and runs as "Security Check 2" in `handleMessage()` (`index.ts:1180`), which the WebSocket `message` handler and the Telegram/Discord bridges all flow through. **This per-channel limiter is inherited from OpenClaw** (present at the first commit, `permissions.ts`). The real gap is the **REST `/api/*` routes** (project create, file ops, config, persona CRUD, image gen, …), which are direct Express routes with no throttle.
  - **OpenClaw provenance:** OpenClaw has the per-channel chat limiter but **no HTTP/`/api/*` route rate limiting** — consistent with its localhost-only design, where the REST surface was never a hostile attack surface. The fork's LAN exposure is what creates the need, so this piece is net-new, not a regression.
  - **Design already decided (user, 2026-05-30): auth-aware + exempt trusted IPs.** Strict limit on **unauthenticated** requests (token brute-force / anon DoS guard); **generous** safety-net limit on authenticated requests (catches a runaway/buggy client burning AI credits without throttling real work); **loopback + `BOOKCLAW_ALLOWED_IPS` exempt entirely**; all limits env-configurable. This deliberately avoids throttling a busy authenticated **MCP client** (the user's stated concern) — MCP authenticates and hits `/api/*`, so a naive per-IP limit would have hit it.
  - **Still open:** implementation choice — `express-rate-limit` (v8.5.2; 1 runtime dep `ip-address`; standard `429` + `Retry-After`/`RateLimit-*` headers so clients back off gracefully; correct IPv6 keying) vs. a hand-rolled per-IP Map mirroring `permissions.ts` (no new dep, matches existing idiom, ~30–40 lines incl. headers). Either satisfies the design; the `skip` exemption is a few lines both ways. Place the gate after the IP gate; key on `req.ip` (respects our `trust proxy` setting, so it matches the `BOOKCLAW_ALLOWED_IPS` gate).

## Quick cleanups (minutes)


## Investigations (under an hour each)


## Larger items (worth scoping before starting)

- [ ] **Extend test coverage to `ProjectEngine` step orchestration + completion hooks.** The test suite now exists — `node --test` (via `tsx`) unit tests + curl API tests, see [COMPLETED.md](COMPLETED.md) 2026-06-01 — with `AIRouter` fully covered and `ProjectEngine`'s pure surface (`getTemplates`, `inferProjectType`) covered. The remaining gap is the *orchestration* path: step sequencing, multi-pass continuation, and the `onProjectCompleted` hooks. Those chain real AI calls through `aiRouter.complete`, so testing them cleanly needs an injection seam (a fake AI provider) — which is intertwined with the **god-class Level 2–3** decomposition below. Do this alongside that refactor rather than bolting test-only hooks onto the current monolith.
- [ ] **God-class refactor — Levels 2–3 (deferred).** **Level 1 is DONE** — see [COMPLETED.md](COMPLETED.md) 2026-06-01: `index.ts` 2,649 → 2,103 (init phases extracted into `gateway/src/init/` modules; `initialize()` is a thin composition root) and `routes.ts` 5,518 → 59 (234 endpoints split into 12 per-feature mounters under `gateway/src/api/routes/` + `_shared.ts`). Remaining per [GOD-CLASS-REFACTOR.md](GOD-CLASS-REFACTOR.md): **Level 2** (service registry) and **Level 3** (plugin contracts — pipelines/providers/channels as data-driven plugins, the North-Star seam). **Defer until** a feature actually needs them *and* `AIRouter`/`ProjectEngine` have unit-test coverage (the doc's stated prerequisite — see the "No test suite exists" item). Don't refactor in a way that hardens the single-author / hardcoded-pipeline assumptions.
- [ ] **Playwright end-to-end test suite.** Once the Mercury deploy automation lands, build a Playwright suite that drives the dashboard against the live container on Mercury: dashboard loads, chat flows, project creation through one production phase, file upload/export, persona CRUD, confirmation-gate approval flow. Decide test runner home (`tests/e2e/` in this repo), how Playwright authenticates to the Mercury host (URL via env var), and what counts as a passing baseline before adding more flows. Complements — does not replace — the existing "No test suite exists" item, which targets unit-level coverage of `AIRouter` and `ProjectEngine` rather than browser-level e2e.
- [ ] **Report real AI costs — follow-ons (account total + pre-call estimates).** The primary per-call fix is DONE (see [COMPLETED.md](COMPLETED.md) 2026-06-01: `completeOpenAICompatible` now uses OpenRouter's `usage.cost`). Remaining, optional: `GET /api/v1/credits` (total credits + total usage on the key) to back a real "spent so far" panel; `GET /api/v1/models` real per-token `pricing.prompt`/`pricing.completion` to replace the placeholder *pre-call* estimates and to cost providers that don't return per-call cost (DeepSeek/Gemini/Claude/OpenAI still use the Sonnet-priced placeholder). Docs: [Usage Accounting](https://openrouter.ai/docs/cookbook/administration/usage-accounting).

- [ ] **Multi-author, multi-book platform with customizable per-author/per-genre pipelines. _(Ultimate goal — umbrella item; framing lives in [North Star](#north-star--the-ultimate-goal-use-this-to-weigh-every-other-decision).)_** The end state: BookClaw runs as a multi-author studio where several books are in flight at once, each owned by a distinct author identity writing in its own genre with its own production pipeline — e.g. a Romantasy from author A, a contemporary romance from author B, and a mundane sci-fi from author C, all simultaneously, with room to add more authors/genres/pipelines without code changes. Today the gateway assumes a single author identity (`workspace/soul/{SOUL,STYLE-GUIDE,VOICE-PROFILE,PERSONALITY}.md`), treats `genre` as a free-text field on each project, and hardcodes the production pipeline (6 templates + `novel-pipeline` in `services/projects.ts` — steps, prompts, taskTypes, and word-count targets are all in code). Build out:
  - A **book** entity (id, title, current phase, author-style ref, genre-style ref, pipeline ref, project history) so multiple books can be in flight simultaneously at different production phases, with a dashboard view that shows each book's current phase and next action.
  - **Author style profiles** — multiple named identity bundles (each its own SOUL/STYLE-GUIDE/VOICE-PROFILE set), selectable per book. Reuse `services/personas.ts` and `services/style-clone.ts` rather than inventing a parallel system; the storage shape should let an author profile be cloned, edited, and assigned without touching the global `workspace/soul/`.
  - **Genre style profiles** — named genre packs (tropes, beats, reader-expectation notes, comp-title pointers) selectable per book, replacing the current free-text `genre` field while keeping backward compatibility with existing projects.
  - **Customizable pipelines (the flexibility ask).** Lift the hardcoded `ProjectEngine` templates into editable, data-driven pipeline definitions so a user can add/remove/reorder steps, edit each step's prompt, change its `taskType`/tier and `wordCountTarget`, and attach skills — all without touching code. A pipeline becomes a named, versioned artifact selectable per book (and per author/genre — a Romantasy author's pipeline can differ from a sci-fi author's). Keep the existing 6 templates + `novel-pipeline` as built-in defaults/seeds; allow cloning one as a starting point. Decide storage shape (JSON under `workspace/.config/` vs. per-author files), how step prompts interpolate author/genre/book context, and how in-flight books pin a pipeline version so editing a pipeline doesn't corrupt a book already mid-production.
  - Migration path for the existing single-book / single-soul setup: treat the current `workspace/soul/` as a default author profile, existing projects as a default book, and the current hardcoded templates as the default built-in pipelines, so nothing breaks on upgrade.

- [ ] **MCP server for BookClaw.** Expose BookClaw's capabilities (projects, chat, files, personas, research, etc.) over the Model Context Protocol so external MCP clients can drive it. Decide transport (stdio vs. HTTP/SSE) and whether it wraps the existing REST API or shares the internal handlers directly. **Security:** an MCP client hitting the HTTP API is a non-browser, no-`Origin` request — CSP `connect-src` does not apply to it; CORS already allows no-Origin requests; the client must present the bearer token (`BOOKCLAW_AUTH_TOKEN`) and fall within `BOOKCLAW_ALLOWED_IPS`. Confirm the confirmation-gate still fronts every external side-effect when actions originate via MCP rather than the dashboard. Note: this is distinct from the *external* browser-automation MCP (Claude-in-Chrome) referenced at `routes.ts:4862+`, which BookClaw consumes rather than provides.

- [ ] **API-first frontend — every UI action goes through a system API.** Establish and enforce going forward that no dashboard/front-end feature implements behavior client-side only: every action the UI takes must call a REST endpoint, so the dashboard, the Telegram/Discord bridges, and the future MCP server (item above) all drive the same handlers. **Payoff:** full MCP/API parity — anything a user can do in the UI is scriptable/automatable. Work: audit the current dashboard for any client-only logic that bypasses `/api/*`, add endpoints where missing, and treat this as a standing review rule for new UI. Directly enables the MCP server item.

## User experience & product features

- [ ] **Built-in "start a new project" tutorial.** A guided, in-app walkthrough for creating a first project (idea → persona → pipeline → first output) so new users aren't facing a blank dashboard. Decide the form: interactive coach-marks/overlay vs. a step-by-step wizard vs. a self-seeding demo project. Pairs with the help system below.
- [ ] **Extensive in-app help system (for non-technical users).** Assume some users aren't technical: contextual help, plain-language explanations of the core concepts (personas, pipelines, providers, skills, the confirmation gate), and answers to common "how do I…" questions — surfaced inside the dashboard, not only in `docs/`. Decide scope (tooltips + a searchable help panel vs. an in-app docs viewer) and how it stays in sync with `docs/`.
- [ ] **In-dashboard editor for prompts + skills (workspace overlay, hot-reload, templates).** _(Grew out of the "markdown file viewer" idea; design decided 2026-06-01 via brainstorm.)_ Make the two things that are *already* data files — author **prompts** (`workspace/soul/*.md`) and **skills** (`skills/**/SKILL.md`, markdown + frontmatter) — editable in the dashboard without a code change or redeploy. **Persistence model (decided): built-in + workspace overlay.** Built-in skills stay baked into the image and read-only; user-created/edited skills live under `workspace/skills/**` (the persisted volume) and **override built-ins by name** — so edits survive Docker rebuilds and `git pull`, and "mine" vs "shipped" stay separated (mirrors the premium-skills pattern and the multi-author "JSON under `workspace/.config`" plan). `workspace/soul/*.md` already lives in the persisted volume. **Pieces:** (1) `SkillLoader` loads built-in + workspace overlay and gains a `reload()`; `SoulService` gains a `reload()`. (2) API (API-first): `GET`/`PUT` whitelisted files (`soul/*.md`, workspace `SKILL.md`) via `safePath`/`SandboxGuard`, `POST …/reload`, create-from-template, delete (workspace only); validate skill frontmatter on save. (3) Dashboard: extend the planned **markdown viewer into a viewer + editor** (render + edit + save); templates = clone a built-in seed as a starting point. No confirmation-gate (internal prompt files, not external side effects). **Out of scope here (it's the multi-author item):** pipelines/`PROJECT_TEMPLATES` are still hardcoded in `services/projects.ts` — lifting them to editable runtime JSON under `workspace/.config/pipelines/` reuses this exact overlay + editor + template pattern. The plain markdown *viewer* for project outputs/manuscripts is the first, smallest slice of this.

## Standing constraints (do not "fix" these)

- Server bind defaults to `0.0.0.0` and is overridable via `BOOKCLAW_BIND`. This is a deliberate departure from the upstream localhost-only contract so the Docker image is usable on a LAN. **Do not revert to `127.0.0.1`-only.** (CORS, Socket.IO, and Helmet `connectSrc` were since tightened from permissive `*` to deny-by-default / `'self'` — see COMPLETED.md; the bind itself stays `0.0.0.0`.)
- Fail-soft init (services log `⚠`, gateway continues) is intentional — don't make startup require optional deps (better-sqlite3, yt-dlp, Ollama).
- `.js` import extensions on `.ts` source files are required by `NodeNext` module resolution — don't strip them.
- `skills/premium/*/` is intentionally gitignored — never commit premium skill content.
- `ConfirmationGateService` must gate every new feature with external side effects (publish, send, submit, upload, bid, purchase).
