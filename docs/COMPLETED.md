# Completed

Items moved here from [TODO.md](TODO.md) as they are finished. Newest first. Each entry is prefixed with its completion date (`YYYY-MM-DD`) and preserves the original TODO bullet text.

## 2026-06-22

- **World Repository — Phase 1: the `world` library kind + document parsers + `WorldService` + documents CRUD API.** First implemented phase of the ★ top-priority World Repository feature (umbrella item stays in TODO; Phases 2–6 remain). A new library `kind: world` registered in `LIBRARY_KINDS` (backend `library-types.ts` + frontend `shared/src/types.ts`), `FILE_KINDS`, and `DIR_LAYOUT` (`worlds/`); a world entry is a directory holding `world.json` (parsed into `LibraryEntryFull.world` by a new `loadKind` branch — config only; `documents/` is owned by `WorldService`). New code: `world-types.ts` (shared contract types, `WORLD_SCHEMA_VERSION=1`); `world-parse.ts` (`parseWorldJson`; hand-rolled frontmatter `parseWorldDoc`/`serializeWorldDoc` with inline `tags:[a,b]` support, no yaml dependency, mirroring `skills/loader.ts`; `nextClassification` — **monotonic max+1** serials for a `{TYPE}-{DOMAIN}-{NNNN}` scheme); `world.ts` (`WorldService`: config read-through via `LibraryService` + documents CRUD in the workspace overlay + auto-classification, fail-soft `needsAttention` on bad frontmatter, `ENTRY_NAME_RE`-gated paths); `worlds.routes.ts` (7 documents routes — list/get-config/catalog/get-doc/create+auto-classify/update/delete; single-doc responses wrapped `{ document }`), mounted in `routes.ts` and wired in `index.ts` (`services.world`) + instantiated in `phase-05-research-skills.ts`. `world` explicitly rejected in `library-transfer.ts` export/import (world zip-transfer deferred to a later phase). **Built via superpowers subagent-driven TDD** (6 tasks, fresh implementer + spec/quality reviewer per task, then a whole-branch review). Two plan defects caught and resolved mid-execution: the brief's `nextClassification` tests were self-contradictory on gap behaviour (owner chose monotonic max+1); the smoke test relied on `BOOKCLAW_WORKSPACE_PATH`, which the app never reads (redesigned to seed/clean the real workspace overlay). Whole-branch review caught two cross-cutting issues (single-doc response envelope diverged from the contract; `world` fell through the transfer paths) — both fixed + locked with tests. **Verified:** world unit tests 21/21 (`world-types`/`world-parse`/`world-service`), `library-transfer` 29/29, `tests/world-crud-smoke.sh` 8/8 (boots the gateway, seeds a world, exercises create→auto-classify→list→read→update→delete), full unit suite green, `npx tsc --noEmit` clean. _(Implements the design spec `docs/superpowers/specs/2026-06-21-world-repository-design.md` §§1+5 per the plan `docs/superpowers/plans/2026-06-21-world-repository-phase-1-kind-and-crud.md`. Deploys to Mercury on next `./push.sh`; the real Luminarch/Shattered-Cradle world import is Phase 2, run against the Neptune writing instance.)_

## 2026-06-21

- **Configurable FTS-index SQLite DB location, decoupled from the workspace (`BOOKCLAW_DB_DIR`).** The `memory-search` FTS index (`workspace/memory/memory-search.db`) was the only live SQLite DB and was hardcoded inside the workspace — blocking a writing instance whose workspace lives in a cloud-synced folder (a live SQLite DB + WAL under continuous sync corrupts). Added a `BOOKCLAW_DB_DIR` env (+ `memory.dbDir` config) that relocates **just the DB file** to a separate (e.g. local, non-synced) disk; the conversation source dir it indexes stays in the workspace, and the default is unchanged (`workspace/memory/memory-search.db`) so nothing regresses. `MemorySearchService` ctor takes an optional `dbDir` and exposes `getDbPath()`; `initialize()` now `mkdir`s the DB's own parent dir; `phase-03-soul-memory` resolves the env/config (tilde-expanded, mirroring the backup-dir pattern), passes it, and logs `ℹ Memory search DB relocated → …` when set; `docker-compose.yml` passes the env through + ships a commented `/app/db` bind-mount example. **Verified:** `tsc` clean; new `tests/unit/memory-search-dbpath.test.ts` (3 cases: default path, relocated path, and `initialize()` creating the DB in the override dir with none in the workspace) passes; full unit suite 487/487; and a live boot with `BOOKCLAW_DB_DIR=/tmp/…` logged the relocation and created the DB (+ `-wal`/`-shm`) only in the override dir. _(Root-cause fix for the parked Neptune writing-instance plan — workspace can now live in the cloud-synced `~/data/bookclaw` while the live DB stays on local disk. Owner ask 2026-06-21.)_

- **Board: group "Books, in flight" by Author / Series / Genre.** Added a grouping control to the right of the phase-filter chips on the Board (`frontend/studio/src/routes/Board.tsx`): a "Group by" divider + label and three chip-sized buttons (Author / Series / Genre). Selecting one renders the (phase-filtered) books in **collapsible groups** — a header (twisty + group label + count) followed by that group's book tiles in the existing card grid — mirroring the library Genres grouping (`EntryList.tsx` / `.ghead`/`.gtw`/`.glabel`/`.gcount`). Clicking the active dimension again returns to the flat grid; switching dimensions resets which groups are collapsed (default open). Buckets sort alphabetically with the catch-all last (books with no series group under **Standalone**; missing author/genre under **Unassigned**). Frontend-only: every book already carries `author`/`series`/`genre` on `BookSummary`, so no API/data change — the card markup + New Book ghost card were extracted into shared `renderCard`/`ghostCard` and reused by both layouts. **Smoke:** `tests/board-grouping-smoke.sh` (report + **leave-in-place**, no AI) — seeds 6 deliberately varied books (2 authors, 2 genres + one un-genred, 1 series of 2 + 4 standalones) and asserts `GET /api/books` yields the grouping inputs the board reads, including the feature's catch-all buckets (no series → **Standalone**; missing author/genre → **Unassigned**) using the same `groupValue` fallbacks as `Board.tsx`. Idempotent (re-runs reseed to exactly 6) and **leaves the books on disk** for manual UI testing; `CLEANUP=1` removes them. Verified: 7/7 PASS against a live gateway, plus `tsc --noEmit` clean and `npm run build:frontend` green. _(Owner ask 2026-06-21; deploys to Mercury on next `./push.sh`. Run against the deployed instance with `BASE_URL=http://192.168.1.32:3847 tests/board-grouping-smoke.sh`.)_

## 2026-06-20

- **Ported the remaining n8n importable workflows: prompt assets, scene-drafter, and two humanize pipelines.** The fourth/final n8n-conversion batch (5-agent fan-out). **6 `prompt` assets** (`library/prompts/`, Prompt Runner, ported from Neptune single-purpose workflows): `scene-brief` (Xznp2), `chapter-summary` (Summary+, dOUOe), `character-arc-summary` (ml4PU), `public-domain-modernize` (m6OBl), `story-hack` (ArzkC), `short-story-hack` (b9wH7) — each the n8n agent prompt re-expressed as a single `systemPrompt` with the source model pinned (`model`/`temperature`). **`scene-drafter` pipeline** (Neptune `F9pZp`): a lightweight per-chapter `expand:chapters` brief→prose drafter (Haiku brief → Opus prose). **Two humanize pipelines** built from the owner's GitHub prompt repos (the external `.json` stage prompts the audit flagged as missing): `humanize-claude` (11 passes, from `github.com/pshort05/ClaudeHumanizer`, Claude models) and `humanize-gemini` (10 passes, from `github.com/pshort05/GeminiHumanizer`, Gemini models) — each a single `expand:chapters` group running the numbered de-AI passes (grammar→AI-word-cleaning→overwritten-language→sensory→subtlety→dialogue→weak-language→imperfections→[8.5 structural, Claude only]→verification→final-sweep) per chapter, all `revision` except the final `final_edit` sweep; the master prohibited-words list (~54KB) was NOT fully inlined (a curated highest-priority subset is inlined on the word passes, deferring to "the book's forbidden-words list in your context"); optional passes 6.1/9.5 skipped. **Verified:** all 9 files valid JSON; the 6 prompts parse via `parsePrompt`, the 3 pipelines via `parsePipelineJson`+`expandSteps` (valid taskTypes, non-empty prompts; humanize-claude expands 1→33, humanize-gemini 1→30, scene-drafter 1→6); gateway boots clean and serves all 9 with no load errors. Models provisional. _(Owner ask 2026-06-20; n8n-conversion steps 4-5 — see [N8N-WORKFLOW-IMPORT-AUDIT.md](N8N-WORKFLOW-IMPORT-AUDIT.md). This completes the import of the importable writing workflows; remaining work is dedup of superseded variants, not new imports.)_

- **Ported the n8n Neptune editorial suite into the "Editorial Review and Edit" BookClaw assets.** The third n8n-conversion batch (after NerdyNovelistAI + MSF) — converted the six Neptune editorial-automation workflows into **6 revision pipelines + 1 sequence**, built by a 6-agent fan-out (real n8n analyze/apply prompts extracted node-by-node, re-expressed against the pipeline schema). Each n8n editor (Form → Google Docs → JS chapter-splitter → splitInBatches → analyze agent → apply agent → Docs write) became a per-chapter `{expand:chapters}` **analyze → apply** pair: `editorial-developmental-edit` (`0lPjG`), `editorial-line-edit` (`5cLbh`), `editorial-copy-edit` (`2mBFA`), `editorial-proofread` (`8cURn`), `editorial-alpha-read` (`I5qY9`). The multi-model `editorial-outline-council` (`T1CaY`, "Council of LLMs") became two `{parallel}` model-fan-out rounds (5 then 3 models) → a synthesis `final_edit` (real model IDs recovered from the node params, not the lying display names). The **`editorial-review-and-edit` sequence** chains the four classic manuscript passes in order (developmental → line → copy → proof); `editorial-alpha-read` (needs supplied reader feedback) and `editorial-outline-council` (outline/planning-stage) were imported standalone, **not** auto-chained. **Adaptations:** splitInBatches→`expand:chapters`; Google Docs I/O + JS chapter-splitter stripped (input = "the Chapter {{n}} draft in your context"); Merge fan-ins→`parallel` groups; apply steps constrained to surgical, length-preserving edits (`wordCountTarget {{wordsPerChapter}}`). **Verified:** all 7 files valid JSON; all 6 parse through the real `parsePipelineJson`+`expandSteps` (valid taskTypes, non-empty prompts; editors expand 1→6, council 3→9); the sequence resolves its 4 members; gateway boots clean and serves all 7 with no load errors. Models provisional (owner to confirm against OpenRouter). _(Owner ask 2026-06-20; n8n-conversion step 3 of 5 — see [N8N-WORKFLOW-IMPORT-AUDIT.md](N8N-WORKFLOW-IMPORT-AUDIT.md). Remaining: standalone prompt assets, then folding the romantasy/numbered-suite variants.)_

- **Genre picker: search + publishing-standard grouping.** The New Book genre picker rendered all 192 genres as one flat grid. Added (1) a type-to-filter **search box** (matches name + description; when a query is present, grouping is bypassed for a flat hit list), (2) a `groups: string[]` field on every genre's `meta.json` classifying it into the 12 publishing-standard buckets (Romance, Fantasy, Science Fiction, Mystery & Crime, Thriller & Suspense, Horror, Western, Historical, Action & Adventure, Speculative & Dystopian, Literary & Upmarket, Comedy & Satire) — hybrids carry several (e.g. `space-fantasy-romance` → science-fiction + fantasy + romance) — and (3) **collapsible group sections** in the picker (a genre appears under each group it belongs to; the group holding the current selection auto-expands; unknown/empty-group genres fall into an "Other" bucket so none can vanish). All 192 classified by a 12-agent fan-out, validated to canonical slugs only. Threaded `groups` end-to-end: `meta.json` → `LibraryService.readMetaSidecar` (refactored from `readDescriptionSidecar`, defensive array/string parsing) → `loadKind` (with builtin-overlay fallback) → `toRow` → `/api/library/genre` → both `LibraryEntry` defs (backend `library.ts` + shared `types.ts`) → studio `pickGenre()`. Independent code review run (one MEDIUM — dead `kind === 'genre'` branches orphaned in the generic `pick()` — fixed; rest LOW/NIT). Verified: `tsc` clean (backend + studio + chat), `build:frontend` green, perimeter smoke (21 checks) green, and a live boot + `curl /api/library/genre` confirming all 192 entries carry `groups`. Follow-up (→ TODO): fold the `yakuza-triad`/`yakuzatriad` and other near-duplicate genre dirs. _(Owner ask 2026-06-20; deploys to Mercury on next `./push.sh` — `library/` is baked at image build, so a rebuild is needed for the new `groups` to reach Mercury.)_
  - **Follow-up (same day):** extended the same search + collapsible groups to the **Library page** (`/library`) Genres list (`EntryList`, library scope only — book scope keeps the flat single-snapshot view). Extracted the shared `GENRE_GROUPS` taxonomy to `frontend/studio/src/lib/genreGroups.ts` so both the New Book picker and the Library list share one source.
  - **Follow-up (same day):** added a top-level **book selector** to the Library & Assets topbar (`AssetStudio`) — choosing a book sets it active (`POST /api/books/active`) and switches to book scope, showing that book's files; `EntryList` is keyed on the active book so it re-fetches on switch. Also fixed a stray `var(--line-1)` (undefined token) in the NewBook/AssetStudio hover rules.

- **Ported the n8n StoryHackerAI + MSF suites into BookClaw pipelines + sequences.** Converted the two cleanest Mercury n8n novel systems (identified in the [n8n import audit](N8N-WORKFLOW-IMPORT-AUDIT.md)) into native BookClaw `library/` assets — **11 new pipelines + 2 new sequences**, faithful ports (real n8n agent prompts, rubrics, blocklists, slider frameworks extracted node-by-node via the n8n MCP and re-expressed against the BookClaw schema), built by an 11-agent fan-out against `romantasy-planning.json`/`romantasy-production.json` as the reference. **StoryHackerAI** (the BookClaw asset suite is named **NerdyNovelistAI**) → `nerdynovelistai-stage1-dossier` (genre→brainstorm→dossier→emotional/name/logic critiques), `nerdynovelistai-stage2-characters` (book_bible), `nerdynovelistai-stage3-worldbuilding` (book_bible), `nerdynovelistai-stage4-outline` (outline + emotional/slider/logic passes), `nerdynovelistai-stage5-chapters` (per-chapter brief→draft→checks→rewrite) + the `nerdynovelistai` **sequence** (renamed from `storyhacker` 2026-06-20 at owner request; the n8n source provenance still cites the original StoryHackerAI workflows). **MSF (Mundane Science Fiction)** → `msf-phase1-ideation` (5-generator + 5-lens multi-model `parallel` fan-out → select → make-strange), `msf-phase2-developmental`, `msf-phase3-outline`, `msf-phase4-prose` (per-chapter, Opus→Gemini→Sonnet cross-model laundering), `msf-phase5-summary-bible`, `msf-phase6-finalize` (blurb + cover-art prompt) + the `msf` **sequence**. **Adaptations:** n8n `splitInBatches` chapter loops → `{expand:chapters}`; `Merge` multi-model juries → `{parallel}` groups + per-step `modelOverride`; per-character/per-element loops → flattened single `book_bible` passes (BookClaw has no generic per-item loop); local-FS/Google-Docs I/O, `executeWorkflow` wiring, slug pointers, and inline XML-contract validator JS nodes stripped, with context re-expressed as "in your context above" + the book's genre guide; n8n Phase-0 scaffolding dropped (book container replaces it) and the Phase-6 cover-image HTTP step dropped (native cover tooling). **Verified:** all 13 files valid JSON; all 11 parse through the real `parsePipelineJson` + `expandSteps` with only valid taskTypes and non-empty prompts (Stage 5 expands 2→19 steps, MSF ideation 5→13 with the parallel fan-out); both sequences resolve their members; gateway boots clean and `/api/library/{pipeline,sequence}` serves all 13 with no load errors. **Provisional:** the carried-over OpenRouter `modelOverride` model IDs need owner confirmation (some n8n nodes used preview/experimental strings). Remaining n8n-conversion work (Neptune editor personas, prompt assets, romantasy/numbered-suite folding) tracked in TODO. _(Owner ask 2026-06-20.)_

## 2026-06-19

- **Pipeline steps: per-step model pinning (+ temperature) and parallel step execution. _(Owner ask 2026-06-19; the two enablers the romantasy-planning port flagged as TODO — now built.)_** Two features, each run through an 8-stage per-feature pipeline (Opus design+plan → Sonnet TDD → Opus review → Sonnet fix-medium+ → Haiku full-suite run → Sonnet fix → Opus smoke), fanned out across model-tiered agents. Plans: `docs/superpowers/plans/2026-06-19-{per-step-model-pinning,parallel-step-execution}.md`. **(1) Per-step model pinning:** a `library/pipelines/*.json` step may carry `modelOverride: { provider, model?, temperature? }` that pins its AI call's provider/model/temperature, falling back to tier routing (with a warn) when the provider isn't configured. Threaded JSON → `emitStep` (`pipeline-expand.ts`) → `ProjectStep` (resolved→step map) → `stepRouting` (`_shared.ts`) / `handleMessage` (`index.ts`) → `aiRouter.complete()`; temperature guarded with `typeof === 'number'` (preserves `0`). Review caught + fixed a HIGH (temperature was dropped on the `/execute` + `/auto-execute` REST paths because `stepRouting` didn't surface it). **(2) Parallel step execution:** a `{ "parallel": [steps...] }` group fans its members out as a batch; the next ordinary step is the implicit JOIN/barrier, gated until every member is completed/skipped. `expandSteps` stamps members `parallelGroup: "g<index>"`; the engine selector (`runnableSteps`/`startProject`/`completeStep`/`activeFrontier`/`resumeProject` in `projects.ts`) drives fan-out + barrier; `pipelinePhases` descends into groups for board rendering. Review caught + fixed a **CRITICAL** (the frontier used `find(pending)`, which skipped still-`active` members and fanned the *next* adjacent group early — exactly the romantasy-planning g0→g1 topology; fixed by gating each group/join behind `groupComplete` of all earlier groups, which also wired in the previously-dead `groupComplete`). **True wall-clock concurrency** (`Promise.all`) delivered in the autonomous driver (`runProjectAutonomously`) via the new `activeFrontier()` helper. **Known gap (→ TODO):** the `/auto-execute` REST route (studio's primary runner, ~200 lines inline) keeps correct barrier bookkeeping but still runs members one-per-tick — true concurrency there is deferred to the god-class `ProjectEngine` extraction. `library/pipelines/romantasy-planning.json` was upgraded to carry **both** the two parallel groups (g0=4 generators, g1=3 evaluators, editor-in-chief join) **and** per-step `modelOverride` (the 4 generators on distinct OpenRouter models at temp ~1.0-1.1, the evaluators at 0.3 — **model ids are provisional, owner to confirm against their OpenRouter account**). Verified: **484 unit tests** (up from 468; per-step-model-pinning, project-step-modeloverride, step-routing-temperature, pipeline-parallel, parallel-orchestration, adjacent-groups, activeFrontier), `tsc` clean, perimeter smoke green, and a new hermetic `tests/parallel-pinning-smoke.sh` (17 checks, real HTTP + engine, zero AI spend, non-destructive) asserting model-pin passthrough, the `g0`/`g1` markers, and fan-out + barrier over the API. Two features integrated by hand (the parallel TDD ran in a worktree branched before the model-pinning feature; merged the overlapping interfaces/map and rebuilt the pipeline). Deploy to Mercury armed via `build_now` — fires on the owner's next `./push.sh`.

- **Romantasy planning pipeline — idea → outline + bibles (ported from the n8n "Idea to Book Outline - Romantasy V2" workflow).** New built-in `library/pipelines/romantasy-planning.json` (18 steps) reproducing that workflow's idea-to-outline development as a BookClaw pipeline, per the owner's three keys. **(1) Starts from the genre template:** the n8n's Google-Docs inputs (TROPES / WORLD_BUILDING / BRAINDUMP) become the book's romantasy **genre guide** (auto-injected — supplies tropes/reader-expectations/comps) + the author's `{{description}}`. **(2) Ends with all production files:** the four artifacts the `romantasy-production` pipeline consumes — **Chapter Outline, World Bible, Character Bible, Production Notes** — each emitted as its own step; the outline is pinned to the book's `{{chapterCount}}` so planning and production align. Designed to run as a pipeline **sequence** (`romantasy-planning` → `romantasy-production`). **(3) Diversity-then-judge ideation:** 4 concept generators → 3 evaluators → an Editor-in-Chief reducer, faithfully mirroring the source's fan-out/fan-in shape. The deep romantasy DNA from the source prompts is baked in: Romance.io spice L3-4 (L1-2 career-damaging, L5 market-limiting), MMC in first 10%, dual first-person POV, **Power With** (vs Power Over) with the autonomy test, **Female Gaze**, HEA/HFN-as-equals, dual narrative engine, both-leads flaws driving the ~80% black moment, the 9-milestone romance arc, the forbidden-AI-names list + phonetic rules, the **Cage Framework** world-building (Ban List / Punishment Ladder / Reward Matrix / Dilemma Generator), and the 9 engagement/hook techniques. **Two adaptations** (documented in the file): BookClaw has no parallel fan-out or per-step model pinning yet, so the 4 generators + 3 evaluators run **sequentially** with distinct **angles/emphases** (dark-political / witty-collapse / epic-war / intimate-curse; market / craft / commercial lenses) standing in for the source's model diversity; and the orphaned/duplicate critique nodes were dropped, with the evaluate→apply pairs kept as the active quality loop (name cleanup → engagement/hook pass → romantasy market-standards pass; character build → depth/consistency pass). Per-book-once cost (~18 calls), not per-chapter. Added a **TODO** (Larger items) for the enabling feature the owner asked to track: **parallel fan-out + per-step `modelOverride` for pipeline steps** (the two capabilities needed to fully reproduce "4 different models in parallel"; relates to the Canon-Driven parallel-chapter gap). Verified: parses via `parsePipelineJson`, loads as a builtin through `LibraryService`, `expandSteps` interpolates all `{{vars}}` (outline → 30 chapters when targetChapters=30), only known routable task types used. `tests/unit/romantasy-planning-pipeline.test.ts` (4) added; `tsc` clean; pipeline/library suites green. _(Owner ask 2026-06-19; pairs with the romantasy-production pipeline below.)_

- **Romantasy multi-stage production pipeline (ported from the n8n "Romantasy Book Writer - Shattered Cradle World" workflow).** New built-in `library/pipelines/romantasy-production.json` reproducing that workflow's per-chapter craft chain as a BookClaw `expand:chapters` pipeline. **Per chapter, 5 stages:** Scene Brief (`outline`) → First Draft (`creative_writing`) → Improvement Plan (`revision`) → Rewrite (`revision`) → Humanize & Final Polish (`final_edit`), then a compile-report step (~5 AI calls/chapter). The n8n WRITING_STYLE + FORBIDDEN_PATTERNS fingerprint is baked into the prose stages verbatim-in-essence (no em dashes, no -ly adverbs, kill-the-rule-of-three, chaotic cadence, human-mess interiority, no dialogue tags beyond said/asked, no filtering language, the AI-cliché + word-swap lists, "not X but Y" collapse, the 14-dimension dev-edit + AI-score critique, and the overused-name avoidance list). **Two adaptations** from the source (documented in the file's `description`): BookClaw has no conditional branch, so the n8n `if (title contains "Intimacy")` INTIMACY agent is folded into a **self-gating** instruction in the Humanize step (enhance on-page intimacy at Heat Level 4 / female-gaze / consent-woven, else leave untouched — never add a scene); and BookClaw runs steps **sequentially** rather than the source's ~89-agent parallel fan-out. Intimacy guidance kept at romance-craft level (heat-level control, emotional-over-physical, sensory layering) rather than transcribing the source's explicit slang lists. **Scope: production-only** — assumes the book already carries a chapter outline + character bible + world guide (from a planning pass and one of the existing `romantasy`/`dark-romantasy`/etc. genre guides), matching the n8n workflow which loaded those from Google Docs. Verified: parses via `parsePipelineJson`, loads as a builtin through `LibraryService`, and `expandSteps` flattens to 5×chapters+1 with all `{{n}}`/`{{wordsPerChapter}}` substituted. `tests/unit/romantasy-pipeline.test.ts` (2) added; `tsc` clean; full pipeline/library unit suites green. _(Owner ask 2026-06-19; relates to the "Romance Book Writer" n8n source and the [Canon-Driven Pipeline](CANON-DRIVEN-PIPELINE.md) umbrella — this is a concrete first multi-stage production pipeline.)_

- **`/genre` chat command (chat + Telegram).** Selects a library genre per channel that steers chat prompts and is a natural pre-fill for new books. Built TDD-first after a review of a first non-TDD cut (reverted and rebuilt). **(1) Resolver:** new pure `gateway/src/services/genre-match.ts` `matchGenre(available, query)` — exact → normalized-exact → unique normalized-substring → ambiguous(candidates)/none, folding case **and** separators so chat input "dark romance" matches the hyphenated library slug `dark-romance` (9 tests). **(2) Per-channel store** on `BookService`: `getChannelGenre`/`setChannelGenre`/`clearChannelGenre`, persisted to `.config/channel-genres.json` and pruned on init when the genre is gone from the library (mirrors the Phase 10 channel-books override; 7 tests). **(3) Prompt wiring:** extracted the genre-guide composition out of `genreGuideOf` into a shared `composeGenreGuide(files)` and added `getChannelGenreGuide(channel)` that composes the selected genre's guide from the **live library** (not a book snapshot) so free chat with no book still gets genre context; injected as a **fallback** in `buildSystemPrompt` assembly — a resolved book's own genre stays authoritative (1 test; existing `genre-guide` tests confirm the refactor). **(4) Surfaces:** shared `listGenres`/`selectGenre` command handlers reused by the dashboard `/genre` command and a new explicit Telegram `/genre` handler (the Telegram router never reached `handleDashboardCommand`, so the first cut silently did nothing on Telegram); `/help` updated on both. **450 unit tests** (17 new), `tsc` + frontend build clean.

- **Editor function not working as expected (bug-fix queue #2).** Brainstormed → spec (`docs/superpowers/specs/2026-06-19-editor-modes-design.md`) → TDD. Three parts. **(1) Selection menu:** `/editor` and `/editors` (no args) now print a numbered menu (`N. Name — Specialty`) with the brainstorm/critique command per editor (new pure `editor-command.ts` parse+menu; a `specialty` field added to the editor schema + `EditorEditor`). **(2) Two modes per editor:** brainstorm vs critique via a shared `MODE_DIRECTIVE` appended at compose time; the 5 built-in editor prompts neutralized (hardcoded brainstorm framing removed); `/editor <name> <mode> [book]` enters, a bare name re-prompts; mode persisted per channel (legacy records default to brainstorm); an in-character **AI greeting** on entry (cost recorded, static fallback). **(3) The actual bug:** the studio chat + standalone Chat app send over **Socket.IO**, whose handler called `handleMessage` directly — so `/editors` etc. went to the model as prose. The socket handler now dispatches commands via `handleDashboardCommand` (shared `isChatCommand` helper, de-duped with `/api/chat`), keyed to the socket's own channel; ephemeral `webchat:*` editor pointers are cleared on disconnect + pruned on init. Shared `resolveEditorRouting` so the OpenRouter model-pin can't drift. Two `/code-review` passes (8-angle): 1 medium fixed (pin duplication), 1 low (dash regex), plus the self-caught `webchat:*` persistence leak; "missing await" was a verified false positive. **428 unit tests** (chat-command/editor-command/editor-prompt/editor-parse/editor-store), `tsc` + frontend build clean; `editors-smoke.sh` + new `chat-socket-smoke.js` cover the API and socket paths. Deployed to Mercury (commits `4df902e` + `4e9b530`); owner-verified working in the Chat app.

- **Chat window still not scrolling (bug-fix queue #1).** The standalone Chat app (`frontend/chat/`) center column lacked `overflow: hidden`, so the 100vh grid row grew to fit every message and `body{overflow:hidden}` clipped the composer off-screen with no scrollbar (the studio's `.main` already had it). Added `overflow: hidden` to `.center` (mirrors the studio shell) so the inner `.thread` scrolls; widened the content caps 640px → 820px to use more screen width. Frontend build green; deployed (commit `279aa43`) and owner-verified.

- **Full-codebase bug audit + fix sweep.** Fanned out across multiple agents in four stages. **(1) Audit:** 18 read-only finder agents partitioned every source file under `gateway/src/` and `frontend/`; each candidate independently verified (recall-biased — only provably-false dropped). 116 candidates → 20 refuted → **95 kept** (19 high, 41 medium, 35 low), documented in [BUG-REVIEW-2026-06-18.md](BUG-REVIEW-2026-06-18.md). **(2) Fix:** one fixer agent per file (disjoint files = no edit conflicts) applied surgical fixes — **94 fixed**, 1 correctly skipped as a false positive; I reverted 1 more (a backup `.vault`/`.audit` exclusion that contradicted the documented offsite-encrypted-copy design and its test). **(3) Review:** an 8-angle code review of the fix diff caught **20 regressions/gaps the fixers introduced** (4 high, 7 medium, 9 low) — mostly fixers over-correcting a false positive and regressing working behavior (e.g. a greedy dialogue-quote regex, a TTS provider-precedence reorder that dead-coded voice detection, a goals pace baseline, a zip-budget check that inflated payloads before the size gate = decompression-bomb DoS). **(4) Resolve:** all 20 fixed by hand (reverts where the original was correct; deeper fixes where warranted — balanced-brace JSON bounding in `context-engine`, a shared `finishRun` + running-guard in `cron-scheduler`, an in-memory per-day line counter in `memory`, chat-app token escaping to match the studio, backward-compatible `proposeStep` phase fallback). Net: ~57 files changed. Verified after each stage and at the end: backend + frontend `tsc` clean, **410/410** unit tests, frontend Vite build green, perimeter smoke all-pass. One pre-existing defense-in-depth gap (import HTML scan only covered `.md/.txt/.json`) was closed by widening `SCAN_EXTS` to the markup formats, since findings are advisory not hard-reject.

## 2026-06-18

- **Frontend dialog + chat polish (F46).** Three parts. **(1) In-app dialogs:** new `DialogProvider` + imperative `useDialog()` hook (`frontend/studio/src/components/Dialog.tsx`, mounted at the studio root) exposing async `confirm()/prompt()/alert()` that resolve via a Promise — a near drop-in for the native API (`if (!confirm(x))` → `if (!(await confirm(x)))`). One modal renderer matching `DeleteBooksModal` (scrim, Esc-to-cancel, shared `Button`). Swept **all 7** native-dialog sites (the TODO named 4; also `Files`, `PromptRunner`, `Series`): EntryList/Settings/ProseEditor/Files/Confirmations/PromptRunner/Series. **(2) Shared sanitized Markdown:** extracted `renderMarkdown()` into `@bookclaw/shared` (`DOMPurify.sanitize(marked.parse(...))`; `marked`/`dompurify` moved to shared deps), replacing the inline duplication in `ProseEditor`/`Files` and now rendering **assistant** chat replies as sanitized HTML in the standalone Chat app's `ChatPane` (user messages stay plain text — never rendered as HTML). **(3) Chat-port plumbing:** `phase-11-http.ts` injects `BOOKCLAW_CHAT_PORT` into the served studio HTML (a `__BOOKCLAW_CHAT_PORT_VALUE__` placeholder, distinct from the global name like the token bridge, so `replaceAll` substitutes only the value); `Rail.tsx` reads `chatPort()` and **hides the Chat link when chat is disabled** (port unset), dropping the hardcoded `:3848`. Two fanned-out code-review sweeps (dialog correctness + XSS/regression) fixed 2: a **medium** XSS — the injected chat port is now digits-only sanitized so a malicious env value can't break out of the JS string literal; and scoping the dialog keydown effect to `[active]`. Verified: backend `tsc` clean + **410 unit tests**; studio+chat `tsc -b` + Vite build green; perimeter smoke passes incl. a new assertion that the served HTML carries the injected chat port (the global name intact). No component test framework exists and the project avoids adding one — verification is build + the backend smoke.

- **Documentation refresh (owner roadmap #8) — full repo-wide pass.** Two stages. **(1) Initial manual pass:** corrected the most-visible staleness in CLAUDE.md (workspace gate no longer "forthcoming"), `version.ts` comments, and QUICKSTART/SECURITY/ARCHITECTURE. **(2) Multi-agent fan-out** (`docs-refresh` workflow, 23 docs × update→verify, 46 agents) auditing every doc against the live codebase, with a per-doc mandate: **LIVING** docs (README, ARCHITECTURE, QUICKSTART, SECURITY, GLOSSARY, LAUNCH-GUIDE, FIRST-NOVEL-GUIDE, the how-to guides, TELEGRAM-SETUP) updated surgically to current reality; **HISTORICAL** point-in-time records (decision logs, the dated code review, refactor/feature plans, OpenClaw/StoryHacker audits, product direction) **preserved** — only a dated `Status (2026-06-18)` note added near the top, never a rewrite of the analysis. 16 docs changed, 7 already accurate. Themes fixed across the living docs: the retired vanilla-JS **dashboard → v6 studio**; obsolete flat `workspace/projects/<id>/NN-phase/*.md` layout → per-book `workspace/books/<slug>/data/`; hardcoded templates → **config-not-code pipeline sequences**; book-container model **complete (Phases 0–12)**; CalVer + the boot **version gate**; full security-perimeter env vars (rate limiting, `?token=`, allowlist, `0.0.0.0` bind); **there is a ~410-test unit suite** (not "no test suite"); Docker host bind-mount; corrected banner/log strings (`Dashboard:` not `Studio:`, `✓ Telegram bridge connected (command center mode)` not a robot emoji), endpoint (`POST /api/projects/create`), and the default-off Chat app. A read-only verify stage reviewed each diff (no over-rewriting of historical docs, no introduced false claims); its three actionable notes were fixed by hand: ARCHITECTURE 3-tier output routing, the second stale Telegram log line, and `ProjectStep.modelOverride` (not `.model`) in PRODUCT-DIRECTION. No emojis added (the `✓` in code blocks are literal console-output reproductions). `TODO.md`/`COMPLETED.md` and the `superpowers/` working dir were out of scope.

- **Versioning — breaking-change gate (owner roadmap follow-on to #1).** Built the enforcing boot-time workspace-compatibility gate. New pure module `gateway/src/services/workspace-version.ts` owns `WORKSPACE_SCHEMA_VERSION` (=1) + new `WORKSPACE_MIN_SUPPORTED` (=1) + `classifyWorkspace(v)` (`<MIN→quarantined`, `>CURRENT→readonly`, else `ok` — mirrors the per-book `classifyVersion` in book-types.ts) + `workspaceGate(version, override)` returning `{halt, level, message}`. `phase-01-config.ts` now reads the persisted `workspace/.bookclaw/workspace.json` `schemaVersion` on boot (missing/non-numeric → 0 → quarantined, fail-closed like the per-book `?? 0`) and, on a determinable incompatibility, prints a FATAL message and `process.exit(1)` — refusing to start so a too-new (or too-old) app can't corrupt the data. The conscious human override is `BOOKCLAW_SKIP_VERSION_GATE=1` (mirrors the `BOOKCLAW_AUTH_DISABLED` escape hatch), which downgrades the halt to a loud `⚠` warning and continues (unsafe). Fail-soft is preserved for non-version problems (missing marker → stamp; unreadable marker → warn + continue). **Owner decisions (2026-06-18):** `BREAKING_VERSION` and `WORKSPACE_SCHEMA_VERSION` stay **separate** — the gate is version-driven by `WORKSPACE_SCHEMA_VERSION` (already persisted in the marker); `BREAKING_VERSION` remains an API-surfaced product marker only (stale `version.ts` comments corrected). "Refuse" = **hard-stop with a human-consent override**, not warn-and-continue. TDD `tests/unit/workspace-version.test.ts` (9 — boundary classify + gate ok/fatal/override branches) + `tests/version-gate-smoke.sh` (self-cleaning real-boot smoke: too-new marker halts non-zero with FATAL, override continues past the gate, compatible marker proceeds; backs up + trap-restores the real marker). **410 unit tests** (9 new), `tsc` clean, version-gate smoke 8/8 + perimeter smoke green. Two independent code-review sweeps (security/correctness + conventions/regression): no medium+ findings (the `process.exit` is the documented, intentional fail-closed exception to the fail-soft init pattern). Remaining (separate TODO): the `CLAUDE.md` line calling the gate \"forthcoming\" is updated in the documentation refresh.

- **Sequence phase ordering under autonomous mode (surfaced by the F1 review).** The F1 auto-advance enforced phase order only on the default/bridge/poller paths; the autonomous **heartbeat** selected work over `(active||pending) && stepsRemaining>0` with no `pipelineId`/`pipelinePhase` awareness, so with autonomous mode ON it could run a later sequence phase before an earlier one completed (all chained phase projects are heartbeat-eligible at creation). Fix: extracted the `advancePipeline` readiness gate into a single reusable predicate `ProjectEngine.sequencePredecessorsComplete(project)` (a pipeline phase is runnable only once every earlier phase has `completed`; a non-pipeline project is always runnable) and refactored `advancePipeline` to call it (behaviour-identical). The heartbeat→engine adapter (`phase-10-heartbeat-bridges.ts` `listProjects` closure) now filters out any `pending` phase whose predecessors aren't complete, so blocked phases never reach the scorer; `active` projects always pass (a running phase is never stranded) and phase 1 (no predecessors) always passes (autonomous mode can still start a sequence). The heartbeat stays pipeline-agnostic. TDD `tests/unit/sequence-ordering.test.ts` (7 — non-pipeline runnable, phase-1 runnable, later phase blocked while predecessor active/pending/failed, becomes runnable on predecessor completion, fresh-sequence adapter filter surfaces only phase 1); `advancePipeline`'s 5 existing tests confirm the refactor. **401 unit tests** (7 new), `tsc` clean, perimeter smoke green. Two independent code-review sweeps (correctness + regression/cross-file): no medium+ findings. Stale comment in `phase-06-content.ts` corrected (ordering is now enforced in autonomous mode). The autonomous-mode gate is deterministic-unit-covered rather than smoke-covered (a real autonomous smoke would need wake-timing and is flaky/expensive).

Config-not-code pipelines — follow-ups (from the 2026-06-14 review). All three built TDD-first, code-reviewed (finder-agent sweeps), and deployed to Mercury per-feature (commits `cce955e`, `a755a17`, `aca105f`); full feature-smoke 90/0/0. **394 unit tests** (12 new); `tsc` clean. Handoff/detail: `CONFIG-PIPELINE-FOLLOWUPS-STATUS.md`.

- **Sequence run advancement (multi-pipeline books).** A book's `pipelineSequence` of N pipelines creates N chained Projects (`createBookSequence`), but only phase-1 was started — no cross-project auto-advance, and the studio `PipelineRail` only consumed `projects[0]`. New `ProjectEngine.advancePipeline(pipelineId)` starts the next still-`pending` phase **only once every earlier phase has `completed`** (marks active, runs no AI itself — a `failed`/`paused` predecessor halts the sequence visibly rather than skipping). An `onProjectCompleted` hook (`phase-06-content`) auto-advances on each completion (the bridge "continue" path only picks up active/paused projects, so without it a sequence stalls); cost stays user/poller-driven in the default posture (the heartbeat runs the advanced phase only when autonomous mode is ON — same as it already ran the `pending` sequence projects). `POST /api/pipeline/:id/advance` is the manual/UI lever (gated identically). `PipelineRail` now follows to the next phase when the tracked phase completes + shows "Phase X / N"; `Project` gains `pipelineId`/`pipelinePhase`. TDD `tests/unit/pipeline-advance.test.ts` (5) + `tests/pipeline-advance-smoke.sh` (gate + real-AI auto-advance; Mercury 7/7). Code-review fixed 2: corrected over-claiming "zero unattended cost" comments; a follow-effect that set its guard *before* the fetch (a transient failure dead-ended the sequence). Surfaced a pre-existing gap → TODO: the autonomous heartbeat doesn't enforce sequence phase order.

- **Skill content on the studio run path.** `BookService.skillContentOf` (snapshot-preferred passive-skill content) was injected only on the bridge/`startAndRunProject` path; the studio `/api/projects/:id/execute` + `/auto-execute` paths injected nothing, so steps with `skill: write/revise` got no guidance from the studio. Extracted a shared `passiveSkillBlock(deps, skillName, bookSlug)` helper into `skill-runner.ts` (snapshot → global `SkillLoader`) and called it from all three sites (replacing the inline block in `index.ts`). Behaviour-equivalent to the old bridge code (code-review confirmed `[]`, no double-injection, valid structural typing). TDD `tests/unit/skill-injection.test.ts` (5) + `tests/skill-injection-smoke.sh` (passive skill with a marker directive → assert the marker reaches the model on `/execute`; Mercury 6/6).

- **Migrated-book pipeline baseline.** `migrateBookToV2` wrote `templates/pipeline/<name>.json` but not the matching `.baseline/pipeline/<name>.json`, so a re-pull of a migrated v1 book's pipeline reported `no-baseline` and lost 3-way merge. The migration now also writes the baseline — relocating the v1 pristine `.baseline/pipeline.json` when present, else seeding from the migrated templates content. TDD `tests/unit/book-migration-v2.test.ts` +2 (relocate + seed paths, `repullStatus.hasBaseline`); `tests/migration-baseline-smoke.sh` is a LOCAL server smoke (drops a v1 fixture, drives repull→migrate; a remote has only v2 books). Code-review `[]` (fail-soft + idempotency preserved). Low.

- **`tests/feature-smoke.sh` Tier E** added: runs the F1 + F2 dedicated sub-smokes against the same target and folds them into the full smoke (F3 noted as local/unit-covered).

## 2026-06-14

- **Prompt Runner — run writing-craft prompts against book files.** Ported the owner's OpenRouter-Interface "Prompt Runner" prototype into the studio. New `prompt` library kind (JSON, mirrors `editor`: list/read/write/export-import via `/api/library`, Asset Studio `PromptEditor`, overlay-overridable) seeded with **12 curated, de-duplicated built-ins** flattened from `~/data/Writing/AI-Tools/OpenRouter-Interface/prompts/` (dialogue/copy/on-the-nose/human-writing editors, engagement + prose-quality checkers, 7-point + female + male character audits, chapter-ending + first-chapter checkers, improve-the-middle-third). Stateless run: `POST /api/prompts/run { prompt, content, bookSlug? }` applies the prompt's `systemPrompt` to file content via the AI router at a new `prompt_run` tier (16K budget), returns output **without saving** (neutral run — the book's author/voice/genre are not injected). Preview-first **Run → decide** flow in a new studio **Prompt Runner** route: file picker (book `data/` files) + prompt picker → Run → output pane with **Replace** (original-vs-output diff; saves + versions prior), **Save as new file**, or **Discard**, plus a per-file **version history** with restore. File write-back + versioning: `PUT /api/books/:slug/files/:filename` (`writeWithVersion` snapshots the prior content to a per-file `.versions/<filename>/<ts>.md` sidecar, keep-last-20 pruned), `GET …/versions`, `POST …/restore` — all `SLUG_RE` + `dataDirOf` + `safePath` guarded. Brainstormed → spec/plan (`docs/superpowers/`) → fanned-out TDD (foundation + 2 prompt-authoring + backend + frontend agents). TDD unit (`prompt-parse`, `file-versions` incl. prune, `prompt-runner`) + `api-test` prompt + write-back round-trip + `tests/prompt-runner-smoke.sh` (real AI). Fanned-out `/code-review` (high): fixed 5 findings (library `WRITABLE` + transfer not handling `prompt` — the same two consumers missed for `editor`; unbounded version growth → keep-last-20 prune; restore route masking IO errors as 404; output budget literal → `getOutputBudget`). Verified: 382 unit, `tsc` + `build:frontend` clean, api + perimeter smoke green; live-verified locally (copy-editor cleaned up rough prose, save-back versioned the prior). Single prompt at a time (the prototype's multi-prompt chains deferred).

- **Editors — interactive developmental-editor chat.** A new `editor` library kind + `EditorService` + chat commands put a channel into a persistent "editor mode" where a developmental-editor persona replaces the author voice for informal brainstorming. `/editors` lists them, `/editor:<name>` (or `/editor <name>`) enters mode, `/editor off` exits; opt-in `/editor <name> book` injects the active book's genre + recent notes as context. Five built-in editors imported/flattened from the source configs (`~/data/Writing/genres/interactive_*_editor_*.json`): **maeve** (romantasy), **rosalind** (romance), **neil** (hard-SF), **lily** (intimate scenes), **sarah** (character names). `editor` is a JSON library kind (mirrors `sequence`): list/read/write/export-import via `/api/library`, editable in the Asset Studio (new `EditorEditor`), overlay-overridable. Chat: `handleMessage` swaps the editor's `systemPrompt` (via a new `composeEditorPrompt` helper + `buildSystemPrompt` editor branch) — author soul/genre/world/sections dropped; routed at the new `editor_chat` mid tier; an editor's optional `model` pins OpenRouter atomically (only when the resolved provider is OpenRouter) and its `temperature` is honored. Per-channel active editor persisted to `workspace/.config/channel-editors.json` (mirrors `channelBooks`), re-inited after backup restore. Brainstormed → spec/plan (`docs/superpowers/`) → fanned-out TDD (foundation + 2 config-authoring + chat-integration + frontend agents). TDD unit (`editor-parse`, `editor-store`, `editor-prompt`) + `api-test` editor assertions + `tests/editors-smoke.sh`. Fanned-out `/code-review` (high): fixed 8 findings (library write/transfer not handling `editor`, EditorEditor loading the wrong field, `withBook` not falling back to the global active book, model/provider non-atomic pin, dropped temperature, `/editors` showing slugs not labels, stale JSDoc). Verified: 375 unit, `tsc` + `build:frontend` clean, api + perimeter smoke green; live-verified locally (Maeve replied fully in character). Deferred: wiring the `/editor` commands into the Telegram bridge's command router (dashboard/API only for now — tracked in TODO).

- **Config-not-code pipelines (North Star) — named pipelines run as an editable book sequence.** Book generation is now fully data-driven. New library kind **`sequence`** (an ordered list of pipeline names; built-in `novel` preset) + a per-book **`pipelineSequence`** (source of truth, snapshotted to `templates/pipeline/<name>.json`). A book runs an ordered sequence of short, named, editable pipelines — phase order *is* the sequence (the hardcoded 6-phase enum is gone from the generation path). The previously-hardcoded **`book-production`** is now real data (`library/pipelines/book-production.json`) using a new per-chapter **expand construct** `{ "expand":"chapters", "steps":[…] }` that flattens to interleaved Write/Polish steps; `pipeline-vars.ts` exposes `{{chapterCount}}/{{wordsPerChapter}}/{{n}}` + structural beats `{{midpoint}}` etc., and `pipeline-expand.ts` does the flattening (`createProjectFromPipeline` resolves through it). Generation resolves from the book's **snapshot** (copy-on-create isolation), chaining one Project per sequence entry (`ProjectEngine.createBookSequence`). **Sections + skills wired into prompts** (`WIRED_KINDS` += section, skill): `BookService.sectionsOf` injects section content alongside genre/world; `skillContentOf` makes a book prefer its frozen snapshot skill over the global loader. **Book schema v2** (`BOOK_SCHEMA_VERSION=2`, min still 1) with a fail-soft lazy v1→v2 migration that wraps a legacy single `pipeline.json` into a one-element sequence; `setPhase` gained the deferred `assertWritable` gate. API: `POST /api/books` accepts `pipelineSequence`/`sequence` (seed); the `sequence` kind round-trips through the library + transfer (export/import) paths. Studio: `SequenceEditor`, a New-Book sequence picker (preset-seeded, reorderable), and `PipelineEditor` expand-group editing. Brainstormed → spec/plan (`docs/superpowers/`) → fanned-out TDD (foundation + book-core + orchestration + frontend agents). TDD unit coverage (`pipeline-expand`, `sequence-store`, `book-sequence`, `book-migration-v2`, `wired-kinds`, extended `book-phases`); `api-test` sequence + create-from-sequence assertions; new `tests/sequence-smoke.sh` (real OpenRouter: sequence→snapshot→expand→generation + per-book spend). Verified: 364 unit, `tsc` + `build:frontend` clean, api + perimeter smoke green. Legacy `createNovelPipeline`/`createBookProduction` kept as no-active-book fallbacks (bridges/`/novel`). Subsumes the TODO sub-asks "phase order as a pipeline property" and "wire sections/skills snapshots into prompts".

- **Persistent lifetime spend + per-book spend (+ Danger-Zone reset).** `CostTracker` gained two persisted, never-auto-reset odometers — `totalSpend` (lifetime) and `byBook` (per-book, with book-less spend keyed to `'unattributed'`) — incremented alongside daily/monthly by a new 4th `bookSlug?` param on `record()`. `getStatus()` exposes `total` + `byBook` (rounded to 4dp to match the `money()` $0.0001 renderer, so cheap-model spend isn't floored to $0.00) and flows through `/api/costs` + `/api/status` unchanged. Attribution threads through the `handleMessage` choke point (chat **and** pipeline steps, via `overrideSlug`; an unresolvable/deleted book attributes to `'unattributed'` rather than a stranded orphan bucket) plus the executable-skill runner. UI: a lifetime line below the Rail daily spend, a per-book "Spend" row on the BookDrawer, and a **Settings → Danger Zone → Reset total spend** dialog (`ResetSpendModal`, mirrors `DeleteBooksModal`) that always resets the lifetime total and optionally selected per-book buckets, gated by typing `RESET MY TOTAL SPEND`; `POST /api/costs/reset-total`. TDD: new `tests/unit/costs.test.ts` (accumulation, survives rollover + budget `reset()`, `resetLifetime` selectivity, sub-cent 4dp preservation) + `api-test.sh` reset-total assertion. Fanned-out `/code-review` (high): 2 medium fixed (4dp rounding; orphan-bucket attribution), pre-existing aux-call under-counting flagged (see TODO). Known limitation: only spend recorded through `handleMessage` + executable-skill steps is counted — auxiliary AI calls (quality judge, direct-routed continuation passes, research/image/video) were never recorded (pre-existing; same as the daily/monthly figures).

- **Re-point the AI-generated-skill writer to the workspace overlay.** `POST /api/tools/ingest/save` (`heartbeat.routes.ts`) wrote generated `SKILL.md` into the baked built-in `skills/` dir (lost on Docker rebuild, bypassed the overlay model); now writes to `workspace/library/skills/` (the canonical user-skill overlay, matching `authoring.routes.ts`), so saved skills survive rebuilds and load as `source: workspace`. `api-test.sh` round-trip asserts it lands in the overlay.

- **Asset Studio book-scope header shows the kind string as the asset name.** In book scope the editor header rendered the kind (e.g. "genre") instead of the snapshotted asset's name. `AssetStudio` now derives a `displayName` from the active book's `pulledFrom.<kind>.name` (flat `author`/`voice`/`genre`/`pipeline` fields on the book summary; `pipeline` added to `BookSummary` in both the gateway and shared types) and passes it to the editors, which render `displayName ?? name`. Library scope and multi-entry kinds (sections/skills) unchanged.

## 2026-06-13

- **Multi-step executable skills (per-phase LLM + temperature + retries) — Phases A+B.** A skill becomes an N-phase chain (no cap; usually 2), each phase its own **OpenRouter** model + temperature + prompt, passing `{{previous}}` forward (canonical "Humanize" = Flash detects AI-tells → Pro rewrites). Runs as a **pipeline step** (last phase's output = the step output); per-failing-phase retries 0–4; **OpenRouter-forced**. Backward-compatible: no `steps.json` = passive skill (markdown injected as before). **A (engine):** `steps.json` sibling to `SKILL.md` + `parseSteps()` (validates non-empty model+prompt per phase, clamps retries 0–4) in `SkillLoader`; new `SkillRunner` (`gateway/src/services/skill-runner.ts`) chains phases with single-pass `{{input}}`/`{{previous}}`/`{{guidance}}` templating, records per-phase cost; `runExecutableSkillStep()` bridge wired into all three step-execution sites (`projects.routes` /execute + /auto-execute, `index.ts` goal-engine) with double-processing (continuation/quality/short-retry loops) guarded off for executable steps; `PUT /api/skills/:name` accepts `steps`+`retries`. **B (studio + transfer):** new `SkillEditor` (`frontend/studio/src/components/asset/`) — SKILL.md textarea + category + executable phases (model/temperature/prompt, add/remove/reorder, stable client keys) + retries; wired into `AssetStudio` for `kind==='skill'` (skills were previously read-only); `library-transfer.ts` skill export/import now round-trips `steps.json` (`LibraryEntryFull` carries `steps`/`retries`; validator allows the optional sibling, re-validated via `parseSteps`; finalize writes it through normalized). Brainstormed → specs/plans (`docs/superpowers/`) → TDD (skill-steps-loader, skill-runner, +4 library-transfer round-trip tests; 342/342 unit, `tsc` + `build:frontend` clean). Fanned-out review per phase: **0 medium+** (Phase B: 2 low fixes applied — stable React keys, floored retries; rest left per surgical-changes). Deployed to Mercury (build PASS, healthy); smokes green on Mercury: `tests/skill-steps-smoke.sh` 5/0/0 (real OpenRouter 2-phase chain) + new `tests/skill-transfer-smoke.sh` 5/0/0 (export→delete→import round-trip preserves steps) + perimeter smoke all pass. Supersedes the old "Per-skill LLM in a pipeline" idea.

- **Series feature — multi-book studio container (Phases A+B+C).** Books that share an author/voice/genre + world-building, the series-level analog of the per-book snapshot (North Star). **A:** book-centric `Series` container (`workspace/series/<id>/series.json`, fail-soft migration from the legacy flat file) with library asset refs + book membership; copy-on-create inheritance (`POST /api/books {series}` snapshots the series' author/voice/genre into the new book + records provenance); ref-divergence + confirmation-gated "pull series assets"; continuity report repointed from projects → member books. **B:** world-building artifact (`characters/places/lore.md`) owned by the series, snapshotted into each book's `templates/worldbuilding/` and injected into generation prompts via `buildSystemPrompt` (mirrors the genre guide). **C:** Series studio route (`/series` — list/create/edit, asset-ref pickers, world-building editor, member add/remove + reading order, report, gated pull, delete), New Book "Series" selector (locks author/voice/genre to the series), and the series name on the Book Board card byline (`BookSummary.series` from `pulledFrom.series.title`, truncated). API in `gateway/src/api/routes/series.routes.ts`; service `series-bible.ts`. Brainstormed → 3 specs + 3 plans (docs/superpowers/) → TDD per phase. Three fanned-out `/code-review`s: **0 medium+** across all phases (low robustness/spec items fixed). Verified: full unit suite green, `tsc` + `build:frontend` clean, extended-feature-smoke (Tier G) 107/0/0 + `tests/series-smoke.sh` 13/0/0 on Mercury. Deferred (optional, not a phase): world-building content-divergence detection. Supersedes the old project-based Series-Bible API.

- **File explorer for documents (+ book outputs).** New studio `/files` route ("Files" in the Rail) with two tabs: **Documents** (`workspace/documents/` — list / preview / download / **delete** / **upload**) and **Book outputs** (per-book `data/` — list / preview / download, read-only). Backend added two read endpoints (`GET /api/documents/:filename`, `GET /api/books/:slug/files/:filename`) plus a shared XSS-safe `serveFile` helper (`_shared.ts`): inert previewable text served `text/plain; charset=utf-8` inline, everything else forced to `application/octet-stream` + attachment, `X-Content-Type-Options: nosniff` always — so a user-supplied file is never served with an active MIME on-origin (allowlist in `file-preview.ts`, unit-tested). Markdown preview via `DOMPurify.sanitize(marked.parse(...))`; download via fetch+Blob so the bearer token stays in the Authorization header (out of URLs). Upload reuses the existing `/api/documents/upload`. Brainstormed → spec (`docs/superpowers/specs/2026-06-13-file-explorer-design.md`) → TDD helper → endpoints → frontend → fanned-out `/code-review` (0 medium+ after fixing an XSS-via-MIME HIGH and a token-in-URL HIGH surfaced by the security scanner). Verified: 307/307 unit, `tsc`+`build:frontend` green, extended-feature-smoke **98/0/0** (incl. the new Tier F), and a live upload→preview→download→delete round-trip on Mercury 2026-06-13.

- **Delete books from disk (UI).** Settings → "Danger zone" card with a `Delete books…` button opening a two-stage modal (`DeleteBooksModal`, `frontend/studio/src/components/`): multi-select books, then a typed-phrase gate (`DELETE MY BOOKS FROM DISK`, exact/trimmed) before the final delete enables. Loops the existing `DELETE /api/books/:slug` client-side (no backend change), refreshes the store, fault-tolerant (per-book success/fail count). For cleanup / after a finished book is pulled elsewhere. Brainstormed → spec (`docs/superpowers/specs/2026-06-13-delete-books-design.md`) → implemented; `npm run build:frontend` green; pushed + live-verified on Mercury 2026-06-13.

- **Book Board phase progression — books now update in real time on the Book Board (owner roadmap; was TODO #15).** Original report: "books don't update in real time on the Book Board." Reproduction (probe `tests/book-phase-probe.sh`, run against Mercury) confirmed **three** intertwined problems: (1) the book manifest `phase` was written `'planning'` once at creation and **never advanced** server-side (no `setPhase` existed), so the chip/bar/next-action were frozen; (2) the `live` strip updated via the 4s poll but the phase indicator wasn't derived from the live project; (3) `Board.tsx`/`BookDrawer.tsx` hardcoded **6** lifecycle phases and rendered "phase X of 6" regardless of the book's pipeline. **Owner decision: pipeline-driven phases (N segments).** Implemented per the approved plan (`docs/BOARD-PHASE-PROGRESSION-FIX.md`): **backend** — `pipelinePhases()` + `NOVEL_PIPELINE_PHASES` (`library-types.ts`); `BookService.phasesForBook()` + `setPhase()` (the missing post-create manifest writer); `BookManifest.phase` widened to `string`; `buildBookCards()` adds `phases` + a live current-phase from the bound active project's step **clamped to the book's segment list** (a `book-production` sub-phase like `'polish'`, absent from a novel-pipeline book's segments, keeps the chip on its containing segment so `card.phase` is always a member of `card.phases`); `ProjectEngine.onStepCompleted()` hook fired in `completeStep()`, wired in `init/phase-06-content.ts` to advance the bound book's phase to the frontier (`next?.phase ?? completedStep.phase`, same clamp); `phases` exposed on `GET /api/books` + `GET /api/books/:slug`. **Frontend** — shared `LIFECYCLE_PHASES` constant (also **closes the "Dedup the pipeline-phase order constant" cleanup**); `Board.tsx` renders N segments from `card.phases` (inline grid override of the CSS `repeat(6)`, PHASE_VAR colors for the novel vocabulary, `phase X of N`); `BookDrawer.tsx` timeline from the book's `phases`. **Probe** extended to report `phases` + count. **Tests (TDD):** +`pipeline-phases`, +`book-phases`, +`project-step-hook`, expanded `book-card` (clamp) — 305/305 unit pass, `tsc` clean, `npm run build:frontend` green. **Two fanned-out `/code-review`s (4 dimensions + adversarial verification each):** round 1 (backend) — 1 medium fixed (the phase-vocabulary divergence → the clamp), 1 low deferred (`setPhase` skips the `assertWritable` schema gate — unreachable on v1, recorded under "schemaVersion gate at the data-output path"); round 2 (frontend) — 0 medium+, fixed only the self-created stale "6-segment" probe comment. **Deployed to Mercury twice (backend, then frontend) — both healthy, no errors;** probe confirms the board phase walks `bible→production→revision` (was frozen at `planning`) and renders 4 segments for the 4-phase probe pipeline (not the legacy 6).

## 2026-06-12

- **Test coverage expansion — batch 1 (security primitives + new util + engine orchestration + smoke).** Closed the biggest gaps from the 2026-06-12 test review with **+78 new unit tests (211 → 289, all green)** and **4 new free smoke checks**, no source changes. New unit files: `confirmation-gate.test.ts` (17 — the universal approval gate had zero coverage: create→pending, pre-auth-claim refusal, `sanitizePayload` redaction, approve/reject state machine, `checkDecision` shapes, `recordOutcome`→terminal + replay-prevention, wall-clock expiry, list/get filters); `vault.test.ts` (8 — AES-256-GCM set/get round-trip, encryption-at-rest, wrong-key fail-closed, GCM tamper detection); `audit.test.ts` (7 — JSONL write, hash-chain links, the 2026-06-12 seed-from-last-line-on-init continuity, corrupt-line fail-soft); `sandbox.test.ts` (9 — in-workspace valid, `../`/absolute escape rejected, forbidden patterns, `sanitizeFilename`); `wordcount.test.ts` (10 — `countWords` + `appendContinuation` overlap-dedupe boundaries + `MAX_CONTINUATION_PASSES`); `async-handler.test.ts` (6 — rejection/throw → `next(err)`, normal resolve passes through); `research-egress.test.ts` (7 — the new SSRF guard rejects private/loopback/link-local IP literals incl. `169.254.169.254` even when allowlisted, via the public `isAllowed`); `project-engine-orchestration.test.ts` (14 — `createProjectResolved`/`startProject`/`completeStep` sequencing + progress, `onProjectCompleted` fires exactly once on the final step with fault isolation, `skipStep`/`pauseProject`). Smoke (`feature-smoke.sh`, free Tier-A): chat token-bridge integrity on `:3848` (asserts the token is injected, no `window.http` mangling — the exact bug that broke chat), `/api/status` CalVer `version` + numeric `breakingVersion` (+ `/api/health`), and the Socket.IO handshake auth gate (no-token rejected). **Notes for follow-on (tracked in TODO "Test coverage expansion"):** the AI *generation loop* (multi-pass continuation, `[AI provider failure]` guard, fallback-cost) lives in the route handlers, not the engine, so it still needs a route-layer fake-AI seam; a full chat WebSocket *message* round-trip needs `socket.io-client`; the website deploy/publish gate still has no smoke. Verified: `tsc` clean, 289/289 unit, `bash -n feature-smoke.sh` clean. A couple of source nuances surfaced and were asserted as-is (documented, not changed): `checkDecision` uses `'expired'` as the catch-all "don't proceed" status for unknown ids, and the completion-hook loop isolates async-rejecting hooks but a *synchronously*-throwing hook is swallowed by the outer catch (completion still succeeds).

- **Versioning → CalVer `V{yy.mm.dd}` + a breaking-change marker (owner roadmap #1, format part).** Changed the display version from the date-time `V5.MM.DD.HH.MM` scheme to CalVer `V{yy.mm.dd}` (e.g. `V26.06.12`): `scripts/deploy.sh` now stamps `BOOKCLAW_VERSION="V$(date +%y.%m.%d)"`, and `gateway/src/version.ts` `bootVersion()` produces the same shape as the local-dev fallback. Added `BREAKING_VERSION` (integer, starts at 1) to `version.ts` as the product's manually-bumped breaking-change marker — surfaced in `/api/status` and `/api/health` as `breakingVersion` — since a date stamp carries no compatibility signal. Verified: `tsc` clean, 211/211 unit tests, boot fallback renders `V26.06.12`, deploy stamp previews `V26.06.12`. **The enforcing boot gate** (compare `BREAKING_VERSION` against the persisted `workspace/.bookclaw/workspace.json` marker and refuse/quarantine on mismatch) is the remaining follow-on, tracked under "Owner roadmap — 2026-06-12 batch" in TODO.md. `package.json` semver (5.0.0) left unchanged (npm/provenance, orthogonal to the display version).

- **Removed the README "rapid changes / wait for v6" warning (owner roadmap #2).** Deleted the top-of-`README.md` notice telling users not to start a novel and to expect breaking changes between commits — the book-container roadmap (Phases 0–12) is complete and the product has a stable baseline.

- **Code review 2026-06-12 — findings addressed (surgical batch).** The full-codebase review (`docs/CODE-REVIEW-2026-06-12.md`) was resolved in a 5-agent fan-out across disjoint file sets; all CRITICAL/HIGH/MEDIUM findings plus the safe LOW items fixed in one pass. **Chat:** repaired the token-bridge placeholder/variable collision that broke the `:3848` app (+ regression test); bound `connect_error`, renamed the reserved `error` event to `chat_error`, disconnect-on-non-transient-rejection, reconnect notices + stale-reply guard; web conversation channel keyed per-socket (`webchat:${socket.id}`) so concurrent clients no longer share history. **Crash safety (CRITICAL):** process-level `unhandledRejection`/`uncaughtException` handlers + an `asyncHandler()` wrapper applied to the unguarded async handlers in `ops`/`website`/`core` routes (a rejected promise no longer kills the gateway). **AI router/engine:** fallback completions now record cost+activity (the budget gate was silently defeated on failover — CRITICAL); the Telegram execution loop gained the `[AI provider failure]` guard (no longer writes error strings into chapters — CRITICAL); `auto-execute` got a per-project in-flight lock; the absolute provider fallback is now budget-aware/fail-closed (matches `getFallbackProvider`); pinned per-step model preserved on the short-response retry; novel-pipeline premise steps moved off the 4096 `general` budget to `book_bible`; shared `wordcount.ts` (countWords + overlap-de-dupe `appendContinuation` + unified `MAX_CONTINUATION_PASSES`) across both loops; prompt cache keyed by system-prompt hash; stale `costs.ts` rate table fixed. **Security:** website deploy/publish now routed through `ConfirmationGateService` (was an ungated Wave-3 `exec` of netlify/vercel/gh-pages/rsync) with a gated finalize; a shared `requireApprovedConfirmation()` preamble + `recordOutcome` on every finalize (books/library/backups/website) closes the approved-state replay window; `.env` vault key chmod 0600; tightened the over-broad `you are now` injection pattern + documented the detector as advisory; private/loopback IP-literal block in `research.ts` + consistent `sanitizedUrl` in `video-research.ts`; audit hash chain seeded from the last log line on init. **Book-container:** schemaVersion gate enforced on `writeTemplate`/`repull` (data-path deferred to the first schema bump); re-pull no longer silently deletes a library-removed file; atomic slug claim fixes the create TOCTOU; per-book restore of the active book returns `restartRecommended:true`; `FILE_SKIP` anchored to `memory/*.sqlite`. **API routes:** removed the dead duplicate `GET /api/audio/voices`, fixed the dead `services.projects` branch (use `getProjectEngine`), retired the legacy `/api/backup/*` block (superseded by Phase-11 `/api/backups`, no callers), added the missing `SLUG_RE` guard on book export. **Frontend:** `api()` errors now carry `.status` (callers branch on it, not substring matching); Board polls `loadBooks()` while any book is live; backup cloud-config PATCH sends the full `cloud` object; empty states gated on `booksLoaded`; `marked.parse(...,{async:false})`; stable `PipelineEditor` step ids; reject-prompt cancel now aborts. Verified: `tsc` clean, **211/211** unit tests (added `chat-token-bridge` + updated the `selectProvider` budget test for the new fail-closed contract), `npm run build:frontend` green. Genuinely-large/deferred items (config-not-code pipelines, phases-as-data, `WIRED_KINDS`, god-class refactor, TLS CSP, custom dialogs, chat markdown, audit verify routine) tracked under "Deferred from code review 2026-06-12" in `docs/TODO.md`.

- **Book-container Phase 12 — library element share/import (post-release).** Export an individual **library entry** (author / voice / genre / pipeline / section / skill) as a portable `.zip` and import one into the **workspace library overlay** — the library-level analog of Phase 5's whole-book share/import. Format: one zip per entry (`library-entry.json` manifest `{formatVersion:1, kind, name, description?, category?}` + `files/<…>`), uniform across kinds for a single import path/validator; `formatVersion` fail-closed. New `LibraryTransferService` (`gateway/src/services/library-transfer.ts`): `export(kind,name)` reads the **resolved** entry (built-ins exportable; synthetic skills refused — no shareable frontmatter); `validateAndStage(zip)` extracts to `workspace/.import-staging/` with the shared guards + per-kind structural validation + injection/HTML scan; `finalizeImport(stagingId)` writes file-backed kinds via `LibraryService.writeEntry` (create-or-override by name, overlay shadows built-in) and skills to `workspace/library/skills/<category>/<name>/SKILL.md` + `SkillLoader.reload()`, one-shot (staging consumed). Phase 5's generic zip-security (zip-slip/symlink/charset guards + injection/HTML scan) was **extracted into a shared `gateway/src/services/transfer-security.ts`** that both transfer services use with zero behavior change (the Phase 11 review's altitude prediction). API on the existing `library.routes.ts`: `GET /api/library/:kind/:name/export`, `POST /api/library/import` (clean → finalize immediately 200; findings → ConfirmationGate 202; structural error → 400), `POST /api/library/import/finalize`; Asset Studio (`/library`) gained per-entry Export links + an "Import entry…" flow with a persisted (localStorage) Finalize affordance that survives navigating to Confirmations. **Verified:** 208 unit tests (`tests/unit/library-transfer.test.ts` + the unchanged book-transfer regression suite), `tsc` clean, frontend builds; Mercury deploy PASS (`50fdeaa`); **live feature-smoke 84/0/0** incl. the new 7-check Phase-12 section (export → import → `source:workspace` → overlay-delete reverts; HTML-payload import gated 202 + rejected; garbage zip → 400). **Post-implementation `/code-review` (high, 7 finders) fixed 12:** zip-bomb/disk cap (`checkZipBudget`, both services, pre-extraction), widened HTML denylist (`base|form|link|meta`), exported `SKILL_CATEGORIES` from the loader (dropped the duplicated copy), synthetic-skill export refusal, 503 unavailable-guard, gated-import payload minimized so a crafted entry name/finding can't trip the pre-auth defense, invalid export name → 400, staging sweep now protects `approved` (not just `pending`) confirmations + a 15-min race guard, frontend Finalize persisted across navigation + cleared on expiry, `ImportFinding` type corrected. Skipped as designed/out-of-scope (in the spec): clean-import auto-finalize/overlay-shadow (spec-sanctioned; normal round-trips export the full file set), reworded-injection scan evasion (pattern-scanner limit; DOMPurify + gate-on-findings are the layered defenses), the now-thrice-hand-rolled confirmation-finalize preamble (broader shared-helper refactor deferred), two pre-existing book-transfer patterns moved verbatim. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-phase12-library-share-import*`. **This was the final planned book-container phase.**

- **Book-container Phase 11 — backup & recovery (THE RELEASE GATE).** Point-in-time recovery shipped: `BackupService` (`gateway/src/services/backup.ts`) writes default-ON **uncompressed mirror snapshots** to a root OUTSIDE the workspace (`BOOKCLAW_BACKUP_DIR` ?? `backup.localPath`, default `~/bookclaw-backups`; refused if inside the workspace), named `YYYY-MM-DDTHH-mm-ss` (tmp-then-rename so a crashed copy never looks valid; `SNAPSHOT_RE` is the hard safety boundary — prune/list/restore never touch non-matching paths), pruned to keep-last-N (default 10). **Scope `standard`** = books/library/.config/soul/memory/documents/projects/.bookclaw (non-regenerable data; soul+memory approved by owner, documents/projects/.bookclaw added at code review under the same rationale); **`full`** = whole workspace incl. `.vault/vault.enc` as an offsite copy (key lives outside the workspace, never archived); live DB files (`*.sqlite*`, `*.db`, `-wal`/`-shm`) excluded everywhere. **Restore** (whole-workspace or per-book) always pre-snapshots first — widened to `full` scope when restoring a full snapshot — never deletes/overwrites `.vault`/`.audit`, re-initializes `BookService` (made reset-safe: stale active/channel pointers cleared) and reloads the composed soul; whole-workspace responds `restartRecommended:true`; a restored too-old manifest classifies `quarantined` via the existing version gate. **Cloud push** (opt-in): snapshot zipped (AdmZip) → directory-drop and/or `rclone:<remote>` and/or post-backup hook (fixed-argv spawn, no shell), each layer fail-soft with failures surfaced on `lastRun.error`; adding a destination/hook is **confirmation-gated** (pending cloud config held server-side keyed by confirmation id — one-shot, replay-proof, immune to payload sanitization); **BookClaw never deletes remote data**. **Triggers:** in-process interval (default 24h, overflow-clamped, staleness-guarded), on-project-completion (10-min guard), manual; `PUT /api/backups/config` re-arms the service live (enable/disable/interval take effect without restart) and disabling logs the loud `⚠` posture warning. **API:** `GET/POST /api/backups`, `POST /api/backups/:id/restore` `{book?}`, `GET/PUT /api/backups/config` + `POST .../config/confirm/:id`. **UI:** studio Settings "Backups" card (disabled-warning banner, status, keep/interval/scope controls, Back-up-now, per-snapshot restore picker with whole/book choice, cloud destinations with pending-approval flow). **Docker:** second host bind-mount `${BOOKCLAW_BACKUP_PATH:-/home/paul/bookclaw-backups}:/app/backups`, deploy.sh mkdir+chown (non-fatal for the backup dir). **Verified:** 21 backup unit tests (185 suite total, green), `tsc` clean, frontend builds; release-gate criteria 1/2/3/5 unit-tested, 4 (disabled warning) boot-checked locally; Mercury deploy PASS; **live feature-smoke 77/0/0** incl. the new 8-check backup section (snapshot create/list, keep round-trip, per-book restore round-trip against the real bind-mount, 404 on unknown id, gate 202 + listed + clean reject, config restore). **Post-implementation `/code-review` (high, 7 finder angles) fixed 12:** runtime config toggle now re-arms timers/hooks (was boot-frozen), gated cloud config made one-shot/clobber-proof/sanitization-proof, pre-restore scope widening, cloud-failure surfacing, memory-search.db exclusion, standard-scope gaps, BookService re-init reset, soul reload after restore, async fs on big tree ops, single-source config defaults (+ dead `format` keys dropped), snapshot meta carries its books list, deploy.sh backup steps made non-fatal. Skipped as designed/out-of-scope: vault-in-full-cloud (deliberate, key separate), whole-restore quiesce (restartRecommended is advisory — known limitation), hardlink dedup (explicit non-goal). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-phase11-backup-recovery*`.

## 2026-06-11

- **Book-container Phase 10 — per-channel active book.** Each channel (Telegram chat, web, Discord) can now select its own active book independently, extending the existing conversation-history-by-channel isolation to book selection. `BookService` gained four channel-override methods: `getChannelBook(channel)` returns the override slug or `undefined`; `resolveBook(channel)` returns the override if present, otherwise the global active-book pointer; `setChannelBook(channel, slug)` persists the override; `clearChannelBook(channel)` removes it. Overrides are persisted to `.config/channel-books.json` (fail-soft load on init; stale slugs pointing to deleted books are pruned on load, and `BookService.delete()` drops overrides for the removed book in the same call). `handleMessage` received a one-line change (`overrideSlug = bookSlug ?? getChannelBook(channel)`) that routes the override through the existing Phase-8 `composeForBook` path — no change to web/default-channel behavior. Telegram gained a `/book` command (no argument lists available books with a `*` marker on the chat's current selection; with a slug argument selects that book for the chat), `/status` now includes the resolved book title, and project-creation commands (`/novel`, `/write`, `/project`) bind to the channel's resolved book rather than the global pointer. Web studio continues to follow the global active-book pointer (unchanged). API and Discord per-channel selection are out of scope. **Verified:** `tests/unit/channel-books.test.ts` (override isolation per channel, persist/load round-trip, stale-slug prune on init — 162 unit total, green); `tsc` clean; `npm run build:frontend` green; Telegram leg covered by the unit resolver tests plus a documented manual `/book` check (no HTTP hook to script it). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-phase10-per-channel-active-book*`.

- **Book-container Phase 9 — book-board UI.** The studio Book Board became the face of the studio: each card now shows the phase, status, suggested **next action**, a **6-segment phase progress bar**, and a **live "writing · <step>" strip** with a pulsing dot when a project bound to that book is running; the rail's Generating/Idle counts went live too. Sourced by enriching `GET /api/books` with `next` (existing `nextStep`) + `live` (active projects grouped by the Phase-8 `bookSlug`) in one call, via a pure, unit-tested `buildBookCards()` helper (`gateway/src/services/book-card.ts`) wired into `books.routes.ts`; frontend `BookSummary` gained optional `next?`/`live?`. Drill-in drawer unchanged. **Verified:** new `tests/unit/{format,book-card}.test.ts` (156 unit total, green); `tsc` clean; `npm run build:frontend` green; Mercury deploy PASS; **live feature-smoke 69/0/0** incl. `Phase 9: /api/books rows carry next-action`; live `/api/books` confirmed returning `next`+`live`. **Post-implementation `/code-review` (high, 4 finder angles)** fixed 3 issues: the progress caption was off-by-one (showed `5/6` for the final phase → now "phase N of 6"), the **Chat app** `Stats` daily-spend still read 2-decimals (→ `money()`), and the Insights mini-activity feed still used `HH:MM` (→ `HH:MM:SS`); the `nextStep`-per-book sync-I/O cost was a spec-accepted tradeoff and a `PHASE_ORDER`/`PHASES` dedup was deferred (see TODO). Mockup: `dashboard/concept/phase9-book-board.html`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-phase9-book-board-ui*`.
  - **Activity monitor: show seconds in timestamps.** Activity (and Insights mini-feed) timestamps now render `HH:MM:SS` via a new shared `hhmmss()` helper, so rapid back-to-back events during a run are distinguishable.
  - **Activity monitor: single-space the rows.** Tightened the activity feed `.ev` row padding/gap so more events are visible without scrolling.
  - **Costs: show fractional cents to 4 decimals (`$0.0001`).** A new shared `money()` helper renders spend amounts at 4 decimals across the studio (Activity, Insights, Confirmations, Rail) and the Chat app (Stats), so cheap-model spend no longer reads as `$0.00`; budget **limits** stay whole-dollar (`$5.00`).
  - **Chat: two-sided layout, single-spacing, AI text shade.** Both chat surfaces — the standalone Chat app (`frontend/chat/ChatPane.tsx` + `App.module.css`) and the studio Write thread (`frontend/studio/components/write/ChatThread.tsx` + `Write.module.css`) — now read like a conversation: message text is single-spaced (line-height `1.62`/`1.65` → `1.35`), the author's (human) messages mirror to the opposite side from the AI's via a `row-reverse` role-modifier (`.msgMe`) with right-aligned body, and AI replies are tinted a shade off the author's parchment (`--text` #efe7d4 → `--dim` #b4a98f) as an extra human-vs-AI cue. CSS + role-class only; `tsc` clean, 156 unit green, both frontends build.

## 2026-06-10

- **Book-container Phase 8 — multi-book concurrency.** Several books can now be in flight at once without their generation cross-leaking. Replaced the single global active-book pointer as the *generation driver* with an immutable per-project book binding: `Project.bookSlug` is captured at creation from the then-active book (lifted from `context` in `createProject`/`createProjectFromPipeline`/`createPipeline`, stamped on the code-generated novel/book-production paths). `BookService` gained stateless slug-parameterised accessors (`authorDirOf`/`voiceDirOf`/`dataDirOf`/`genreGuideOf`/`pipelineOf`); the old `active…()` methods are now thin wrappers over them. `SoulService.composeForBook()` composes a book's Author/Voice **without** mutating the singleton (so a bound project's prompt never disturbs chat's global soul). `handleMessage` gained an optional `bookSlug` (8th arg): when set, soul + genre are composed for that book; when absent, chat is unchanged (per-channel chat selection stays Phase 10). Every project file read/write routes via the `dataDirOf(project.bookSlug) ?? activeDataDir() ?? legacy` ladder (`index.ts`, `projects.routes.ts`, `documents.routes.ts`, `export`/`wave` gather resolver). The feature-smoke Tier-D block was upgraded from a sequential precursor to the **decisive concurrency proof**: bind a project to Book A, flip the global active book to B, execute, and assert A still receives the output (pre-Phase-8 that flip leaked A's output into B). **Verified:** `tests/unit/{book-slug-accessors,soul-compose,project-bookslug}.test.ts` + extended genre-guide tests (148 unit, green); `npx tsc --noEmit` clean; Mercury deploy PASS; **live feature-smoke 68/0/0** incl. `Tier D: output isolated :: binding beat the global pointer`. **Post-implementation `/code-review` (high, 7 finder angles) — 2 real correctness bugs fixed** that the smoke couldn't see: (1) the dashboard/API execute + auto-execute paths (`projects.routes.ts`, 6 `handleMessage` calls) never passed `bookSlug`, so a project bound to A run while B was active composed **B's** soul/genre into A's prompts; (2) `createProjectFromPipeline` dropped `bookSlug` on the dynamic/novel-pipeline branch. Plus a defensive `composeForBook` try/catch (a mid-run FS error degrades to the global Author instead of crashing the step), a redundant-stat cleanup, and a comment pinning the deliberate soul/genre fallback asymmetry (genre must NOT fall back to the global book — that would be the cross-leak Phase 8 prevents). Three low-severity altitude follow-ups (ladder-helper consolidation, binding centralization, smoke positive-assertion) deferred to Phase 9/10 — see TODO. Chat stays on the global pointer; per-channel active book is Phase 10; book-board UI is Phase 9. Spec: `docs/superpowers/specs/2026-06-10-phase8-multi-book-concurrency-design.md`; plan: `docs/superpowers/plans/2026-06-10-phase8-multi-book-concurrency.md`.

## 2026-06-09

- **Book-container Phase 7 — genre wiring.** The active book's genre guide is now injected into generation prompts alongside Author + Voice. New `BookService.getActiveGenreGuide()` composes `templates/genre/*.md` in canonical order (reader-expectations → tropes → themes → beats → must-haves → genre-killers → comps) under `## Genre Guide — <Title>` headers, threaded through the single `buildSystemPrompt` chokepoint (`gateway/src/index.ts`) — which every path (web chat, `/api/chat`, Telegram, Discord, and every pipeline/`goal-engine` step) funnels through, so the guide reaches interactive chat *and* automated book production. Genre-guide content schema expanded (research-backed): added `themes.md`, `must-haves.md`, `genre-killers.md`; expanded `reader-expectations.md` (tone/pacing/setting/archetypes/length) and `beats.md` (named obligatory scenes); `romantasy` fleshed out as the worked example; `docs/GENRE-GUIDE-TEMPLATE.md` documents the 7-file schema. Verified: `tests/unit/genre-guide.test.ts` (compose order / genre-less→null / fresh-read) + a deterministic sentinel-echo assertion in `tests/feature-smoke.sh`. Spec: `docs/superpowers/specs/2026-06-08-phase7-genre-wiring-design.md`; plan: `docs/superpowers/plans/2026-06-08-phase7-genre-wiring.md`. Broad genre-library content remains out of scope.

## 2026-06-08

- **Date-time product version (`V5.MM.DD.HH.MM`).** `scripts/deploy.sh` stamps `BOOKCLAW_VERSION="V5.$(date +%m.%d.%H.%M)"` into `docker/.env` at build time → passed via `docker-compose.yml` → read by `gateway/src/version.ts` (`DISPLAY_VERSION`, with a boot-time fallback for local dev). Shown in the startup banner, `/api/status` (+ `/api/health`, fixing the stale `4.0.0`), and the UI footer. `package.json` stays semver `5.0.0` (npm/provenance). So every Mercury push shows a fresh version; a restart keeps the build's version. **Verified live on Mercury (`/api/status` → `V5.06.09.01.20`).**

- **Book-container Phase 6 — front-end / UI rewrite (COMPLETE).** Replaced the single vanilla-JS dashboard with a **v6 React + Vite studio** (npm workspaces `frontend/{shared,studio,chat}`, "Atelier" design system, a shared api/socket/store seam with a `useActiveBook()` indirection so Phase 8 multi-book concurrency can drop the global active-book pointer without touching components). Shipped in sub-phases, each: plan → subagent build → high-effort `/code-review` + fixes → deploy to Mercury → `feature-smoke`. **6a** foundation (studio shell + Book Board). **6b** v6 made the DEFAULT UI + live-tailing **Activity** SSE feed + Rail spend/badge. **6c** full **Book Board** (byline backend, filters, ghost New-Book) + detail **drawer** (assets + phase timeline + Set-active). **6e** per-asset **descriptions** (overlay→builtin `meta.json` sidecars, snapshotted) + **suggested-next-step** endpoints (`/api/books/active/next`, `/:slug/next`). **6f** **Asset Studio** (`/library`): two-scope editor (Library overlay vs active-book snapshot) + multi-file prose/marked-preview + pipeline step editor + entry CRUD + re-pull; security: DOMPurify-sanitised preview + import-scan HTML/event-handler gating. **6g** full **New-Book picker** (`/new-book`). **6d** **Write workspace** (`/write`, `/write/:slug`): outline · Socket.IO chat · pipeline rail + generation controls + per-step model override. **6h** **Insights** (`/insights`) + **Settings** (`/settings`, API-key vault — values write-only) + **Confirmations** (`/confirmations` approval queue). **6i** standalone **Chat app** (`frontend/chat`) on a 2nd port (`BOOKCLAW_CHAT_PORT`=3848), cross-origin; `gateway/src/init/phase-12-chat-http.ts` is a 2nd http.Server serving only the chat dist with per-request token+API-base injection + tight chat CSP, validated Host (forged Host → localhost fallback), reused IP-allowlist, and minimally-widened CORS (no wildcard). **Close-out (2026-06-08):** human browser click-through (owner-verified); **legacy `dashboard/` retired** — `dashboard/{src,dist}` + `build.mjs` deleted, the `BOOKCLAW_UI` switch removed (studio is now the only UI served), `build:dashboard` scripts + Dockerfile dashboard build/copy removed; only `dashboard/concept/` remains as the design source-of-truth. Plans under `docs/superpowers/plans/2026-06-0{7,8}-phase6*.md`; spec `docs/superpowers/specs/2026-06-07-phase6-frontend-rewrite-design.md`. Deferred (logged as a Quick-cleanup): the cosmetic book-scope Asset Studio header showing the kind string instead of the asset name.

- **`GET /api/books/:slug/files` — list a book's `data/` outputs without a bound project.** New `BookService.listFiles(slug)` (top-level real files only — dotfiles/subdirs skipped, newest-first, `{name,bytes,modified}`; `null` for an invalid/missing slug) + the route in `books.routes.ts` (SLUG_RE-guarded, 404 on null). Lets the Write OutlinePane and the Chat "what you've made" list show a book's prior outputs even with no project running (Phase 8 will bind projects to books). Unit test `tests/unit/book-files.test.ts` (empty / listing-order / invalid-slug) + a `feature-smoke.sh` assertion. Phase 6 backend follow-up logged during 6d.

## 2026-06-07

- **Book-container Phase 5 — share / import security.** Export a book as a single portable `.zip` (whitelist `book.json` + `templates/` + `data/`; never `.baseline/` or the vault) and import one back safely via the new `BookTransferService` (`gateway/src/services/book-transfer.ts`). Import extracts an untrusted zip to an isolated `workspace/.import-staging/` dir with strict per-entry guards (absolute path / `..` traversal / zip-slip / symlink-mode / off-whitelist all hard-rejected, staging purged), validates `book.json`, classifies the schema version, and runs `InjectionDetector` over every prompt-bearing file (author/voice/genre/sections `.md`, pipeline step prompts, each `SKILL.md`) plus `data/` text. **Structural violations → 400; a clean + version-compatible book lands directly; any injection finding or an incompatible version routes through the `ConfirmationGate`** (24h, no auto-approve) — `POST /api/books/import/finalize` consumes only an *approved* `book-transfer` confirmation, so nothing flagged lands without explicit dashboard approval. Imported books get a fresh `uniqueSlug` + re-seeded `.baseline/`; orphan staging dirs are swept on boot (keeping pending-confirmation ones). Routes: `GET /api/books/:slug/export`, `POST /api/books/import`, `POST /api/books/import/finalize`; `uploadZip` multer (200MB, .zip); dashboard Export (per row) + Import (file picker) + gated-finalize in the books panel. **Tests:** `tests/unit/book-transfer.test.ts` (export whitelist, zip-slip/symlink/whitelist rejection, injection-in-any-file, version flag, finalize round-trip + baseline reseed, staging sweep — 105 unit tests total green); `tests/feature-smoke.sh` export→import round-trip + gated malicious-skill assertions. Spec: `docs/superpowers/specs/2026-06-07-phase5-share-import-security-design.md`; plan: `docs/superpowers/plans/2026-06-07-phase5-share-import-security.md`. **Pending: Mercury deploy + safety-net run.**

- **Book-container Phase 4 — two-scope editor + re-pull from library.** The dashboard editor now edits either a shared **library** template (workspace-overlay CRUD: edit/create/delete; built-ins read-only, deleting an overlay reverts to its built-in) or the **active book's snapshot**, across author/voice/genre/sections/skills/pipeline (`dashboard/src/panels/authoring.js`; library write API `POST/PUT/DELETE` in `gateway/src/api/routes/library.routes.ts`; book-snapshot edit `GET/PUT /api/books/active/templates/:kind/:name?` in `books.routes.ts`). Per-book **re-pull from library** does a true 3-way merge against a pristine `.baseline/` mirror captured at book create time (`gateway/src/services/merge.ts` via `node-diff3`; `BookService.repullStatus()`/`repull()` + `.baseline/` capture in `book.ts`): non-conflicting changes auto-merge, collisions are written with git-style `<<<<<<< book` / `>>>>>>> library` markers to resolve in the editor; pipeline JSON is whole-asset take/keep; pre-Phase-4 books with no baseline fall back to keep-mine/take-library (and gain a baseline). Re-pull UI + status badges in `dashboard/src/panels/books.js` (`GET /api/books/active/repull`, `POST .../repull/:kind/:name`). Hardened: explicit kind/name guards on the repull route; `/api/authoring/reload` now also reloads the library. **Tests:** new unit suites `tests/unit/{merge,book-baseline,library-write,book-repull}.test.ts` (86 unit tests total, green); `tests/feature-smoke.sh` + `tests/openrouter-pipeline.sh` gained Phase 4 e2e assertions. Spec: `docs/superpowers/specs/2026-06-07-phase4-editor-repull-design.md`; plan: `docs/superpowers/plans/2026-06-07-phase4-editor-repull.md`. **Post-implementation `/code-review` (10 findings) all corrected:** re-pull now updates `book.json` `pulledFrom` provenance; `assetsOf` guards a malformed manifest; the pipeline/no-baseline re-pull requires an explicit resolution; `WIRED_KINDS`/`MD_FILE_RE`/`parsePipelineJson` deduped into `book-types.ts`; book-snapshot read/write moved into `BookService.readTemplate()`/`writeTemplate()`; the template API uses a single canonical **singular** kind vocabulary (dropping the dashboard `toTemplatePath` mapping); single-snapshot kinds reject a `:name`. 95 unit tests green. **Pending: Mercury deploy + safety-net run (maintainer).**
- **Phase 4 prep (surfaced by the Author/Voice split, 2026-06-07).** (1) `SoulService.updateVoiceProfile()` writing `VOICE-PROFILE.md` to the author dir instead of the voice dir, and (2) the prompts editor not reflecting edits once a book is active — both resolved by the Phase 4 editor re-point: the two-scope editor edits the active book's `templates/{author,voice}` snapshot (and the library) directly, and `updateVoiceProfile()` already targets the voice dir (loose-ends fix).
- **Author/Voice split — Voice is a first-class library kind.** `voice` is now the fifth file-backed library kind (`gateway/src/services/library-types.ts` `LIBRARY_KINDS`, `gateway/src/services/library.ts` `FILE_KINDS` + `DIR_LAYOUT` → `voices`), so a book can mix-and-match an author identity with a separately-chosen voice. Built-in content was restructured (no migration — decision 6): `library/authors/default/` keeps `SOUL.md` + `PERSONALITY.md`; `STYLE-GUIDE.md` + `VOICE-PROFILE.md` moved to `library/voices/default/`. `BookService.create()` snapshots the chosen voice into `templates/voice/` and `SoulService` composes author identity from the author dir and style from the voice dir (falling back to the author dir when a book has no separate voice snapshot). Dashboard New Book form gained a voice select. **Tests:** `tests/unit/library-voice.test.ts` (voice list/get) + `soul-usebook.test.ts` two-dir composition cases.
- **Per-book skills snapshot (frozen record).** `BookService.create()` now snapshots every pipeline-referenced `SKILL.md` into the book's `templates/skills/`, recording the source under `book.json` `pulledFrom.skills` (alongside `pulledFrom.voice`), so a book carries a frozen copy of the skills its pipeline used at create time. (`gateway/src/services/book-types.ts`, `book.ts`.)
- **Book DELETE endpoint + dashboard delete button.** `DELETE /api/books/:slug` removes a book container and re-resolves the active-book pointer (deleting the active book re-activates the newest remaining book, or re-seeds a default if none remain). Dashboard Books panel gained a per-book delete button. (`gateway/src/api/routes/books.routes.ts`, `dashboard/src/panels/books.js`.) `tests/feature-smoke.sh` gained a voice assertion and DELETE-book teardown.

## 2026-06-06

- **Phase 3 code-review #7 — `GET /api/projects` consistent contract.** The endpoint returned two different shapes depending on active-book state: flat per-step `.md` filenames from the active book's `data/` dir vs project directory slugs from `workspace/projects/`. No consumer used the bare endpoint (the dashboard/bridges only call `/api/projects/list`, `/api/projects/:id/...`, `/api/projects/templates`). Rewrote `gateway/src/api/routes/core.routes.ts` `GET /api/projects` to mirror `/api/projects/list` — returns `{ projects: engine.listProjects() }` (the engine's known project objects, ids/titles/status) regardless of active book, with a `503` when the engine isn't initialized. Dropped the filesystem-listing branches.

- **Phase 3 code-review #8 — un-validated `pipeline.json` hangs the create handler.** `POST /api/projects/create` called `engine.createProjectFromPipeline(activePipeline, …)` on the un-validated parsed `activePipeline()` JSON with no try/catch; a corrupted `pipeline.json` (steps missing / not an array) made `pipeline.steps.map(...)` throw a raw `TypeError`, which became an unhandled rejection in the async Express handler → no response, request hangs. Fixed defense-in-depth on both layers. **Service guard** (`gateway/src/services/projects.ts` ~line 613): `createProjectFromPipeline` now throws `Error('Invalid pipeline: steps[] missing or not an array')` up front when `!pipeline || (!pipeline.dynamic && pipeline.name !== 'novel-pipeline' && !Array.isArray(pipeline.steps))` (the dynamic / novel-pipeline branch never touches `steps`). **Handler** (`gateway/src/api/routes/projects.routes.ts` ~line 93–155): wrapped the whole create body in try/catch returning `res.status(400).json({ error: 'Failed to create project: …' })`. The `index.ts` Telegram/AI-tool twin (`createProjectFromPipeline`/`createProjectResolved`) is already safe — all callers (`bridges/telegram.ts`, `index.ts:1108/1118`) wrap the handler in try/catch and surface the error, so a throw is caught/logged, not a process crash. **Test:** `tests/unit/pipeline-from-json.test.ts` — non-dynamic pipeline with missing / non-array `steps` throws the validation error (not a raw `TypeError`).

- **Phase 3 code-review #10 — `seedDefaultBook` "activate newest" nondeterministic tiebreak.** `book.ts` `list()` (~line 158) sorted `createdAt` with no tiebreak, so two books created in the same millisecond had undefined relative order, making `seedDefaultBook`'s `list()[0]` flaky. Added a stable secondary sort by slug: newest-first by `createdAt`, then `slug.localeCompare` ascending on ties. **Test:** `tests/unit/active-book.test.ts` — two books with identical `createdAt` → `list()` order is deterministic (slug ascending).

- **Phase 3 code-review #9 — active-book pointer in backup/restore.** `workspace/.config/active-book.json` was not captured by `media.routes.ts` backup/restore (the lists named `books`/`projects`/`personas`/`memory`/`config/user.json`/`vault.enc` only; `.config` was excluded entirely), so a restore could re-activate the wrong book. Added `workspace/.config/active-book.json` (as `active-book.json` in the archive) to all three lists — backup `sources`, pre-restore safety `currentSources`, and the `restoreMap` — consistent with how the other single-file entries are handled (file loop creates the parent dir).

- **brand label — dynamic sidebar version.** `dashboard/src/index.html` sidebar `brand-ver` was a static `v4.0.0`. Gave it `id="brandVer"` and wired `main.js` `loadStatus()` to set it from `/api/status` `data.version` (same place `#appVersion` is set), falling back to the static text if absent. Cache-busting `<meta name="dashboard-version">` left untouched. Rebuilt `dashboard/dist/index.html` (`brandVer` present, `__BOOKCLAW_AUTH_TOKEN__` placeholder intact).

## 2026-06-05

- **In-dashboard editor for prompts + skills (workspace overlay, hot-reload, templates)** — author prompts (`workspace/soul/*.md`) and skills (`skills/**/SKILL.md`, markdown + frontmatter) are now editable in the dashboard with no code change or redeploy, on the **built-in + workspace-overlay** model: built-ins stay baked into the image and read-only; user edits live under `workspace/skills/**` and override built-ins by name (override-then-delete reverts to the built-in); `workspace/soul/*.md` already persists. **Stage A** — `SkillLoader` gained the workspace overlay + a `source` field + `reload()`, `SoulService` gained `reload()`. **Stage B** — `gateway/src/api/routes/authoring.routes.ts`: `GET/PUT /api/prompts[/:file]`, `GET /api/skills`, `GET/PUT/DELETE /api/skills/:name`, `POST /api/authoring/reload`; path-whitelisted via `safePath`, frontmatter-validated, hot-reload; covered by `tests/api/api-test.sh` + a live e2e. **Stage C** — `dashboard/src/panels/authoring.js` + an "Authoring" nav panel: browse prompts + skills (grouped, source-badged), `marked` markdown preview + raw edit, Save (live reload), Reload-from-disk, Delete (workspace only), and "Save as new" / "+ New Skill" (clone a built-in seed as a template). Deployed to Mercury in `2260fbe` and **accepted via click-through on 2026-06-05** (create/edit/reload a skill + edit a prompt). **Seeds the per-book editor:** under [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md) this global editor will be re-pointed at the **library** + the **active book's snapshot** (edit-library vs edit-book-copy, plus opt-in re-pull).

- **Book-container Phase 0 — workspace host bind-mount + schema marker** (first phase of [BOOK-CONTAINER-ARCHITECTURE.md](BOOK-CONTAINER-ARCHITECTURE.md)). `docker/docker-compose.yml`: `/app/workspace` is now a host bind-mount (`${BOOKCLAW_WORKSPACE_PATH:-/home/paul/bookclaw-workspace}`), dropping the `bookclaw-workspace` named volume so the working data is directly backup-able/shareable on the host; the `bookclaw-vault` volume stays separate. `gateway/src/init/phase-01-config.ts` stamps `workspace/.bookclaw/workspace.json` (`WORKSPACE_SCHEMA_VERSION=1`, `createdByApp` from package.json) on first boot, logs the version on later boots, fail-soft — no compatibility gate yet (a later phase). `scripts/deploy.sh` writes `BOOKCLAW_WORKSPACE_PATH` to `docker/.env`, `mkdir -p`s the host dir, and — because Docker does **not** chown a host bind-mount and the app runs as the baked `bookclaw` uid 999 — aligns ownership after the build via a one-off root container (`docker compose run --user 0 --entrypoint chown bookclaw -R bookclaw:bookclaw /app/workspace`); without this a fresh host dir crashes the app on first write. `.env.example` documents the var; smoke-test Phase 1 asserts the marker. No data migration — the existing workspace was disposable (owner decision). Verified: `tsc` clean, unit 34/34, marker stamp + idempotent read via alt-port boots; **deployed to Mercury 2026-06-05** — container recreated on the bind-mount (old one shut down), `Up (healthy)`, `healthz=200`, `/api/status` `401` (auth enforced, vault decrypted = API keys preserved), marker present on the host. The old `docker_bookclaw-workspace` named volume is now orphaned (disposable). Committed `096c81d`.

## 2026-06-02

- **API-level rate limiting** (security review; the REST `/api/*` gap that the inherited per-channel chat limiter doesn't cover). Implemented the previously-settled design (auth-aware + trusted-IP-exempt) as a hand-rolled per-IP fixed-window limiter — no new dependency, mirroring the `PermissionManager.checkRateLimit` idiom. **Placement:** a middleware on `/api/*` registered after the source-IP allowlist gate and **in front of** `express.json` + the bearer gate, so unauthenticated token-guessing is also capped (`gateway/src/index.ts`). **Exemption:** reuses `isIpAllowed(ip)`, which is exactly loopback + `BOOKCLAW_ALLOWED_IPS` members — so localhost self-calls (e.g. the Telegram bridge) and trusted IPs are never throttled. **Auth-aware buckets:** keyed `req.ip` + `|auth`/`|anon` (independent counters), so a strict anonymous cap never throttles a busy authenticated client and vice-versa; "is this request authenticated" is computed with a new shared `extractToken(req)` helper that the bearer gate now also uses, so the two can't drift (auth-disabled ⇒ gate open ⇒ requests treated as authenticated/generous). **On exceed:** `429` + `Retry-After` (seconds to window reset) + an audit `rate_limited` event mirroring `ip_blocked`. **Env-configurable, posture logged at startup:** `BOOKCLAW_RATELIMIT_UNAUTH` (default **30**/window — strict), `BOOKCLAW_RATELIMIT_AUTH` (default **360**/window = 6/sec — generous safety net sized above realistic combined dashboard+MCP peak, since under the default Docker bridge all external authed clients share one source IP), `BOOKCLAW_RATELIMIT_WINDOW_MS` (default **60000**); a limit of `0` disables that bucket. **Tests:** smoke-test **Phase 5** (7 checks, TDD — written first and watched fail) drives distinct non-loopback IPs via `X-Forwarded-For` under `BOOKCLAW_TRUST_PROXY=1`: startup posture logged; anon `429` past the strict limit with a `Retry-After` header; a separate authed IP sails past the strict limit and only `429`s past the generous one (proves independent buckets); loopback never `429`s (exempt). `npx tsc --noEmit` clean; `npm test` green (34 unit + API + smoke). This is the inbound `/api/*` gate; distinct from the inbound per-channel chat limiter and the outbound provider 429-retry.

## 2026-06-01

- **Accurate per-call AI cost (OpenRouter `usage.cost`) + provider 429 retry** (`gateway/src/ai/router.ts`). Two router robustness fixes motivated by live observation (the pipeline test showed a real ~$0.09 spend reported as ~$2.43, and intermittent OpenRouter free-tier `HTTP 429`s). **(1) Real cost:** `completeOpenAICompatible` now uses the provider-reported `data.usage.cost` (USD, returned by OpenRouter on every response) as `estimatedCost` when present, falling back to the per-1k placeholder estimate only when absent (DeepSeek etc.) — so the dashboard/`CostTracker` reflect actual OpenRouter spend instead of the inflated Sonnet-priced guess. **(2) 429 handling:** added a `fetchWithRetry()` helper that retries `HTTP 429`/`503` with exponential backoff (1s, 2s; up to 2 retries) honoring a `Retry-After` header, applied to the three cloud providers (Gemini, Claude, OpenAI-compatible); Ollama (local) and the tags-probe are unchanged. Network errors and other statuses pass through, and the caller still handles a final non-OK — so it only smooths transient throttling and keeps the fail-soft contract. This is outbound provider throttling, distinct from the inbound per-channel `PermissionManager.checkRateLimit`. **Tests:** 3 new `router.test.ts` unit tests (fetch-stubbed) — `usage.cost` preferred when present, placeholder fallback when absent, and one retry on 429-then-200. `npx tsc --noEmit` clean; `npm test` green (32 unit + API + smoke). Cost follow-ons (account-total panel via `/api/v1/credits`, real per-model pre-call estimates via `/api/v1/models`) remain a TODO.
- **Dashboard per-feature module split (complete).** Split the monolithic dashboard JS into ES modules bundled by esbuild (build pipeline landed first — see entry below). Final source tree: `dashboard/src/lib/{state,api,ui,format}.js` + `dashboard/src/panels/{personas,idle-tasks,insights,settings,projects,chat,library,hq,home}.js` + a 175-line `main.js` app shell (imports + nav `switchPanel` + status polling + theme + init). **`main.js` went 2,677 → 175 lines.** Key design points: shared mutable state moved to a `state` object (ES modules can't reassign imported bindings); the app shell exports `switchPanel`/`loadStatus`/`renderHomeProjects` so panels import them via cycles esbuild resolves through hoisted function declarations; chat is a side-effect-only module (wires its own controls on import); the 3 `window.*` globals (`deletePreference`/`orchAction`/`orchRemove`) used by inline `onclick` handlers preserved. Done incrementally (one or a few panels per push), each verified by: esbuild build clean (token placeholder + the window globals present, every cross-module function defined exactly once), a cross-panel import audit (no module calls an un-imported function — the one runtime-break class esbuild can't catch), `npm test` green (29 unit + API + smoke), and `tsc` clean. **Acceptance:** deployed to Mercury at each milestone and the final fully-modularized dashboard passed a human visual click-through (Home, Projects incl. per-step model pickers, Personas, Library, Settings cards, Lessons/Preferences/Orchestrator inline buttons, Author HQ, chat, theme toggle) — behavior-preserving confirmed.
- **Dashboard build pipeline (esbuild) + `dashboard/src/` tree** — first stage of the "introduce a real build + split into modules" item. Stood up a `dashboard/src/` source tree (`index.html` template with `<style>/*__CSS__*/</style>` + `<script>/*__JS__*/</script>` placeholders, `styles.css`, `main.js`) and `dashboard/build.mjs`: esbuild bundles `main.js` into a single IIFE which, with `styles.css`, is inlined into one self-contained `dist/index.html` — the served shape is byte-for-byte equivalent in behavior to the old monolith (esbuild strips comments, so it's smaller). The build **guards that the `__BOOKCLAW_AUTH_TOKEN__` placeholder survives** into the output (the dashboard can't authenticate without it), and a `--watch` mode rebuilds on any `src/` change. Wiring: `npm run build:dashboard` / `:watch`; `esbuild` added as an explicit devDependency (was transitive via tsx; lockfile synced); Docker builder stage now copies `dashboard/` and runs the build **before** `npm prune --omit=dev`, and the runtime stage copies the freshly-built `dashboard/` from the builder (so the image never ships a stale committed `dist`). `dist/index.html` stays **committed** (source of truth is `src/`) so local `npm start` + the smoke/API tests work with no build step; CLAUDE.md updated to say "edit `src/`, not `dist/`". Verified: `npm run build:dashboard` clean, placeholder + all key functions present in output, `npm test` green (29 unit + API + smoke incl. "dashboard / serves with token injected"). **Deferred (own TODO item):** splitting the still-monolithic `main.js` into per-feature `lib/`+`panels/` ES modules — chosen to ship the low-risk pipeline first and do the reference-heavy split (shared-state object refactor) incrementally with Mercury visual checks, since there's no behavioral test net.
- **Per-step model selection** — each pipeline/project step can now pin its own AI provider + exact model id (cheap-draft / premium-edit within one pipeline), instead of every step routing only by task tier. **Data model:** `ProjectStep.modelOverride?: { provider, model? }` (blank model = pin the provider only). **Router:** `CompletionRequest` gained an optional `model`; `AIRouter.complete()` dispatches with a shallow provider-clone whose `model` is the override, so every per-provider completion method (Ollama/Gemini/OpenAI-compatible) picks it up unchanged — unset = the provider's default model (today's behavior). **Threading:** `handleMessage(...)` gained a 7th `overrideModel?` param (the provider-pin half already existed via `preferredProvider`); the step executors pass it. A shared `stepRouting(project, step)` helper (`routes/_shared.ts`) resolves precedence **step override → project `preferredProvider` → tier routing** and is applied at the primary + continuation + quality-retry call sites in `projects.routes.ts` and the goal-engine path in `index.ts` (the deliberate `'general'` short-response retries are left unpinned so they can still fall back). **Endpoint:** `POST /api/projects/:id/steps/:stepId/model` ({provider, model}; empty provider clears) → `ProjectEngine.setStepModelOverride()`; provider validated against the known set (incl. `openrouter`), model id is free-text (OpenRouter ids are arbitrary) but length-capped at 200 and rejected if it contains control chars. **Dashboard:** each step in the project-detail view gained a compact provider dropdown ("Inherit (default)" + the 6 providers) + an optional model-id input; changes POST on `change` (not per keystroke) and don't re-render, preserving focus/open-output. **Incidental fix (noted, not silent):** the dashboard execution paths in `projects.routes.ts` previously passed *no* provider to `handleMessage`, so the existing project-level "AI Model" override (set in the create modal) was silently ignored on dashboard-triggered runs — `stepRouting`'s `… → project.preferredProvider` fallback now makes that control actually take effect there too. **Cost note (follow-up):** `estimatedCost` still uses the base provider's placeholder rates for an overridden model — real per-model pricing remains a separate item. **Tests:** 2 router unit tests (model sent on the wire / provider default when unset, via a fetch stub), 1 engine unit test (`setStepModelOverride` set/normalize-blank/clear/not-found), 3 API contract tests (unknown project → 404, invalid provider → 400, over-long model → 400); plus a live end-to-end check (create pending project → set override → GET shows it persisted → clear → delete) and confirmation the served dashboard carries the new control. `npx tsc --noEmit` clean; `npm test` green (29 unit + API + smoke).
- **Cleanup from the Level 1 refactor: prune now-unused imports in `index.ts`** — the TODO framed these as "service-class imports," but an empirical scan (every imported symbol checked for references outside its own import line) found the service-class imports are all still used as `public field!: Type` field-type annotations — none were removable. The genuinely-unused leftovers were 4 *utility* imports orphaned when their logic moved during the extraction: `fileURLToPath` (`url`) and `dirname` (`path`) → moved to `gateway/src/paths.ts`; `randomBytes` (`crypto`) → the auth-token generation moved into the security init phase; and `createAPIRoutes` (`./api/routes.js`) → now imported/called only by `init/phase-11-http.ts`. Trimmed surgically: removed the `url` and `createAPIRoutes` import lines outright, and dropped only the dead symbols from the shared `path`/`crypto` lines (kept `join` — 47 refs — and `timingSafeEqual` — 3 refs). Verified: a re-scan reports 0 unused imports, `npx tsc --noEmit` clean, and `npm test` green (26 unit + 17 API + 19 smoke — the API/smoke suites boot the gateway, exercising the edited `index.ts`).
- **Seed the test suite (unit + API)** — "No test suite exists" addressed by standing up two contained, dependency-free test layers (per user direction: unit tests + simple curl API tests, explicitly **no** Playwright/heavy UI-coupled tests since the UI is changing). **Runner:** `node --test` run through `tsx` (`node --import tsx --test tests/unit/*.test.ts`) — no new deps, matches the sibling `claude-cowork-linux` project's choice; `tests/` is outside the `tsconfig` `include`, so the production build is unaffected. **Unit (`tests/unit/`, 26 checks):** `router.test.ts` covers `AIRouter` thoroughly — tier routing (free/mid/premium), unknown-task → general tier, tier fall-through, global + per-call preferred-provider override, unavailable-preferred fallback, the over-budget gate (skip paid → free; absolute fallback still returns a paid provider when it's the only one; throw when none), `getFallbackProvider` (prefer free / fail-closed over budget / paid when under budget), and the `getOutputBudget`/`getRecommendedThinking` helpers — all network-free via a fake vault (every provider but Ollama is key-presence only) + Ollama disabled + a fake cost tracker. `projects.test.ts` covers `ProjectEngine`'s pure surface — `getTemplates` shape/contents and the `inferProjectType` natural-language heuristic — constructed against a non-existent state path so the constructor is inert. **API (`tests/api/api-test.sh`, 17 checks):** hermetic curl tests (loopback, env-supplied token, read-only) asserting the contract of `/api/status`, `/api/projects/list`, `/api/config` (incl. a regression guard that the removed `branding` block is gone), `/api/vault/keys`, plus error contracts (empty `POST /api/vault` → 400, unknown route → 404, no token → 401). **`package.json`:** added `test:unit`, `test:api`, and an aggregate `test` (unit + api + smoke). All green: 26 + 17 + 19(smoke) pass, no stray processes. **Deferred:** `ProjectEngine` step orchestration + completion-hook coverage needs an AI injection seam — folded into the god-class Level 2–3 item.
- **Docker-image deploy automation to Mercury (test server)** — automated path from "edit on any workstation" to "running container on Mercury", no manual SSH/Docker. **As-built (sentinel-trigger approach):** the repo lives on Mercury's local disk and is NFS-exported to the workstations, so the working tree is already on the build host — no `docker save`/registry/SSH-ship needed. Delivered (2026-05-31): `scripts/build-watch.sh` (rebuilds + `docker compose up -d` from the live working tree when a `build_now` sentinel exists, logs to gitignored `.build-logs/`, reuses the stable vault key from `.env` so the persisted vault stays decryptable), `scripts/systemd/bookclaw-build.{service,timer}` (1-min poll), and `scripts/systemd/README.md`. Reuses the standard `bookclaw`/`3847` container (no separate test instance). Workflow: `touch build_now` from any machine → Mercury rebuilds within ~1 min. **Verified end-to-end (2026-06-01):** the systemd timer is installed and active on Mercury (`192.168.1.32`), and a `touch build_now` triggered a full rebuild of commit `58d4801` → `result=PASS` in `.build-logs/last-build.status`, the `bookclaw` container recreated and reported healthy, and `http://192.168.1.32:3847/` served the updated BookClaw dashboard (title `BookClaw Dashboard`, zero `footer-links` occurrences). Note: a separate, unrelated pre-rename `AuthorClaw v4.0.0` container runs on a different host (`192.168.1.28:3847`) — not managed by this automation.
- **OpenRouter key doesn't auto-register providers on save** — `POST /api/vault` only reinitialized the AI router for keys in its `apiKeyNames` list (gemini/deepseek/anthropic/openai); `openrouter_api_key` was missing, so storing it left OpenRouter unregistered until a manual `/api/providers/refresh` or a restart. **Fix:** added `'openrouter_api_key'` to the `apiKeyNames` array in `gateway/src/api/routes/settings.routes.ts` (the `POST /api/vault` handler). Scope note: the separate `/api/vault/load-from-files` path has the same array *and* omits OpenRouter, but its `keyFiles` map has no `openrouter_api_key.txt` entry, so that path can't load an OpenRouter key at all — adding it to only the array there would be dead, and wiring up file-loading is a feature beyond this cleanup; left as-is. **Verification:** added `tests/openrouter-autoregister.sh` — a hermetic, non-destructive regression test that boots the gateway (loopback, env-supplied token), asserts OpenRouter is absent from `/api/status` on a clean vault, stores a *dummy* `openrouter_api_key` (registration is key-presence only — no network call), and asserts OpenRouter now appears **without** a manual refresh. It refuses to touch a pre-existing real key (degrades to a presence check) and on exit deletes the dummy key and removes the vault file if the run created it. `bash tests/openrouter-autoregister.sh` passes 4/4; `npx tsc --noEmit` clean.
- **Latent init-ordering bug: `characterVoices` wired with `undefined` styleClone** — Phase 6g9 (`init/phase-07-knowledge.ts`) called `characterVoices.setStyleClone(gw.styleClone)` before `styleClone` was constructed in Phase 6j (`init/phase-09-export-wave.ts`), so it was wired with `undefined`. Confirmed `CharacterVoicesService.initialize()` only does `mkdir` and never touches styleClone (the runtime methods `analyze`/`ingestChapter`/`detectDrift` are the only consumers, and they `throw` if unwired), so the dependency is needed at request time, not construction time. **Fix:** kept each service in its existing phase and moved the *wiring* to Phase 6j, immediately after `styleClone` is constructed — `styleClone` stays in its Wave 2 grouping, `characterVoices` is still constructed+initialized in 6g9, and `setStyleClone` now runs with a real instance. Updated the phase doc-comments/inline notes to describe the deferred wiring instead of the bug. Verified: `npx tsc --noEmit` clean and `npm run test:smoke` all pass (the gateway boots through the full init sequence including 6g9→6j, exercising the new ordering end-to-end).
- **Remove the small icons at the bottom-left of the dashboard** (site, yt, sub, tools) — deleted the `.footer-links` block (Site/YT/Sub/Tools anchors) from `dashboard/dist/index.html`, plus its `.footer-links` CSS rules and the mobile `display:none` rule, and the `applyFooterLinks()` JS function + its boot-time call. Removing that function orphaned its only data source, so also dropped the now-dead `branding.footerLinks` block from `config/default.json` and the `branding:` field from the `/api/config` response in `gateway/src/api/routes/settings.routes.ts`. The heartbeat dot/label and theme toggle in the same `.sidebar-footer` are untouched. Verified: `config/default.json` still parses, `npx tsc --noEmit` clean, no remaining `footer-links`/`footerAuthorLinks`/`applyFooterLinks`/`footerLinks` references in the tree.
- **Fresh-test reset script** (`scripts/reset-test-data.sh`) — wipes BookClaw runtime data for a clean/fresh test while keeping settings. Targets the running Docker container (default `bookclaw`): resolves the `/app/workspace` and `/app/config/.vault` volumes straight from the container's mounts (no name-guessing), then **stop → empty the workspace volume (throwaway container on the same image, `--entrypoint find … -delete`, so no image pull and the docker-entrypoint wrapper is bypassed) → start**, and waits for the healthcheck to go green. **Keeps:** API keys (the separate `bookclaw-vault` volume is never touched) and app config (`config/user.json`/`default.json`/`research-allowlist.json` are baked into the image, not in the wiped volume). **Wipes:** everything under the workspace volume — projects, context, memory/conversations, soul/author identity, logs, exports, audio/images, costs (the user chose the fullest "keys + config only" blank-slate scope). Uses the same container (no rebuild/recreate) so the bearer token and vault key are preserved. Safety: prints a KEEP/WIPE plan and requires typing `RESET` to confirm (`-y`/`--yes` skips it for scripted use); best-effort post-reset project count printed. Run on the host or `ssh mercury bash …/scripts/reset-test-data.sh`.
- **God-class refactor — Level 1 (phase/route extraction)** ([GOD-CLASS-REFACTOR.md](GOD-CLASS-REFACTOR.md)) — split the two god classes into thin composition roots. Behavior-preserving (pure relocation, no interface/behavior changes), done on branch `refactor/level1-init-extraction` in verified increments. **`index.ts`: 2,649 → 2,103 lines** — every `initialize()` phase block extracted into `gateway/src/api/../init/phase-01..11` modules (config, security, soul-memory, ai, research-skills, content, knowledge, website, export-wave, heartbeat-bridges, http); `initialize()` is now a 12-call sequence. Fixed the duplicate `Phase 6h` (→ `6h2`) and added `gateway/src/paths.ts` (shared `ROOT_DIR`, avoids a circular value-import). **`routes.ts`: 5,518 → 59 lines** — all 234 endpoints moved into 12 per-feature mounters under `gateway/src/api/routes/` (core, settings, projects, documents, heartbeat, personas, media, ops, export, wave, knowledge, website) + `routes/_shared.ts` (`safePath`, `upload`, `addWaveDisclaimer`, `makeGatherChapters` factory); `createAPIRoutes` is a thin composition root. **Verification (every increment):** `tsc` clean, a route-registration diff (234/234 endpoints register identically before/after), a called-vs-defined mounter check, `npm run test:smoke` 18/18, a full Docker rebuild on Mercury (build PASS + healthy), and an end-to-end `tests/openrouter-pipeline.sh` run (51/51 pipeline steps PASS through the refactored handlers). One latent init-ordering bug found and **preserved** (characterVoices wired with `undefined` styleClone) — logged separately in TODO Quick cleanups. **Levels 2–3 (service registry, plugin contracts) intentionally deferred** — see the remaining TODO item and the doc's feature-driven guidance.

## 2026-05-31

- **[RENAME-PLAN.md](RENAME-PLAN.md)** — rename project from AuthorClaw → BookClaw. Decisions captured, runbook ready, not yet executed. *Executed in stages per the recommended decisions (1a remote-rename-in-place, 2a hard env-var rename, 3a local-dir rename, 4a class rename). **Stage A — code:** bulk-replaced all occurrences across 147 tracked files (`AuthorClaw→BookClaw`, `AUTHORCLAW→BOOKCLAW` — broadened to catch standalone all-caps banners, `authorclaw→bookclaw`); `.env` var renamed `AUTHORCLAW_VAULT_KEY→BOOKCLAW_VAULT_KEY` (value preserved); no `vault.enc` existed so 2a carried no credential-migration risk; `AuthorClawGateway`→`BookClawGateway`. Committed `9cf372c`. **Stage C — remote:** `gh repo rename` → `pshort05/bookclaw`, local `origin` URL updated, old URL auto-redirects. **Stage D — local dir:** `mv authorclaw bookclaw`, session relaunched from the new path. Post-`mv` validation all green: `git remote -v` shows `pshort05/bookclaw`, `git grep -li authorclaw` returns only `docs/RENAME-PLAN.md` (deliberately quotes the old name in its resume steps), `.env` present with `BOOKCLAW_VAULT_KEY` intact, `npx tsc --noEmit` clean, `npm run test:smoke` 18/18 pass.*

## 2026-05-30

- **Re-read every place that hardcoded `127.0.0.1` / `localhost`** (security review) — confirm no remaining assumptions silently treat localhost as a trust boundary after the LAN patch. *Audited all 20 source hits. Most benign: Ollama outbound endpoint defaults (`ai/router.ts`), the intentional loopback-always-allowed recovery path in the IP allowlist (`index.ts`), accurate `BOOKCLAW_BIND` comments, and the gate docstring fixed earlier today. Two real findings, both fixed: **(1) stale trust comment** — `api/routes.ts:6-8` claimed "All endpoints are currently unauthenticated. This is acceptable because the server binds to 127.0.0.1 only" and "implement Bearer token auth" — triple-stale (endpoints are now auth-gated, bind is `0.0.0.0`, the auth TODO is done); rewrote it to describe the real model (bearer auth + optional IP allowlist in `index.ts`, bind defaults to `0.0.0.0`, don't treat localhost as a trust boundary). **(2) functional regression from the auth work** — the Telegram bridge makes in-process HTTP self-calls to `http://localhost:3847/api/*` (`telegram.ts` ×6: author-os/format, workspace/stats, workspace/clean, audio/generate ×2, projects/:id/pause; plus `index.ts:1969` /export) sending NO `Authorization` header, so under the now-default auth they would 401 — breaking the `/export`, `/files`, `/clean`, `/speak`, and pause Telegram commands. The bridge had assumed localhost = no auth needed. Fix (option A, token header): added a `TelegramBridge.apiHeaders()` helper that attaches the bearer token read from `process.env.BOOKCLAW_AUTH_TOKEN` (which the gateway sets at `index.ts:360`; absent when auth is disabled → no header) and applied it to all six calls; the `index.ts:1969` self-call uses `this.authToken` inline. Loopback-skip-auth (option C) was rejected — it would reinstate exactly the trust boundary this review removed. Verification: `npx tsc --noEmit` clean; `npm run test:smoke` all 18 pass (the "no token → 401" + "valid bearer → 200" assertions cover the HTTP behavior the fix depends on). A bridge-instantiation test was not added — it requires a Telegram bot token, and the gate behavior the fix relies on is already smoke-tested. Cosmetic-only, left as-is: several user-facing strings still display `http://localhost:3847` (startup log, Telegram messages) — slightly misleading on a LAN but not a trust issue.*
- **Audit the confirmation-gate's "local requester" assumption** (security review) — `ConfirmationGateService` was designed assuming the approver is on the same machine; confirm nothing in the gate flow trusts source IP / loopback in a way LAN exposure breaks. *Audit outcome: no active vulnerability. Traced the full create → list → approve/reject → outcome flow and every caller. The gate never grants access based on source IP or loopback — there is no `req.ip`/`127.0.0.1` check anywhere in `confirmation-gate.ts` or the routes. Approval is reachable ONLY via `POST /api/confirmations/:id/approve` (the sole caller of `gate.approve()`/`reject()`/`recordOutcome()`), which sits behind the bearer-auth gate + optional source-IP allowlist on `/api/*`; bridges expose no approval command and the WebSocket handler has only `message`/`disconnect`, so observed/injected content and other channels cannot trigger an approval. The one real finding was a **stale, now-false security comment**: the `approve()` docstring (`confirmation-gate.ts:194`) claimed approval was safe because "BookClaw binds to 127.0.0.1" — written before auth existed (loopback was then the only protection) and never updated after the LAN patch moved the bind to `0.0.0.0`. Latent risk only (a future maintainer could add an approval path trusting a guarantee that no longer holds), so the fix was a surgical docstring correction stating the real model — the bearer token, not the bind address, is the trust boundary; never expose approval on an unauthenticated route or non-HTTP channel. No behavioral change; `npx tsc --noEmit` clean. Two cross-references recorded (not fixes): `decidedBy` is hardcoded `'user'` so there's no per-approver identity under a shared token (the separate "audit-log source IP" item would close the forensic gap), and the gate's protection now rests entirely on the bearer token, which is extractable from the public dashboard HTML at `/` (the already-logged token-exposure finding) — both within the accepted home-LAN threat model.*
- **Restore + tighten Helmet CSP** (security review item #3) — replace `connectSrc: ["'self'", "*"]` with an allowlist that matches what the dashboard actually loads; reconsider `upgradeInsecureRequests: null`. *Investigation: the dashboard (`dashboard/dist/index.html`, one inline-JS file served same-origin) fetches only its own origin (`var API = ''`, a relative base), loads no external subresources (no CDN scripts/styles/fonts/images, no `data:`/`blob:` URIs), and opens no browser WebSocket — the lone `io(` grep hit was a false match inside `new Audio(`. The ~17 external `https://` URLs are all `href target="_blank"` navigations, which CSP `connect-src`/`img-src` don't govern. So `connect-src 'self'` is exact. Decisions (confirmed by user): set **`connectSrc: ["'self'"]`** (not `'self'` + CORS origins — CORS governs the inbound direction, not the dashboard's outbound, and an MCP server is a non-browser client that CSP cannot gate regardless); **keep `upgradeInsecureRequests: null` (off)** because the server speaks plain HTTP on the LAN, so forcing `https://` rewrites would break the same-origin dashboard — documented flipping it to `{}` once an HTTPS/reverse-proxy deployment is recommended. Implementation (`gateway/src/index.ts`): tightened the `connectSrc` directive and added comments explaining the `'unsafe-inline'` requirement (inline-JS dashboard), the `'self'`-is-exact rationale, and the `upgradeInsecureRequests` decision. Smoke test Phase 1 gained two assertions via a new `rheader` helper: `connect-src` is exactly `'self'` and contains no permissive `*` (the old `'self' *` value fails both). 18 checks total pass; `npx tsc --noEmit` clean. Next security item: API-level rate limiting.*
- **Source-IP allowlist (`BOOKCLAW_ALLOWED_IPS`)** (security review; new item raised during CORS work) — a network-level gate, distinct from CORS, restricting which *client machines* may reach the server at all (applies to every client, in front of even the bearer token). *Investigation first surfaced a make-or-break caveat: the current Docker deploy (default bridge + published port, no `daemon.json` so `userland-proxy` defaults true) makes the container see the bridge gateway IP for every external client, so an app-level per-IP gate only sees real IPs under host-net/native/VPS or behind a trusted proxy — the most robust enforcement for the VPS "only my home IP" case is the host firewall / provider security group, with this as defense-in-depth. Decisions (confirmed by user): **unset = allow all + a startup notice** (enforcement strictly opt-in; auto-deriving a local /24 was rejected as a trap — in Docker it derives the 172.x container subnet, on a VPS the datacenter subnet, blocking the real client both ways); **always allow loopback** as a recovery path; **opt-in `BOOKCLAW_TRUST_PROXY=1`** reads the client IP from `X-Forwarded-For` for reverse-proxy deploys (off by default; spoofable unless behind a sole-ingress proxy). Implementation (`gateway/src/index.ts`): added `ipaddr.js` as a direct dep (was transitive via `proxy-addr`) for correct IPv4/IPv6/IPv4-mapped + CIDR matching; constructor parses `BOOKCLAW_ALLOWED_IPS` (comma-separated IPs/CIDRs; single IPs → /32 or /128; invalid entries warned and skipped), sets `trust proxy`, and registers an Express gate (403 + audit `ip_blocked` on deny) ahead of the auth middleware; a matching `io.use()` gates the Socket.IO handshake; `isIpAllowed()` uses `ipaddr.process()` to normalize IPv4-mapped addresses and treats `range()==='loopback'` as always-allowed; startup logs the posture (`ℹ IP allowlist: not set …` / `✓ IP allowlist: N rule(s) enforced …`). Smoke test Phase 4 (trust-proxy on, so allow/deny is deterministic from loopback via XFF): exact IP allowed, CIDR member allowed, unlisted IP → 403 even with a valid token (proves the gate is in front of auth), loopback always allowed, and the enforcement log present. 16 checks total pass; `npx tsc --noEmit` clean. Next security item: Helmet CSP.*
- **Tighten CORS once auth lands** (security review item #2) — replace `cors: { origin: '*' }` (Express + Socket.IO) with an explicit allowlist driven by `BOOKCLAW_CORS_ORIGINS`. *Implemented a shared `corsOptions` (one origin-callback used by both Express and the Socket.IO server, so the WebSocket origin check is covered too). Decisions (confirmed by user): **deny all cross-origin by default** when the env var is unset — safe because the dashboard is served same-origin (`var API = ''`) so it never needs CORS, and curl/MCP/bridges are non-browser so CORS doesn't apply; `BOOKCLAW_CORS_ORIGINS=<comma-separated origins>` is the precise override; a literal `*` entry restores permissive CORS as a loudly-logged escape hatch (mirrors `BOOKCLAW_AUTH_DISABLED`). Requests with no `Origin` header (curl/MCP/server-to-server/same-origin) are always allowed since CORS only protects browsers and the bearer token is the real gate. Startup logs the effective posture (`✓ CORS: cross-origin denied …` / `… N allowed origin(s) …` / `⚠ CORS: wildcard …`). Extended `tests/smoke-test.sh` (now 11 checks): default-deny gives no `Access-Control-Allow-Origin`; a new Phase 3 starts with an allowlist and asserts a listed origin is echoed while an unlisted one is not. Manually verified the `*` wildcard path echoes arbitrary origins and logs the warning. `npx tsc --noEmit` clean; `npm run test:smoke` all pass. Note: a distinct, stronger control for this threat model — a **source-IP allowlist** (`BOOKCLAW_ALLOWED_IPS`) — was identified during this work and added to `docs/TODO.md` as the next security item (CORS governs browser origins; an IP allowlist governs which machines can connect at all).*
- **Script the auth smoke test into `tests/` + scripted-tests directive** — turn the manual curl verification of security item #1 into a repeatable script, and add a standing testing directive to the project `CLAUDE.md`. *Added `tests/smoke-test.sh` (`npm run test:smoke`): boots the gateway and asserts the bearer-token gate (401 without token, 200 with bearer, 200 via `?token=` fallback, 401 wrong token, dashboard token injection, and the `BOOKCLAW_AUTH_DISABLED=1` escape hatch). Hermetic/non-destructive — token supplied via env so `.env` is never written, binds loopback only, launches `node --import tsx` directly (not `npm start`) so the PID is killable, EXIT-trap cleanup, `-v` streams the captured server log. Added a `## Testing` section to `CLAUDE.md` with two directives — tests must be scripted/repeatable, and debug logging must be available for testing — and corrected the now-stale "no test suite / no `tests/` directory / don't invent test commands" statements in the intro and Commands sections. Verified: `npm run test:smoke` → all 8 checks pass, exit 0, `.env` unchanged, no stray process.*
- **Add HTTP/WebSocket authentication** (security review item #1) — any host on the LAN could drive the agent, read the workspace, and trigger confirmation-gate items. Pick a model — bearer token in env var (simplest), shared-secret HMAC on every request, or mTLS — and enforce it on every Express route and on the Socket.IO `connection` handshake (`socket.handshake.auth.token`). *Implemented option (A), bearer token. Design decisions (all confirmed by user): missing token auto-generates and persists to `.env` (mirrors `BOOKCLAW_VAULT_KEY`); `BOOKCLAW_AUTH_DISABLED=1` escape hatch with a loud startup warning; token stored plaintext in `.env`; Telegram/Discord bridges left on their own platform auth. Changes — `gateway/src/index.ts`: token load/generate in Phase 2c, Express middleware gating `/api/*` (constant-time compare, accepts `Authorization: Bearer` or `?token=` query fallback for native-element GETs), `io.use()` handshake gate, and `/` now serves the dashboard HTML with the token substituted into a `__BOOKCLAW_AUTH_TOKEN__` placeholder (`express.static` set `index:false` so `/` reaches the injector). `dashboard/dist/index.html`: `AUTH_TOKEN` global + `authHeaders()`/`authUrl()` helpers; both fetch wrappers (`api`, `apiRaw`) send the bearer header; the three native-element URLs (project-file `<img>`, download `<a>`, TTS `<Audio>`) use `?token=`. Smoke test (`npm start`): no token → 401, bearer → 200, `?token=` → 200, wrong token → 401, `/` loads with placeholder substituted; with `BOOKCLAW_AUTH_DISABLED=1` → warning fires and `/api/status` → 200 unauthenticated. `npx tsc --noEmit` clean.*
- **Stale version banner in `gateway/src/index.ts`** — console banner prints `v3.0.0` (around line 202), but `package.json` is `4.0.0`. Update the banner string. *Fix: bumped to `v5.0.0` as part of the new-fork version bump (package.json + package-lock.json + banner all moved 4.0.0 → 5.0.0).*
- **Stale version in `QUICKSTART.md`** — sample startup output also shows `BookClaw v3.0.0`. Update to match. *Fix: bumped to `v5.0.0` alongside the index.ts banner.*
- **Stale localhost-only claim in `README.md:338`** — Security feature bullet read `**Localhost Only**: Server binds to 127.0.0.1 (no external access)`, contradicting the fork's `0.0.0.0` default already documented later in the same file. *Fix: replaced with a "Configurable Bind" bullet that points to the Deployment — Defense in Depth section for the full fork posture.*
- **Stale `SECURITY.md` reference in `TODO.md` standing constraints** — note claimed `SECURITY.md` still described the old localhost-only contract, but `SECURITY.md` had already been updated to describe the `0.0.0.0` default (lines 63, 70, 74). *Fix: removed the obsolete "SECURITY.md and the README still describe the old contract" trailer from the standing-constraints bullet (the README fix above resolved the other half).*
- **Stale historical comment in `gateway/src/skills/loader.ts`** — comment explains that the `ops` category was once missing from the load list. The bug is fixed; delete the comment. *Fix: deleted the 3-line comment at `loader.ts:40-42`. Also removed two now-orphaned entries from CLAUDE.md's "Things that look broken but aren't" section: the `v3.0.0` banner note (resolved by the 5.0.0 version bump earlier today) and the `ops`-comment-historical-context note (resolved by this deletion).*
- **Windows path in `LAUNCH-GUIDE.md`** — section "Local PC (Windows — Direct)" references `C:\Users\chris\...`. Either generalize it or move under a per-user-example heading; right now it reads like documentation of a specific machine. *Fix: applied option (a) — replaced the literal path at `LAUNCH-GUIDE.md:11` with the placeholder `C:\path\to\bookclaw`. Added a follow-up TODO under "Investigations" to reconsider Windows as a targeted platform (may go Mac/Linux/Docker-only with Windows routed through WSL2 or Docker Desktop).*
- **`npm audit` / dependency check.** No tests, no CI; vulnerable transitive deps could ship silently. Run `npm audit --omit=dev` and triage. *Fix: ran `npm audit --omit=dev`, found 9 vulnerabilities (3 high, 6 moderate) in `multer`, `path-to-regexp`, `socket.io-parser`, `qs` (via body-parser/express), and `ws` (via engine.io/socket.io-adapter). All in-range — `npm audit fix` (no `--force`) resolved all 9 without touching `package.json` semver ranges; `package-lock.json` updated. Smoke test: `npm start` → server ready in 1s, `/api/status` returned valid JSON, 29 skills loaded, all subsystems clean, banner correctly shows `v5.0.0`.*
- **Confirm `OpenClaw` / `openclaw` references are intentional** — they appear in `package.json` keywords, README, and inline comments. Decide whether to keep them as fork attribution or scrub the ones that are no longer accurate. *Outcome: audited all refs across `*.ts`, `*.js`, `*.json`, `*.md`. Categorised into three buckets — fork attribution (4: `package.json` keyword, `index.ts` header + banner, CLAUDE.md note), "Inspired by" feature credits (5: `router.ts`, `index.ts`, `routes.ts` ×2, `tts.ts`), and roadmap/analysis docs (`OPENCLAW-UPDATES.md`, `GOD-CLASS-REFACTOR.md`, `STORYHACKERAI-PORTING.md`, README links). No obsolete refs found — all intentional. Single small fix: `CLAUDE.md:173` claimed `OpenClawWorld` refs exist in code, but grep finds zero; tightened that line to describe the two real reference categories (fork attribution + inspired-by credits).*
- **Decide whether `npm run build` (tsc emit to `dist/`) is still needed** — dev and production both run through `tsx`. If `dist/` is never consumed, the script and `tsconfig` emit settings can be simplified. *Outcome: TODO premise was wrong. `docker/Dockerfile` is a multi-stage build — stage 1 compiles via `npx tsc --outDir dist`, stage 2 runs `node dist/gateway/src/index.js` as the production CMD. So `dist/` IS consumed in production (Docker images), `tsconfig.json` emit settings are load-bearing, and `npm run build` is a useful local alias for the same compile step. Nothing to simplify. Tightened CLAUDE.md's `npm run build` Commands-section line from "rarely needed; only for packaging" to call out the Docker dependency explicitly.*
- **Stale localhost-only claims in `docs/LAUNCH-GUIDE.md`** — line 162 (Ports table: `BookClaw | 3847 | localhost only`) and line 172 (Security Checklist: `Server binds to 127.0.0.1 (default — don't change)`) both contradict the fork's `0.0.0.0` default and the same file's own "Network access" section (lines 68-96). Update both to match the actual posture (configurable bind via `BOOKCLAW_BIND`, defaulting to `0.0.0.0`). *Fix: Ports table cell now describes the `BOOKCLAW_BIND`-driven default-`0.0.0.0` behavior; Security Checklist item replaced with two items — one for "bind appropriate for environment" and one for "tunnel or auth proxy in front of any non-trusted-LAN deployment". Ollama's localhost-only row left as-is (still correct for the local Ollama daemon).*
- **Reconsider Windows support as a targeted platform.** `LAUNCH-GUIDE.md` currently documents a "Local PC (Windows — Direct)" path alongside Linux/Docker. The fork may end up Mac/Linux-only (via Docker or local install), with Windows users routed through WSL2 or Docker Desktop rather than direct `npm start`. *Outcome: applied option (C) — docs-and-intent only, no code surgery. Audited the surface: `process.platform === 'win32'` branches exist in 3 source files (`services/website-deploy.ts`, `services/external-tools.ts`, `security/vault.ts`), `.ps1` VM-provisioning helpers exist in `scripts/`, and `setup-wizard.sh` has OSTYPE detection. README has no Windows-direct install refs. Decision: leave inherited code branches in place (they don't hurt Linux/Mac/Docker), keep the `.ps1` helpers (they're VM-admin tooling, not app-runtime), and update `LAUNCH-GUIDE.md` to drop the "Local PC (Windows — Direct)" section in favour of a "Windows — via Docker Desktop or WSL2" section pointing at the existing Linux/Docker flows. Surfaces the actual supported posture without invasive change. Spotted two unrelated stale `localhost only` / `127.0.0.1 (default — don't change)` lines in the same file (lines 162 and 172) — added as a new Quick-cleanup TODO entry.*
