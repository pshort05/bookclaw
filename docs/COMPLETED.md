# Completed

Items moved here from [TODO.md](TODO.md) as they are finished. Newest first. Each entry is prefixed with its completion date (`YYYY-MM-DD`) and preserves the original TODO bullet text.

## 2026-06-01

- **Fresh-test reset script** (`scripts/reset-test-data.sh`) — wipes BookClaw runtime data for a clean/fresh test while keeping settings. Targets the running Docker container (default `bookclaw`): resolves the `/app/workspace` and `/app/config/.vault` volumes straight from the container's mounts (no name-guessing), then **stop → empty the workspace volume (throwaway container on the same image, `--entrypoint find … -delete`, so no image pull and the docker-entrypoint wrapper is bypassed) → start**, and waits for the healthcheck to go green. **Keeps:** API keys (the separate `bookclaw-vault` volume is never touched) and app config (`config/user.json`/`default.json`/`research-allowlist.json` are baked into the image, not in the wiped volume). **Wipes:** everything under the workspace volume — projects, context, memory/conversations, soul/author identity, logs, exports, audio/images, costs (the user chose the fullest "keys + config only" blank-slate scope). Uses the same container (no rebuild/recreate) so the bearer token and vault key are preserved. Safety: prints a KEEP/WIPE plan and requires typing `RESET` to confirm (`-y`/`--yes` skips it for scripted use); best-effort post-reset project count printed. Run on the host or `ssh mercury bash …/scripts/reset-test-data.sh`.
- **God-class refactor — Level 1 (phase/route extraction)** ([GOD-CLASS-REFACTOR.md](GOD-CLASS-REFACTOR.md)) — split the two god classes into thin composition roots. Behavior-preserving (pure relocation, no interface/behavior changes), done on branch `refactor/level1-init-extraction` in verified increments. **`index.ts`: 2,649 → 2,103 lines** — every `initialize()` phase block extracted into `gateway/src/api/../init/phase-01..11` modules (config, security, soul-memory, ai, research-skills, content, knowledge, website, export-wave, heartbeat-bridges, http); `initialize()` is now a 12-call sequence. Fixed the duplicate `Phase 6h` (→ `6h2`) and added `gateway/src/paths.ts` (shared `ROOT_DIR`, avoids a circular value-import). **`routes.ts`: 5,518 → 59 lines** — all 234 endpoints moved into 12 per-feature mounters under `gateway/src/api/routes/` (core, settings, projects, documents, heartbeat, personas, media, ops, export, wave, knowledge, website) + `routes/_shared.ts` (`safePath`, `upload`, `addWaveDisclaimer`, `makeGatherChapters` factory); `createAPIRoutes` is a thin composition root. **Verification (every increment):** `tsc` clean, a route-registration diff (234/234 endpoints register identically before/after), a called-vs-defined mounter check, `npm run test:smoke` 18/18, a full Docker rebuild on Mercury (build PASS + healthy), and an end-to-end `tests/openrouter-pipeline.sh` run (51/51 pipeline steps PASS through the refactored handlers). One latent init-ordering bug found and **preserved** (characterVoices wired with `undefined` styleClone) — logged separately in TODO Quick cleanups. **Levels 2–3 (service registry, plugin contracts) intentionally deferred** — see the remaining TODO item and the doc's feature-driven guidance.

## 2026-05-31

- **[RENAME-PLAN.md](RENAME-PLAN.md)** — rename project from AuthorClaw → BookClaw. Decisions captured, runbook ready, not yet executed. *Executed in stages per the recommended decisions (1a remote-rename-in-place, 2a hard env-var rename, 3a local-dir rename, 4a class rename). **Stage A — code:** bulk-replaced all occurrences across 147 tracked files (`AuthorClaw→BookClaw`, `AUTHORCLAW→BOOKCLAW` — broadened to catch standalone all-caps banners, `authorclaw→bookclaw`); `.env` var renamed `AUTHORCLAW_VAULT_KEY→BOOKCLAW_VAULT_KEY` (value preserved); no `vault.enc` existed so 2a carried no credential-migration risk; `AuthorClawGateway`→`BookClawGateway`. Committed `9cf372c`. **Stage C — remote:** `gh repo rename` → `pshort05/bookclaw`, local `origin` URL updated, old URL auto-redirects. **Stage D — local dir:** `mv authorclaw bookclaw`, session relaunched from the new path. Post-`mv` validation all green: `git remote -v` shows `pshort05/bookclaw`, `git grep -li authorclaw` returns only `docs/RENAME-PLAN.md` (deliberately quotes the old name in its resume steps), `.env` present with `BOOKCLAW_VAULT_KEY` intact, `npx tsc --noEmit` clean, `npm run test:smoke` 18/18 pass.*

## 2026-05-30

- **Re-read every place that hardcoded `127.0.0.1` / `localhost`** (security review) — confirm no remaining assumptions silently treat localhost as a trust boundary after the LAN patch. *Audited all 20 source hits. Most benign: Ollama outbound endpoint defaults (`ai/router.ts`), the intentional loopback-always-allowed recovery path in the IP allowlist (`index.ts`), accurate `BOOKCLAW_BIND` comments, and the gate docstring fixed earlier today. Two real findings, both fixed: **(1) stale trust comment** — `api/routes.ts:6-8` claimed "All endpoints are currently unauthenticated. This is acceptable because the server binds to 127.0.0.1 only" and "implement Bearer token auth" — triple-stale (endpoints are now auth-gated, bind is `0.0.0.0`, the auth TODO is done); rewrote it to describe the real model (bearer auth + optional IP allowlist in `index.ts`, bind defaults to `0.0.0.0`, don't treat localhost as a trust boundary). **(2) functional regression from the auth work** — the Telegram bridge makes in-process HTTP self-calls to `http://localhost:3847/api/*` (`telegram.ts` ×6: author-os/format, workspace/stats, workspace/clean, audio/generate ×2, projects/:id/pause; plus `index.ts:1969` /export) sending NO `Authorization` header, so under the now-default auth they would 401 — breaking the `/export`, `/files`, `/clean`, `/speak`, and pause Telegram commands. The bridge had assumed localhost = no auth needed. Fix (option A, token header): added a `TelegramBridge.apiHeaders()` helper that attaches the bearer token read from `process.env.BOOKCLAW_AUTH_TOKEN` (which the gateway sets at `index.ts:360`; absent when auth is disabled → no header) and applied it to all six calls; the `index.ts:1969` self-call uses `this.authToken` inline. Loopback-skip-auth (option C) was rejected — it would reinstate exactly the trust boundary this review removed. Verification: `npx tsc --noEmit` clean; `npm run test:smoke` all 18 pass (the "no token → 401" + "valid bearer → 200" assertions cover the HTTP behavior the fix depends on). A bridge-instantiation test was not added — it requires a Telegram bot token, and the gate behavior the fix relies on is already smoke-tested. Cosmetic-only, left as-is: several user-facing strings still display `http://localhost:3847` (startup log, Telegram messages) — slightly misleading on a LAN but not a trust issue.*
- **Audit the confirmation-gate's "local requester" assumption** (security review) — `ConfirmationGateService` was designed assuming the approver is on the same machine; confirm nothing in the gate flow trusts source IP / loopback in a way LAN exposure breaks. *Audit outcome: no active vulnerability. Traced the full create → list → approve/reject → outcome flow and every caller. The gate never grants access based on source IP or loopback — there is no `req.ip`/`127.0.0.1` check anywhere in `confirmation-gate.ts` or the routes. Approval is reachable ONLY via `POST /api/confirmations/:id/approve` (the sole caller of `gate.approve()`/`reject()`/`recordOutcome()`), which sits behind the bearer-auth gate + optional source-IP allowlist on `/api/*`; bridges expose no approval command and the WebSocket handler has only `message`/`disconnect`, so observed/injected content and other channels cannot trigger an approval. The one real finding was a **stale, now-false security comment**: the `approve()` docstring (`confirmation-gate.ts:194`) claimed approval was safe because "BookClaw binds to 127.0.0.1" — written before auth existed (loopback was then the only protection) and never updated after the LAN patch moved the bind to `0.0.0.0`. Latent risk only (a future maintainer could add an approval path trusting a guarantee that no longer holds), so the fix was a surgical docstring correction stating the real model — the bearer token, not the bind address, is the trust boundary; never expose approval on an unauthenticated route or non-HTTP channel. No behavioral change; `npx tsc --noEmit` clean. Two cross-references recorded (not fixes): `decidedBy` is hardcoded `'user'` so there's no per-approver identity under a shared token (the separate "audit-log source IP" item would close the forensic gap), and the gate's protection now rests entirely on the bearer token, which is extractable from the public dashboard HTML at `/` (the already-logged token-exposure finding) — both within the accepted home-LAN threat model.*
- **Restore + tighten Helmet CSP** (security review item #3) — replace `connectSrc: ["'self'", "*"]` with an allowlist that matches what the dashboard actually loads; reconsider `upgradeInsecureRequests: null`. *Investigation: the dashboard (`dashboard/dist/index.html`, one inline-JS file served same-origin) fetches only its own origin (`var API = ''`, a relative base), loads no external subresources (no CDN scripts/styles/fonts/images, no `data:`/`blob:` URIs), and opens no browser WebSocket — the lone `io(` grep hit was a false match inside `new Audio(`. The ~17 external `https://` URLs are all `href target="_blank"` navigations, which CSP `connect-src`/`img-src` don't govern. So `connect-src 'self'` is exact. Decisions (confirmed by user): set **`connectSrc: ["'self'"]`** (not `'self'` + CORS origins — CORS governs the inbound direction, not the dashboard's outbound, and an MCP server is a non-browser client that CSP cannot gate regardless); **keep `upgradeInsecureRequests: null` (off)** because the server speaks plain HTTP on the LAN, so forcing `https://` rewrites would break the same-origin dashboard — documented flipping it to `{}` once an HTTPS/reverse-proxy deployment is recommended. Implementation (`gateway/src/index.ts`): tightened the `connectSrc` directive and added comments explaining the `'unsafe-inline'` requirement (inline-JS dashboard), the `'self'`-is-exact rationale, and the `upgradeInsecureRequests` decision. Smoke test Phase 1 gained two assertions via a new `rheader` helper: `connect-src` is exactly `'self'` and contains no permissive `*` (the old `'self' *` value fails both). 18 checks total pass; `npx tsc --noEmit` clean. Next security item: API-level rate limiting.*
- **Source-IP allowlist (`BOOKCLAW_ALLOWED_IPS`)** (security review; new item raised during CORS work) — a network-level gate, distinct from CORS, restricting which *client machines* may reach the server at all (applies to every client, in front of even the bearer token). *Investigation first surfaced a make-or-break caveat: the current Docker deploy (default bridge + published port, no `daemon.json` so `userland-proxy` defaults true) makes the container see the bridge gateway IP for every external client, so an app-level per-IP gate only sees real IPs under host-net/native/VPS or behind a trusted proxy — the most robust enforcement for the VPS "only my home IP" case is the host firewall / provider security group, with this as defense-in-depth. Decisions (confirmed by user): **unset = allow all + a startup notice** (enforcement strictly opt-in; auto-deriving a local /24 was rejected as a trap — in Docker it derives the 172.x container subnet, on a VPS the datacenter subnet, blocking the real client both ways); **always allow loopback** as a recovery path; **opt-in `BOOKCLAW_TRUST_PROXY=1`** reads the client IP from `X-Forwarded-For` for reverse-proxy deploys (off by default; spoofable unless behind a sole-ingress proxy). Implementation (`gateway/src/index.ts`): added `ipaddr.js` as a direct dep (was transitive via `proxy-addr`) for correct IPv4/IPv6/IPv4-mapped + CIDR matching; constructor parses `BOOKCLAW_ALLOWED_IPS` (comma-separated IPs/CIDRs; single IPs → /32 or /128; invalid entries warned and skipped), sets `trust proxy`, and registers an Express gate (403 + audit `ip_blocked` on deny) ahead of the auth middleware; a matching `io.use()` gates the Socket.IO handshake; `isIpAllowed()` uses `ipaddr.process()` to normalize IPv4-mapped addresses and treats `range()==='loopback'` as always-allowed; startup logs the posture (`ℹ IP allowlist: not set …` / `✓ IP allowlist: N rule(s) enforced …`). Smoke test Phase 4 (trust-proxy on, so allow/deny is deterministic from loopback via XFF): exact IP allowed, CIDR member allowed, unlisted IP → 403 even with a valid token (proves the gate is in front of auth), loopback always allowed, and the enforcement log present. 16 checks total pass; `npx tsc --noEmit` clean. Next security item: Helmet CSP.*
- **Tighten CORS once auth lands** (security review item #2) — replace `cors: { origin: '*' }` (Express + Socket.IO) with an explicit allowlist driven by `BOOKCLAW_CORS_ORIGINS`. *Implemented a shared `corsOptions` (one origin-callback used by both Express and the Socket.IO server, so the WebSocket origin check is covered too). Decisions (confirmed by user): **deny all cross-origin by default** when the env var is unset — safe because the dashboard is served same-origin (`var API = ''`) so it never needs CORS, and curl/MCP/bridges are non-browser so CORS doesn't apply; `BOOKCLAW_CORS_ORIGINS=<comma-separated origins>` is the precise override; a literal `*` entry restores permissive CORS as a loudly-logged escape hatch (mirrors `BOOKCLAW_AUTH_DISABLED`). Requests with no `Origin` header (curl/MCP/server-to-server/same-origin) are always allowed since CORS only protects browsers and the bearer token is the real gate. Startup logs the effective posture (`✓ CORS: cross-origin denied …` / `… N allowed origin(s) …` / `⚠ CORS: wildcard …`). Extended `tests/smoke-test.sh` (now 11 checks): default-deny gives no `Access-Control-Allow-Origin`; a new Phase 3 starts with an allowlist and asserts a listed origin is echoed while an unlisted one is not. Manually verified the `*` wildcard path echoes arbitrary origins and logs the warning. `npx tsc --noEmit` clean; `npm run test:smoke` all pass. Note: a distinct, stronger control for this threat model — a **source-IP allowlist** (`BOOKCLAW_ALLOWED_IPS`) — was identified during this work and added to `docs/TODO.md` as the next security item (CORS governs browser origins; an IP allowlist governs which machines can connect at all).*
- **Script the auth smoke test into `tests/` + scripted-tests directive** — turn the manual curl verification of security item #1 into a repeatable script, and add a standing testing directive to the project `CLAUDE.md`. *Added `tests/smoke-test.sh` (`npm run test:smoke`): boots the gateway and asserts the bearer-token gate (401 without token, 200 with bearer, 200 via `?token=` fallback, 401 wrong token, dashboard token injection, and the `BOOKCLAW_AUTH_DISABLED=1` escape hatch). Hermetic/non-destructive — token supplied via env so `.env` is never written, binds loopback only, launches `node --import tsx` directly (not `npm start`) so the PID is killable, EXIT-trap cleanup, `-v` streams the captured server log. Added a `## Testing` section to `CLAUDE.md` with two directives — tests must be scripted/repeatable, and debug logging must be available for testing — and corrected the now-stale "no test suite / no `tests/` directory / don't invent test commands" statements in the intro and Commands sections. Verified: `npm run test:smoke` → all 8 checks pass, exit 0, `.env` unchanged, no stray process.*
- **Add HTTP/WebSocket authentication** (security review item #1) — any host on the LAN could drive the agent, read the workspace, and trigger confirmation-gate items. Pick a model — bearer token in env var (simplest), shared-secret HMAC on every request, or mTLS — and enforce it on every Express route and on the Socket.IO `connection` handshake (`socket.handshake.auth.token`). *Implemented option (A), bearer token. Design decisions (all confirmed by user): missing token auto-generates and persists to `.env` (mirrors `BOOKCLAW_VAULT_KEY`); `BOOKCLAW_AUTH_DISABLED=1` escape hatch with a loud startup warning; token stored plaintext in `.env`; Telegram/Discord bridges left on their own platform auth. Changes — `gateway/src/index.ts`: token load/generate in Phase 2c, Express middleware gating `/api/*` (constant-time compare, accepts `Authorization: Bearer` or `?token=` query fallback for native-element GETs), `io.use()` handshake gate, and `/` now serves the dashboard HTML with the token substituted into a `__BOOKCLAW_AUTH_TOKEN__` placeholder (`express.static` set `index:false` so `/` reaches the injector). `dashboard/dist/index.html`: `AUTH_TOKEN` global + `authHeaders()`/`authUrl()` helpers; both fetch wrappers (`api`, `apiRaw`) send the bearer header; the three native-element URLs (project-file `<img>`, download `<a>`, TTS `<Audio>`) use `?token=`. Smoke test (`npm start`): no token → 401, bearer → 200, `?token=` → 200, wrong token → 401, `/` loads with placeholder substituted; with `BOOKCLAW_AUTH_DISABLED=1` → warning fires and `/api/status` → 200 unauthenticated. `npx tsc --noEmit` clean.*
- **Stale version banner in `gateway/src/index.ts`** — console banner prints `v3.0.0` (around line 202), but `package.json` is `4.0.0`. Update the banner string. *Fix: bumped to `v5.0.0` as part of the new-fork version bump (package.json + package-lock.json + banner all moved 4.0.0 → 5.0.0).*
- **Stale version in `QUICKSTART.md`** — sample startup output also shows `BookClaw v3.0.0`. Update to match. *Fix: bumped to `v5.0.0` alongside the index.ts banner.*
- **Stale localhost-only claim in `README.md:338`** — Security feature bullet read `**Localhost Only**: Server binds to 127.0.0.1 (no external access)`, contradicting the fork's `0.0.0.0` default already documented later in the same file. *Fix: replaced with a "Configurable Bind" bullet that points to the Deployment — Defense in Depth section for the full fork posture.*
- **Stale `SECURITY.md` reference in `TODO.md` standing constraints** — note claimed `SECURITY.md` still described the old localhost-only contract, but `SECURITY.md` had already been updated to describe the `0.0.0.0` default (lines 63, 70, 74). *Fix: removed the obsolete "SECURITY.md and the README still describe the old contract" trailer from the standing-constraints bullet (the README fix above resolved the other half).*
- **Stale historical comment in `gateway/src/skills/loader.ts`** — comment explains that the `ops` category was once missing from the load list. The bug is fixed; delete the comment. *Fix: deleted the 3-line comment at `loader.ts:40-42`. Also removed two now-orphaned entries from CLAUDE.md's "Things that look broken but aren't" section: the `v3.0.0` banner note (resolved by the 5.0.0 version bump earlier today) and the `ops`-comment-historical-context note (resolved by this deletion).*
- **Windows path in `LAUNCH-GUIDE.md`** — section "Local PC (Windows — Direct)" references `C:\Users\chris\...`. Either generalize it or move under a per-user-example heading; right now it reads like documentation of a specific machine. *Fix: applied option (a) — replaced the literal path at `LAUNCH-GUIDE.md:11` with the placeholder `C:\path\to\bookclaw`. Added a follow-up TODO under "Investigations" to reconsider Windows as a targeted platform (may go Mac/Linux/Docker-only with Windows routed through WSL2 or Docker Desktop).*
- **`npm audit` / dependency check.** No tests, no CI; vulnerable transitive deps could ship silently. Run `npm audit --omit=dev` and triage. *Fix: ran `npm audit --omit=dev`, found 9 vulnerabilities (3 high, 6 moderate) in `multer`, `path-to-regexp`, `socket.io-parser`, `qs` (via body-parser/express), and `ws` (via engine.io/socket.io-adapter). All in-range — `npm audit fix` (no `--force`) resolved all 9 without touching `package.json` semver ranges; `package-lock.json` updated. Smoke test: `npm start` → server ready in 1s, `/api/status` returned valid JSON, 29 skills loaded, all subsystems clean, banner correctly shows `v5.0.0`.*
- **Confirm `OpenClaw` / `openclaw` references are intentional** — they appear in `package.json` keywords, README, and inline comments. Decide whether to keep them as fork attribution or scrub the ones that are no longer accurate. *Outcome: audited all refs across `*.ts`, `*.js`, `*.json`, `*.md`. Categorised into three buckets — fork attribution (4: `package.json` keyword, `index.ts` header + banner, CLAUDE.md note), "Inspired by" feature credits (5: `router.ts`, `index.ts`, `routes.ts` ×2, `tts.ts`), and roadmap/analysis docs (`OPENCLAW-UPDATES.md`, `GOD-CLASS-REFACTOR.md`, `STORYHACKERAI-PORTING.md`, README links). No obsolete refs found — all intentional. Single small fix: `CLAUDE.md:173` claimed `OpenClawWorld` refs exist in code, but grep finds zero; tightened that line to describe the two real reference categories (fork attribution + inspired-by credits).*
- **Decide whether `npm run build` (tsc emit to `dist/`) is still needed** — dev and production both run through `tsx`. If `dist/` is never consumed, the script and `tsconfig` emit settings can be simplified. *Outcome: TODO premise was wrong. `docker/Dockerfile` is a multi-stage build — stage 1 compiles via `npx tsc --outDir dist`, stage 2 runs `node dist/gateway/src/index.js` as the production CMD. So `dist/` IS consumed in production (Docker images), `tsconfig.json` emit settings are load-bearing, and `npm run build` is a useful local alias for the same compile step. Nothing to simplify. Tightened CLAUDE.md's `npm run build` Commands-section line from "rarely needed; only for packaging" to call out the Docker dependency explicitly.*
- **Stale localhost-only claims in `docs/LAUNCH-GUIDE.md`** — line 162 (Ports table: `BookClaw | 3847 | localhost only`) and line 172 (Security Checklist: `Server binds to 127.0.0.1 (default — don't change)`) both contradict the fork's `0.0.0.0` default and the same file's own "Network access" section (lines 68-96). Update both to match the actual posture (configurable bind via `BOOKCLAW_BIND`, defaulting to `0.0.0.0`). *Fix: Ports table cell now describes the `BOOKCLAW_BIND`-driven default-`0.0.0.0` behavior; Security Checklist item replaced with two items — one for "bind appropriate for environment" and one for "tunnel or auth proxy in front of any non-trusted-LAN deployment". Ollama's localhost-only row left as-is (still correct for the local Ollama daemon).*
- **Reconsider Windows support as a targeted platform.** `LAUNCH-GUIDE.md` currently documents a "Local PC (Windows — Direct)" path alongside Linux/Docker. The fork may end up Mac/Linux-only (via Docker or local install), with Windows users routed through WSL2 or Docker Desktop rather than direct `npm start`. *Outcome: applied option (C) — docs-and-intent only, no code surgery. Audited the surface: `process.platform === 'win32'` branches exist in 3 source files (`services/website-deploy.ts`, `services/external-tools.ts`, `security/vault.ts`), `.ps1` VM-provisioning helpers exist in `scripts/`, and `setup-wizard.sh` has OSTYPE detection. README has no Windows-direct install refs. Decision: leave inherited code branches in place (they don't hurt Linux/Mac/Docker), keep the `.ps1` helpers (they're VM-admin tooling, not app-runtime), and update `LAUNCH-GUIDE.md` to drop the "Local PC (Windows — Direct)" section in favour of a "Windows — via Docker Desktop or WSL2" section pointing at the existing Linux/Docker flows. Surfaces the actual supported posture without invasive change. Spotted two unrelated stale `localhost only` / `127.0.0.1 (default — don't change)` lines in the same file (lines 162 and 172) — added as a new Quick-cleanup TODO entry.*
